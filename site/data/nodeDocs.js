window.NODE_DOCS = {
  "agent": {
    "title": "AgentNode",
    "description": "",
    "parameters": "*   `goal`: The high-level goal for the agent to achieve.\n*   `llm`: The LLM node to use for reasoning.\n*   `tools`: An object mapping tool names to their corresponding node instances.",
    "returns": "Returns the final agent output (finish tool output) or an error message if max steps are reached.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DuckDuckGoSearchNode, ShellCommandNode, ReadFileNode, WriteFileNode, HttpRequestNode, ScrapeURLNode, UserInputNode, AgentNode, AgentDeepSeekLLMNode } from '@fractal-solutions/qflow/nodes';\n\n// Ensure your DeepSeek API Key is set as an environment variable\nconst DEEPSEEK_API_KEY = process.env.DEEPSEEK_API_KEY;\n\n(async () => {\n  if (!DEEPSEEK_API_KEY) {\n    console.warn(\"WARNING: DEEPSEEK_API_KEY is not set. Please set it to run the AgentNode example.\");\n    return;\n  }\n\n  console.log('--- Running AgentNode Test Workflow ---');\n\n  // 1. Instantiate the LLM for the agent's reasoning\n  const agentLLM = new AgentDeepSeekLLMNode();\n  agentLLM.setParams({ apiKey: DEEPSEEK_API_KEY });\n\n  // 2. Instantiate the tools the agent can use\n  const duckduckgoSearch = new DuckDuckGoSearchNode();\n  const shellCommand = new ShellCommandNode();\n  const readFile = new ReadFileNode();\n  const writeFile = new WriteFileNode();\n  const httpRequest = new HttpRequestNode();\n  const webScraper = new ScrapeURLNode();\n  const userInput = new UserInputNode();\n\n  // Map tool names to their instances\n  const availableTools = {\n    duckduckgo_search: duckduckgoSearch,\n    shell_command: shellCommand,\n    read_file: readFile,\n    write_file: writeFile,\n    http_request: httpRequest,\n    web_scraper: webScraper,\n    user_input: userInput,\n    // Add other tools as needed\n  };\n\n  // 3. Instantiate the AgentNode\n  const agent = new AgentNode(agentLLM, availableTools);\n  agent.setParams({\n    goal: \"Find out what the current working directory is, then create a file named 'agent_output.txt' in it with the content 'Hello from the agent!'. Finally, confirm the file exists by listing the directory contents.\"\n  });\n\n  // 4. Create and run the flow with the AgentNode as the start\n  const agentFlow = new AsyncFlow(agent);\n\n  try {\n    const finalResult = await agentFlow.runAsync({});\n    console.log('\\n--- AgentNode Test Workflow Finished ---');\n    console.log('Final Agent Output:', finalResult);\n  } catch (error) {\n    console.error('\\n--- AgentNode Test Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DuckDuckGoSearchNode, ShellCommandNode, ReadFileNode, WriteFileNode, HttpRequestNode, ScrapeURLNode, UserInputNode, AgentNode, AgentDeepSeekLLMNode } from '@fractal-solutions/qflow/nodes';\n\n// Ensure your DeepSeek API Key is set as an environment variable\nconst DEEPSEEK_API_KEY = process.env.DEEPSEEK_API_KEY;\n\n(async () => {\n  if (!DEEPSEEK_API_KEY) {\n    console.warn(\"WARNING: DEEPSEEK_API_KEY is not set. Please set it to run the AgentNode example.\");\n    return;\n  }\n\n  console.log('--- Running AgentNode Test Workflow ---');\n\n  // 1. Instantiate the LLM for the agent's reasoning\n  const agentLLM = new AgentDeepSeekLLMNode();\n  agentLLM.setParams({ apiKey: DEEPSEEK_API_KEY });\n\n  // 2. Instantiate the tools the agent can use\n  const duckduckgoSearch = new DuckDuckGoSearchNode();\n  const shellCommand = new ShellCommandNode();\n  const readFile = new ReadFileNode();\n  const writeFile = new WriteFileNode();\n  const httpRequest = new HttpRequestNode();\n  const webScraper = new ScrapeURLNode();\n  const userInput = new UserInputNode();\n\n  // Map tool names to their instances\n  const availableTools = {\n    duckduckgo_search: duckduckgoSearch,\n    shell_command: shellCommand,\n    read_file: readFile,\n    write_file: writeFile,\n    http_request: httpRequest,\n    web_scraper: webScraper,\n    user_input: userInput,\n    // Add other tools as needed\n  };\n\n  // 3. Instantiate the AgentNode\n  const agent = new AgentNode(agentLLM, availableTools);\n  agent.setParams({\n    goal: \"Find out what the current working directory is, then create a file named 'agent_output.txt' in it with the content 'Hello from the agent!'. Finally, confirm the file exists by listing the directory contents.\"\n  });\n\n  // 4. Create and run the flow with the AgentNode as the start\n  const agentFlow = new AsyncFlow(agent);\n\n  try {\n    const finalResult = await agentFlow.runAsync({});\n    console.log('\\n--- AgentNode Test Workflow Finished ---');\n    console.log('Final Agent Output:', finalResult);\n  } catch (error) {\n    console.error('\\n--- AgentNode Test Workflow Failed ---', error);\n  }\n})();",
    "raw": "## AgentNode\n\nThe `AgentNode` is the core of agentic behavior in qflow. It orchestrates tools and LLM reasoning to achieve complex goals.\n\n### Parameters\n\n*   `goal`: The high-level goal for the agent to achieve.\n*   `llm`: The LLM node to use for reasoning.\n*   `tools`: An object mapping tool names to their corresponding node instances.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DuckDuckGoSearchNode, ShellCommandNode, ReadFileNode, WriteFileNode, HttpRequestNode, ScrapeURLNode, UserInputNode, AgentNode, AgentDeepSeekLLMNode } from '@fractal-solutions/qflow/nodes';\n\n// Ensure your DeepSeek API Key is set as an environment variable\nconst DEEPSEEK_API_KEY = process.env.DEEPSEEK_API_KEY;\n\n(async () => {\n  if (!DEEPSEEK_API_KEY) {\n    console.warn(\"WARNING: DEEPSEEK_API_KEY is not set. Please set it to run the AgentNode example.\");\n    return;\n  }\n\n  console.log('--- Running AgentNode Test Workflow ---');\n\n  // 1. Instantiate the LLM for the agent's reasoning\n  const agentLLM = new AgentDeepSeekLLMNode();\n  agentLLM.setParams({ apiKey: DEEPSEEK_API_KEY });\n\n  // 2. Instantiate the tools the agent can use\n  const duckduckgoSearch = new DuckDuckGoSearchNode();\n  const shellCommand = new ShellCommandNode();\n  const readFile = new ReadFileNode();\n  const writeFile = new WriteFileNode();\n  const httpRequest = new HttpRequestNode();\n  const webScraper = new ScrapeURLNode();\n  const userInput = new UserInputNode();\n\n  // Map tool names to their instances\n  const availableTools = {\n    duckduckgo_search: duckduckgoSearch,\n    shell_command: shellCommand,\n    read_file: readFile,\n    write_file: writeFile,\n    http_request: httpRequest,\n    web_scraper: webScraper,\n    user_input: userInput,\n    // Add other tools as needed\n  };\n\n  // 3. Instantiate the AgentNode\n  const agent = new AgentNode(agentLLM, availableTools);\n  agent.setParams({\n    goal: \"Find out what the current working directory is, then create a file named 'agent_output.txt' in it with the content 'Hello from the agent!'. Finally, confirm the file exists by listing the directory contents.\"\n  });\n\n  // 4. Create and run the flow with the AgentNode as the start\n  const agentFlow = new AsyncFlow(agent);\n\n  try {\n    const finalResult = await agentFlow.runAsync({});\n    console.log('\\n--- AgentNode Test Workflow Finished ---');\n    console.log('Final Agent Output:', finalResult);\n  } catch (error) {\n    console.error('\\n--- AgentNode Test Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "append_file": {
    "title": "AppendFileNode",
    "description": "/**",
    "parameters": "*   `filePath`: The absolute path to the file to append to.\n*   `content`: The content to append to the file.",
    "returns": "{Promise<void>} A promise that resolves when the content has been appended.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { WriteFileNode, AppendFileNode, ReadFileNode, ListDirectoryNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running File System Test Workflow ---');\n\n  const writeFile = new WriteFileNode();\n  writeFile.setParams({\n    filePath: './test.txt',\n    content: 'Hello, qflow!'\n  });\n\n  const appendFile = new AppendFileNode();\n  appendFile.setParams({\n    filePath: './test.txt',\n    content: '\\nThis is a new line.'\n  });\n\n  const readFile = new ReadFileNode();\n  readFile.setParams({\n    filePath: './test.txt'\n  });\n\n  const listDir = new ListDirectoryNode();\n  listDir.setParams({\n    directoryPath: '.'\n  });\n\n  writeFile.next(appendFile);\n  appendFile.next(readFile);\n  readFile.next(listDir);\n\n  const fileSystemFlow = new AsyncFlow(writeFile);\n\n  try {\n    const result = await fileSystemFlow.runAsync({});\n    console.log('--- File System Test Workflow Finished ---');\n    console.log('Final Result:', result);\n  } catch (error) {\n    console.error('--- File System Test Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { WriteFileNode, AppendFileNode, ReadFileNode, ListDirectoryNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running File System Test Workflow ---');\n\n  const writeFile = new WriteFileNode();\n  writeFile.setParams({\n    filePath: './test.txt',\n    content: 'Hello, qflow!'\n  });\n\n  const appendFile = new AppendFileNode();\n  appendFile.setParams({\n    filePath: './test.txt',\n    content: '\\nThis is a new line.'\n  });\n\n  const readFile = new ReadFileNode();\n  readFile.setParams({\n    filePath: './test.txt'\n  });\n\n  const listDir = new ListDirectoryNode();\n  listDir.setParams({\n    directoryPath: '.'\n  });\n\n  writeFile.next(appendFile);\n  appendFile.next(readFile);\n  readFile.next(listDir);\n\n  const fileSystemFlow = new AsyncFlow(writeFile);\n\n  try {\n    const result = await fileSystemFlow.runAsync({});\n    console.log('--- File System Test Workflow Finished ---');\n    console.log('Final Result:', result);\n  } catch (error) {\n    console.error('--- File System Test Workflow Failed ---', error);\n  }\n})();",
    "raw": "## AppendFileNode\n\nThe `AppendFileNode` appends content to an existing file.\n\n### Parameters\n\n*   `filePath`: The absolute path to the file to append to.\n*   `content`: The content to append to the file.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { WriteFileNode, AppendFileNode, ReadFileNode, ListDirectoryNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running File System Test Workflow ---');\n\n  const writeFile = new WriteFileNode();\n  writeFile.setParams({\n    filePath: './test.txt',\n    content: 'Hello, qflow!'\n  });\n\n  const appendFile = new AppendFileNode();\n  appendFile.setParams({\n    filePath: './test.txt',\n    content: '\\nThis is a new line.'\n  });\n\n  const readFile = new ReadFileNode();\n  readFile.setParams({\n    filePath: './test.txt'\n  });\n\n  const listDir = new ListDirectoryNode();\n  listDir.setParams({\n    directoryPath: '.'\n  });\n\n  writeFile.next(appendFile);\n  appendFile.next(readFile);\n  readFile.next(listDir);\n\n  const fileSystemFlow = new AsyncFlow(writeFile);\n\n  try {\n    const result = await fileSystemFlow.runAsync({});\n    console.log('--- File System Test Workflow Finished ---');\n    console.log('Final Result:', result);\n  } catch (error) {\n    console.error('--- File System Test Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "browser_control": {
    "title": "BrowserControlNode",
    "description": "*   The node reuses `shared.browser` and `shared.page` across calls.\n*   On failures (except while closing), it automatically closes and clears browser state from `shared`.\n*   `evaluate` returns the raw result of `page.evaluate(...)`.",
    "parameters": "*   `action` (required): One of `goto`, `click`, `type`, `screenshot`, `evaluate`, `close`.\n*   `url`: The URL to navigate to (for `goto`).\n*   `selector`: A CSS selector to target an element (for `click` and `type`).\n*   `text`: The text to type into an input field (for `type`).\n*   `path`: The file path to save a screenshot (for `screenshot`).\n*   `script`: JavaScript to evaluate in the page context (for `evaluate`).\n*   `browserType`: Browser engine used when launching a new browser (`chromium`, `firefox`, `webkit`). Default is `chromium`.\n*   `launchOptions`: Playwright launch options used when creating a browser. Default is `{ headless: true }`.",
    "returns": "Returns an action result. For evaluate it returns the evaluated value; for other actions it returns a message object.",
    "notes": "*   The node reuses `shared.browser` and `shared.page` across calls.\n*   On failures (except while closing), it automatically closes and clears browser state from `shared`.\n*   `evaluate` returns the raw result of `page.evaluate(...)`.",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { BrowserControlNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  const shared = {};\n\n  const html = `\n    <html>\n      <body>\n        <input id=\"name\" />\n        <button id=\"save\" onclick=\"document.body.dataset.saved='1'\">Save</button>\n      </body>\n    </html>\n  `;\n\n  // 1) Open page\n  const gotoSite = new BrowserControlNode();\n  gotoSite.setParams({\n    action: 'goto',\n    url: `data:text/html,${encodeURIComponent(html)}`,\n    browserType: 'chromium',\n    launchOptions: { headless: true },\n  });\n\n  // 2) Fill input\n  const typeName = new BrowserControlNode();\n  typeName.setParams({\n    action: 'type',\n    selector: '#name',\n    text: 'Qflow',\n  });\n\n  // 3) Click button\n  const clickSave = new BrowserControlNode();\n  clickSave.setParams({\n    action: 'click',\n    selector: '#save',\n  });\n\n  // 4) Evaluate DOM state\n  const evaluateState = new BrowserControlNode();\n  evaluateState.setParams({\n    action: 'evaluate',\n    script: '({ saved: document.body.dataset.saved, value: document.querySelector(\"#name\").value })',\n  });\n  evaluateState.postAsync = async (sharedState, _, execRes) => {\n    sharedState.evaluateResult = execRes;\n    return 'default';\n  };\n\n  class LogResultNode extends AsyncNode {\n    async execAsync(_, sharedState) {\n      console.log('Evaluate result:', sharedState.evaluateResult);\n      return 'default';\n    }\n  }\n  const logResult = new LogResultNode();\n\n  // 5) Screenshot\n  const screenshot = new BrowserControlNode();\n  screenshot.setParams({\n    action: 'screenshot',\n    path: './browser_control_demo.png',\n  });\n\n  // 6) Close browser\n  const closeBrowser = new BrowserControlNode();\n  closeBrowser.setParams({\n    action: 'close',\n  });\n\n  gotoSite\n    .next(typeName)\n    .next(clickSave)\n    .next(evaluateState)\n    .next(logResult)\n    .next(screenshot)\n    .next(closeBrowser);\n\n  const browserFlow = new AsyncFlow(gotoSite);\n  await browserFlow.runAsync(shared);\n})();\n```",
    "example": "import { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { BrowserControlNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  const shared = {};\n\n  const html = `\n    <html>\n      <body>\n        <input id=\"name\" />\n        <button id=\"save\" onclick=\"document.body.dataset.saved='1'\">Save</button>\n      </body>\n    </html>\n  `;\n\n  // 1) Open page\n  const gotoSite = new BrowserControlNode();\n  gotoSite.setParams({\n    action: 'goto',\n    url: `data:text/html,${encodeURIComponent(html)}`,\n    browserType: 'chromium',\n    launchOptions: { headless: true },\n  });\n\n  // 2) Fill input\n  const typeName = new BrowserControlNode();\n  typeName.setParams({\n    action: 'type',\n    selector: '#name',\n    text: 'Qflow',\n  });\n\n  // 3) Click button\n  const clickSave = new BrowserControlNode();\n  clickSave.setParams({\n    action: 'click',\n    selector: '#save',\n  });\n\n  // 4) Evaluate DOM state\n  const evaluateState = new BrowserControlNode();\n  evaluateState.setParams({\n    action: 'evaluate',\n    script: '({ saved: document.body.dataset.saved, value: document.querySelector(\"#name\").value })',\n  });\n  evaluateState.postAsync = async (sharedState, _, execRes) => {\n    sharedState.evaluateResult = execRes;\n    return 'default';\n  };\n\n  class LogResultNode extends AsyncNode {\n    async execAsync(_, sharedState) {\n      console.log('Evaluate result:', sharedState.evaluateResult);\n      return 'default';\n    }\n  }\n  const logResult = new LogResultNode();\n\n  // 5) Screenshot\n  const screenshot = new BrowserControlNode();\n  screenshot.setParams({\n    action: 'screenshot',\n    path: './browser_control_demo.png',\n  });\n\n  // 6) Close browser\n  const closeBrowser = new BrowserControlNode();\n  closeBrowser.setParams({\n    action: 'close',\n  });\n\n  gotoSite\n    .next(typeName)\n    .next(clickSave)\n    .next(evaluateState)\n    .next(logResult)\n    .next(screenshot)\n    .next(closeBrowser);\n\n  const browserFlow = new AsyncFlow(gotoSite);\n  await browserFlow.runAsync(shared);\n})();",
    "raw": "## BrowserControlNode\n\nThe `BrowserControlNode` controls a Playwright browser to navigate pages, interact with elements, evaluate scripts, take screenshots, and close browser sessions.\n\n### Parameters\n\n*   `action` (required): One of `goto`, `click`, `type`, `screenshot`, `evaluate`, `close`.\n*   `url`: The URL to navigate to (for `goto`).\n*   `selector`: A CSS selector to target an element (for `click` and `type`).\n*   `text`: The text to type into an input field (for `type`).\n*   `path`: The file path to save a screenshot (for `screenshot`).\n*   `script`: JavaScript to evaluate in the page context (for `evaluate`).\n*   `browserType`: Browser engine used when launching a new browser (`chromium`, `firefox`, `webkit`). Default is `chromium`.\n*   `launchOptions`: Playwright launch options used when creating a browser. Default is `{ headless: true }`.\n\n### Notes\n\n*   The node reuses `shared.browser` and `shared.page` across calls.\n*   On failures (except while closing), it automatically closes and clears browser state from `shared`.\n*   `evaluate` returns the raw result of `page.evaluate(...)`.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { BrowserControlNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  const shared = {};\n\n  const html = `\n    <html>\n      <body>\n        <input id=\"name\" />\n        <button id=\"save\" onclick=\"document.body.dataset.saved='1'\">Save</button>\n      </body>\n    </html>\n  `;\n\n  // 1) Open page\n  const gotoSite = new BrowserControlNode();\n  gotoSite.setParams({\n    action: 'goto',\n    url: `data:text/html,${encodeURIComponent(html)}`,\n    browserType: 'chromium',\n    launchOptions: { headless: true },\n  });\n\n  // 2) Fill input\n  const typeName = new BrowserControlNode();\n  typeName.setParams({\n    action: 'type',\n    selector: '#name',\n    text: 'Qflow',\n  });\n\n  // 3) Click button\n  const clickSave = new BrowserControlNode();\n  clickSave.setParams({\n    action: 'click',\n    selector: '#save',\n  });\n\n  // 4) Evaluate DOM state\n  const evaluateState = new BrowserControlNode();\n  evaluateState.setParams({\n    action: 'evaluate',\n    script: '({ saved: document.body.dataset.saved, value: document.querySelector(\"#name\").value })',\n  });\n  evaluateState.postAsync = async (sharedState, _, execRes) => {\n    sharedState.evaluateResult = execRes;\n    return 'default';\n  };\n\n  class LogResultNode extends AsyncNode {\n    async execAsync(_, sharedState) {\n      console.log('Evaluate result:', sharedState.evaluateResult);\n      return 'default';\n    }\n  }\n  const logResult = new LogResultNode();\n\n  // 5) Screenshot\n  const screenshot = new BrowserControlNode();\n  screenshot.setParams({\n    action: 'screenshot',\n    path: './browser_control_demo.png',\n  });\n\n  // 6) Close browser\n  const closeBrowser = new BrowserControlNode();\n  closeBrowser.setParams({\n    action: 'close',\n  });\n\n  gotoSite\n    .next(typeName)\n    .next(clickSave)\n    .next(evaluateState)\n    .next(logResult)\n    .next(screenshot)\n    .next(closeBrowser);\n\n  const browserFlow = new AsyncFlow(gotoSite);\n  await browserFlow.runAsync(shared);\n})();\n```"
  },
  "code_interpreter": {
    "title": "CodeInterpreterNode",
    "description": "",
    "parameters": "*   `code`: The Python code snippet to execute.\n*   `timeout`: Maximum execution time in milliseconds.\n*   `args`: Command-line arguments to pass to the script.\n*   `requireConfirmation`: If true, the user will be prompted for confirmation before executing the code.",
    "returns": "Returns { stdout, stderr, exitCode } and writes shared.codeInterpreterResult.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { CodeInterpreterNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running Python CodeInterpreterNode Test Workflow ---');\n\n  // --- Test 1: Simple Python Execution ---\n  console.log('\\n--- Test 1: Simple Python Execution ---');\n  const pythonCode = `\nprint(\"Hello from Python!\")\n`;\n\n  const pythonInterpreterNode = new CodeInterpreterNode();\n  pythonInterpreterNode.setParams({\n    code: pythonCode,\n    timeout: 5000, // 5 seconds timeout\n    requireConfirmation: true\n  });\n\n  pythonInterpreterNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('Python Execution Result:');\n    console.log('  Stdout:', execRes.stdout);\n    console.log('  Stderr:', execRes.stderr);\n    console.log('  Exit Code:', execRes.exitCode);\n    if (execRes.stdout.includes(\"Hello from Python!\") && execRes.exitCode === 0) {\n      console.log('Test 1 Passed: Python output as expected.');\n    } else {\n      console.error('Test 1 Failed: Unexpected Python output or non-zero exit code.');\n    }\n    return 'default';\n  };\n\n  const pythonFlow = new AsyncFlow(pythonInterpreterNode);\n  try {\n    await pythonFlow.runAsync({});\n  } catch (error) {\n    console.error('Test 1 Failed: Python Execution Flow Failed:', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { CodeInterpreterNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running Python CodeInterpreterNode Test Workflow ---');\n\n  // --- Test 1: Simple Python Execution ---\n  console.log('\\n--- Test 1: Simple Python Execution ---');\n  const pythonCode = `\nprint(\"Hello from Python!\")\n`;\n\n  const pythonInterpreterNode = new CodeInterpreterNode();\n  pythonInterpreterNode.setParams({\n    code: pythonCode,\n    timeout: 5000, // 5 seconds timeout\n    requireConfirmation: true\n  });\n\n  pythonInterpreterNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('Python Execution Result:');\n    console.log('  Stdout:', execRes.stdout);\n    console.log('  Stderr:', execRes.stderr);\n    console.log('  Exit Code:', execRes.exitCode);\n    if (execRes.stdout.includes(\"Hello from Python!\") && execRes.exitCode === 0) {\n      console.log('Test 1 Passed: Python output as expected.');\n    } else {\n      console.error('Test 1 Failed: Unexpected Python output or non-zero exit code.');\n    }\n    return 'default';\n  };\n\n  const pythonFlow = new AsyncFlow(pythonInterpreterNode);\n  try {\n    await pythonFlow.runAsync({});\n  } catch (error) {\n    console.error('Test 1 Failed: Python Execution Flow Failed:', error);\n  }\n})();",
    "raw": "## CodeInterpreterNode\n\nThe `CodeInterpreterNode` executes Python code snippets.\n\n### Parameters\n\n*   `code`: The Python code snippet to execute.\n*   `timeout`: Maximum execution time in milliseconds.\n*   `args`: Command-line arguments to pass to the script.\n*   `requireConfirmation`: If true, the user will be prompted for confirmation before executing the code.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { CodeInterpreterNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running Python CodeInterpreterNode Test Workflow ---');\n\n  // --- Test 1: Simple Python Execution ---\n  console.log('\\n--- Test 1: Simple Python Execution ---');\n  const pythonCode = `\nprint(\"Hello from Python!\")\n`;\n\n  const pythonInterpreterNode = new CodeInterpreterNode();\n  pythonInterpreterNode.setParams({\n    code: pythonCode,\n    timeout: 5000, // 5 seconds timeout\n    requireConfirmation: true\n  });\n\n  pythonInterpreterNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('Python Execution Result:');\n    console.log('  Stdout:', execRes.stdout);\n    console.log('  Stderr:', execRes.stderr);\n    console.log('  Exit Code:', execRes.exitCode);\n    if (execRes.stdout.includes(\"Hello from Python!\") && execRes.exitCode === 0) {\n      console.log('Test 1 Passed: Python output as expected.');\n    } else {\n      console.error('Test 1 Failed: Unexpected Python output or non-zero exit code.');\n    }\n    return 'default';\n  };\n\n  const pythonFlow = new AsyncFlow(pythonInterpreterNode);\n  try {\n    await pythonFlow.runAsync({});\n  } catch (error) {\n    console.error('Test 1 Failed: Python Execution Flow Failed:', error);\n  }\n})();\n```"
  },
  "data_extractor": {
    "title": "DataExtractorNode",
    "description": "",
    "parameters": "*   `input`: The content string from which to extract data.\n*   `type`: The type of content to extract from (html, json, or text).\n*   `selector`: A CSS selector to target elements (for HTML).\n*   `jsonPath`: A dot-notation path to extract data (for JSON).\n*   `regex`: A regular expression to match and extract data (for text).\n*   `group`: The capturing group index to return from the regex match.",
    "returns": "Returns extracted data (array, value, or object) and writes shared.extractedData.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncNode, AsyncFlow } from '@fractal-solutions/qflow';\nimport { DataExtractorNode, ScrapeURLNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running DataExtractorNode Test Workflow ---');\n\n  // --- Example 1: Extracting from HTML (using WebScraperNode to get HTML) ---\n  console.log('\\n--- HTML Extraction Example ---');\n  const scrapeNode = new ScrapeURLNode();\n  scrapeNode.setParams({ url: 'https://www.example.com' });\n\n  const extractHtmlNode = new DataExtractorNode();\n  extractHtmlNode.setParams({\n    type: 'html',\n    selector: 'h1' // Extract the text from the first h1 tag\n  });\n\n  scrapeNode.next(extractHtmlNode);\n\n  extractHtmlNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('Extracted HTML (h1 content):', execRes);\n    return 'default';\n  };\n\n  const htmlFlow = new AsyncFlow(scrapeNode);\n  try {\n    const scrapedHtml = await htmlFlow.runAsync({});\n\n    extractHtmlNode.setParams({\n      input: scrapedHtml,\n      type: 'html',\n      selector: 'h1' // Extract the text from the first h1 tag\n    });\n\n    const extractedResult = await new AsyncFlow(extractHtmlNode).runAsync({});\n\n    console.log('Extracted HTML (h1 content):', extractedResult);\n    if (extractedResult && extractedResult[0] === 'Example Domain') {\n      console.log('HTML Extraction Test Passed: h1 content extracted as expected.');\n    } else {\n      console.error('HTML Extraction Test Failed: Unexpected h1 content.', extractedResult);\n    }\n  } catch (error) {\n    console.error('HTML Extraction Flow Failed:', error);\n  }\n})();\n```",
    "example": "import { AsyncNode, AsyncFlow } from '@fractal-solutions/qflow';\nimport { DataExtractorNode, ScrapeURLNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running DataExtractorNode Test Workflow ---');\n\n  // --- Example 1: Extracting from HTML (using WebScraperNode to get HTML) ---\n  console.log('\\n--- HTML Extraction Example ---');\n  const scrapeNode = new ScrapeURLNode();\n  scrapeNode.setParams({ url: 'https://www.example.com' });\n\n  const extractHtmlNode = new DataExtractorNode();\n  extractHtmlNode.setParams({\n    type: 'html',\n    selector: 'h1' // Extract the text from the first h1 tag\n  });\n\n  scrapeNode.next(extractHtmlNode);\n\n  extractHtmlNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('Extracted HTML (h1 content):', execRes);\n    return 'default';\n  };\n\n  const htmlFlow = new AsyncFlow(scrapeNode);\n  try {\n    const scrapedHtml = await htmlFlow.runAsync({});\n\n    extractHtmlNode.setParams({\n      input: scrapedHtml,\n      type: 'html',\n      selector: 'h1' // Extract the text from the first h1 tag\n    });\n\n    const extractedResult = await new AsyncFlow(extractHtmlNode).runAsync({});\n\n    console.log('Extracted HTML (h1 content):', extractedResult);\n    if (extractedResult && extractedResult[0] === 'Example Domain') {\n      console.log('HTML Extraction Test Passed: h1 content extracted as expected.');\n    } else {\n      console.error('HTML Extraction Test Failed: Unexpected h1 content.', extractedResult);\n    }\n  } catch (error) {\n    console.error('HTML Extraction Flow Failed:', error);\n  }\n})();",
    "raw": "## DataExtractorNode\n\nThe `DataExtractorNode` extracts structured data from HTML, JSON, or plain text.\n\n### Parameters\n\n*   `input`: The content string from which to extract data.\n*   `type`: The type of content to extract from (html, json, or text).\n*   `selector`: A CSS selector to target elements (for HTML).\n*   `jsonPath`: A dot-notation path to extract data (for JSON).\n*   `regex`: A regular expression to match and extract data (for text).\n*   `group`: The capturing group index to return from the regex match.\n\n### Example Usage\n\n```javascript\nimport { AsyncNode, AsyncFlow } from '@fractal-solutions/qflow';\nimport { DataExtractorNode, ScrapeURLNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running DataExtractorNode Test Workflow ---');\n\n  // --- Example 1: Extracting from HTML (using WebScraperNode to get HTML) ---\n  console.log('\\n--- HTML Extraction Example ---');\n  const scrapeNode = new ScrapeURLNode();\n  scrapeNode.setParams({ url: 'https://www.example.com' });\n\n  const extractHtmlNode = new DataExtractorNode();\n  extractHtmlNode.setParams({\n    type: 'html',\n    selector: 'h1' // Extract the text from the first h1 tag\n  });\n\n  scrapeNode.next(extractHtmlNode);\n\n  extractHtmlNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('Extracted HTML (h1 content):', execRes);\n    return 'default';\n  };\n\n  const htmlFlow = new AsyncFlow(scrapeNode);\n  try {\n    const scrapedHtml = await htmlFlow.runAsync({});\n\n    extractHtmlNode.setParams({\n      input: scrapedHtml,\n      type: 'html',\n      selector: 'h1' // Extract the text from the first h1 tag\n    });\n\n    const extractedResult = await new AsyncFlow(extractHtmlNode).runAsync({});\n\n    console.log('Extracted HTML (h1 content):', extractedResult);\n    if (extractedResult && extractedResult[0] === 'Example Domain') {\n      console.log('HTML Extraction Test Passed: h1 content extracted as expected.');\n    } else {\n      console.error('HTML Extraction Test Failed: Unexpected h1 content.', extractedResult);\n    }\n  } catch (error) {\n    console.error('HTML Extraction Flow Failed:', error);\n  }\n})();\n```"
  },
  "data_validation": {
    "title": "DataValidationNode",
    "description": "",
    "parameters": "*   `data`: The data to be validated.\n*   `schema`: The JSON Schema object directly.\n*   `schemaPath`: Optional. Path to a JSON Schema file. If provided, 'schema' parameter is ignored.\n*   `action`: The action to perform. Currently only 'validate' is supported.",
    "returns": "Returns a validation result object (valid flag and error details).",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DataValidationNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running DataValidationNode Example ---');\n\n  // --- Define a sample JSON Schema ---\n  const userSchema = {\n    type: \"object\",\n    properties: {\n      id: { type: \"number\" },\n      name: { type: \"string\", minLength: 3 },\n      email: { type: \"string\", format: \"email\" },\n      age: { type: \"number\", minimum: 18 },\n      isActive: { type: \"boolean\" }\n    },\n    required: [\"id\", \"name\", \"email\", \"age\"],\n    additionalProperties: false\n  };\n\n  // --- Example 1: Validate valid data directly with schema object ---\n  console.log('\\n--- Validating VALID data ---');\n  const validData = {\n    id: 1,\n    name: \"Alice Smith\",\n    email: \"alice@example.com\",\n    age: 30,\n    isActive: true\n  };\n\n  const validateValidNode = new DataValidationNode();\n  validateValidNode.setParams({\n    action: 'validate',\n    data: validData,\n    schema: userSchema\n  });\n\n  try {\n    const result = await new AsyncFlow(validateValidNode).runAsync({});\n    console.log('Validation Result (Valid Data):', result);\n  } catch (error) {\n    console.error('Validation Failed (Valid Data - unexpected):', error.message);\n  }\n\n  console.log('\\n--- DataValidationNode Example Finished ---');\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DataValidationNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running DataValidationNode Example ---');\n\n  // --- Define a sample JSON Schema ---\n  const userSchema = {\n    type: \"object\",\n    properties: {\n      id: { type: \"number\" },\n      name: { type: \"string\", minLength: 3 },\n      email: { type: \"string\", format: \"email\" },\n      age: { type: \"number\", minimum: 18 },\n      isActive: { type: \"boolean\" }\n    },\n    required: [\"id\", \"name\", \"email\", \"age\"],\n    additionalProperties: false\n  };\n\n  // --- Example 1: Validate valid data directly with schema object ---\n  console.log('\\n--- Validating VALID data ---');\n  const validData = {\n    id: 1,\n    name: \"Alice Smith\",\n    email: \"alice@example.com\",\n    age: 30,\n    isActive: true\n  };\n\n  const validateValidNode = new DataValidationNode();\n  validateValidNode.setParams({\n    action: 'validate',\n    data: validData,\n    schema: userSchema\n  });\n\n  try {\n    const result = await new AsyncFlow(validateValidNode).runAsync({});\n    console.log('Validation Result (Valid Data):', result);\n  } catch (error) {\n    console.error('Validation Failed (Valid Data - unexpected):', error.message);\n  }\n\n  console.log('\\n--- DataValidationNode Example Finished ---');\n})();",
    "raw": "## DataValidationNode\n\nThe `DataValidationNode` validates structured data against a JSON Schema.\n\n### Parameters\n\n*   `data`: The data to be validated.\n*   `schema`: The JSON Schema object directly.\n*   `schemaPath`: Optional. Path to a JSON Schema file. If provided, 'schema' parameter is ignored.\n*   `action`: The action to perform. Currently only 'validate' is supported.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DataValidationNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running DataValidationNode Example ---');\n\n  // --- Define a sample JSON Schema ---\n  const userSchema = {\n    type: \"object\",\n    properties: {\n      id: { type: \"number\" },\n      name: { type: \"string\", minLength: 3 },\n      email: { type: \"string\", format: \"email\" },\n      age: { type: \"number\", minimum: 18 },\n      isActive: { type: \"boolean\" }\n    },\n    required: [\"id\", \"name\", \"email\", \"age\"],\n    additionalProperties: false\n  };\n\n  // --- Example 1: Validate valid data directly with schema object ---\n  console.log('\\n--- Validating VALID data ---');\n  const validData = {\n    id: 1,\n    name: \"Alice Smith\",\n    email: \"alice@example.com\",\n    age: 30,\n    isActive: true\n  };\n\n  const validateValidNode = new DataValidationNode();\n  validateValidNode.setParams({\n    action: 'validate',\n    data: validData,\n    schema: userSchema\n  });\n\n  try {\n    const result = await new AsyncFlow(validateValidNode).runAsync({});\n    console.log('Validation Result (Valid Data):', result);\n  } catch (error) {\n    console.error('Validation Failed (Valid Data - unexpected):', error.message);\n  }\n\n  console.log('\\n--- DataValidationNode Example Finished ---');\n})();\n```"
  },
  "database": {
    "title": "DatabaseNode",
    "description": "",
    "parameters": "*   `connection`: The connection string for the database. For `sqlite`, this is the path to the database file.\n*   `adapter`: The adapter to use (`sqlite` or `sql`).\n*   `action`: The action to perform.\n*   `query`: The SQL query to execute (for the `query` action).\n*   `table`: The name of the table to operate on.\n*   `data`: The data to insert or update. For `insert`, this is an object. For `bulk_insert`, this is an array of objects.\n*   `where`: The `WHERE` clause for `update` and `delete` actions.\n*   `operations`: An array of operations to execute within a transaction (for the `transaction` action).",
    "returns": "Returns query results or action status depending on the action.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DatabaseNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  // --- Create a table ---\n  const createTable = new DatabaseNode();\n  createTable.setParams({\n    adapter: 'sqlite',\n    connection: ':memory:',\n    action: 'query',\n    query: 'CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT, email TEXT);'\n  });\n\n  // --- Insert a user ---\n  const insertUser = new DatabaseNode();\n  insertUser.setParams({\n    adapter: 'sqlite',\n    connection: ':memory:',\n    action: 'insert',\n    table: 'users',\n    data: { '$name': 'Alice', '$email': 'alice@example.com' }\n  });\n\n  // --- Bulk Insert users ---\n  const bulkInsertUsers = new DatabaseNode();\n  bulkInsertUsers.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'bulk_insert',\n      table: 'users',\n      data: [\n          { '$name': 'Bob', '$email': 'bob@example.com' },\n          { '$name': 'Charlie', '$email': 'charlie@example.com' }\n      ]\n  });\n\n  // --- Query users ---\n  const queryUsers = new DatabaseNode();\n  queryUsers.setParams({\n    adapter: 'sqlite',\n    connection: ':memory:',\n    action: 'query',\n    query: 'SELECT * FROM users;'\n  });\n\n  queryUsers.postAsync = async (shared, prepRes, execRes) => {\n    console.log('--- Users after insert ---');\n    console.log(execRes);\n    return 'default';\n  };\n\n  // --- Update a user ---\n  const updateUser = new DatabaseNode();\n  updateUser.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'update',\n      table: 'users',\n      data: { '$email': 'alice.new@example.com' },\n      where: 'name = \\'Alice\\''\n  });\n\n  // --- Query users after update ---\n  const queryUsersAfterUpdate = new DatabaseNode();\n  queryUsersAfterUpdate.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'query',\n      query: 'SELECT * FROM users;'\n  });\n\n  queryUsersAfterUpdate.postAsync = async (shared, prepRes, execRes) => {\n      console.log('--- Users after update ---');\n      console.log(execRes);\n      return 'default';\n  };\n\n  // --- Delete a user ---\n  const deleteUser = new DatabaseNode();\n  deleteUser.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'delete',\n      table: 'users',\n      where: 'name = \\'Bob\\''\n  });\n\n  // --- Query users after delete ---\n  const queryUsersAfterDelete = new DatabaseNode();\n  queryUsersAfterDelete.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'query',\n      query: 'SELECT * FROM users;'\n  });\n\n  queryUsersAfterDelete.postAsync = async (shared, prepRes, execRes) => {\n      console.log('--- Users after delete ---');\n      console.log(execRes);\n      return 'default';\n  };\n\n\n\n  createTable.next(insertUser)\n      .next(bulkInsertUsers)\n      .next(queryUsers)\n      .next(updateUser)\n      .next(queryUsersAfterUpdate)\n      .next(deleteUser)\n      .next(queryUsersAfterDelete);\n\n  const flow = new AsyncFlow(createTable);\n  await flow.runAsync({});\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DatabaseNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  // --- Create a table ---\n  const createTable = new DatabaseNode();\n  createTable.setParams({\n    adapter: 'sqlite',\n    connection: ':memory:',\n    action: 'query',\n    query: 'CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT, email TEXT);'\n  });\n\n  // --- Insert a user ---\n  const insertUser = new DatabaseNode();\n  insertUser.setParams({\n    adapter: 'sqlite',\n    connection: ':memory:',\n    action: 'insert',\n    table: 'users',\n    data: { '$name': 'Alice', '$email': 'alice@example.com' }\n  });\n\n  // --- Bulk Insert users ---\n  const bulkInsertUsers = new DatabaseNode();\n  bulkInsertUsers.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'bulk_insert',\n      table: 'users',\n      data: [\n          { '$name': 'Bob', '$email': 'bob@example.com' },\n          { '$name': 'Charlie', '$email': 'charlie@example.com' }\n      ]\n  });\n\n  // --- Query users ---\n  const queryUsers = new DatabaseNode();\n  queryUsers.setParams({\n    adapter: 'sqlite',\n    connection: ':memory:',\n    action: 'query',\n    query: 'SELECT * FROM users;'\n  });\n\n  queryUsers.postAsync = async (shared, prepRes, execRes) => {\n    console.log('--- Users after insert ---');\n    console.log(execRes);\n    return 'default';\n  };\n\n  // --- Update a user ---\n  const updateUser = new DatabaseNode();\n  updateUser.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'update',\n      table: 'users',\n      data: { '$email': 'alice.new@example.com' },\n      where: 'name = \\'Alice\\''\n  });\n\n  // --- Query users after update ---\n  const queryUsersAfterUpdate = new DatabaseNode();\n  queryUsersAfterUpdate.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'query',\n      query: 'SELECT * FROM users;'\n  });\n\n  queryUsersAfterUpdate.postAsync = async (shared, prepRes, execRes) => {\n      console.log('--- Users after update ---');\n      console.log(execRes);\n      return 'default';\n  };\n\n  // --- Delete a user ---\n  const deleteUser = new DatabaseNode();\n  deleteUser.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'delete',\n      table: 'users',\n      where: 'name = \\'Bob\\''\n  });\n\n  // --- Query users after delete ---\n  const queryUsersAfterDelete = new DatabaseNode();\n  queryUsersAfterDelete.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'query',\n      query: 'SELECT * FROM users;'\n  });\n\n  queryUsersAfterDelete.postAsync = async (shared, prepRes, execRes) => {\n      console.log('--- Users after delete ---');\n      console.log(execRes);\n      return 'default';\n  };\n\n\n\n  createTable.next(insertUser)\n      .next(bulkInsertUsers)\n      .next(queryUsers)\n      .next(updateUser)\n      .next(queryUsersAfterUpdate)\n      .next(deleteUser)\n      .next(queryUsersAfterDelete);\n\n  const flow = new AsyncFlow(createTable);\n  await flow.runAsync({});\n})();",
    "raw": "## DatabaseNode\n\nThe `DatabaseNode` provides a powerful and flexible way to interact with SQL databases directly within your qflow workflows. It supports both `bun:sqlite` for lightweight, file-based databases and `bun:sql` for connecting to more robust database systems like PostgreSQL and MySQL.\n\n### Adapters\n\nThe `DatabaseNode` supports two adapters:\n\n*   `sqlite`: Uses `bun:sqlite` for interacting with SQLite databases.\n*   `sql`: Uses `bun:sql` for interacting with other SQL databases.\n\n### Actions\n\nThe `DatabaseNode` supports the following actions:\n\n*   `query`: Executes a raw SQL query.\n*   `insert`: Inserts a single record into a table.\n*   `bulk_insert`: Inserts multiple records into a table.\n*   `update`: Updates records in a table based on a `where` clause.\n*   `delete`: Deletes records from a table based on a `where` clause.\n*   `transaction`: Executes a series of SQL operations within a transaction.\n\n### Parameters\n\n*   `connection`: The connection string for the database. For `sqlite`, this is the path to the database file.\n*   `adapter`: The adapter to use (`sqlite` or `sql`).\n*   `action`: The action to perform.\n*   `query`: The SQL query to execute (for the `query` action).\n*   `table`: The name of the table to operate on.\n*   `data`: The data to insert or update. For `insert`, this is an object. For `bulk_insert`, this is an array of objects.\n*   `where`: The `WHERE` clause for `update` and `delete` actions.\n*   `operations`: An array of operations to execute within a transaction (for the `transaction` action).\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DatabaseNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  // --- Create a table ---\n  const createTable = new DatabaseNode();\n  createTable.setParams({\n    adapter: 'sqlite',\n    connection: ':memory:',\n    action: 'query',\n    query: 'CREATE TABLE users (id INTEGER PRIMARY KEY, name TEXT, email TEXT);'\n  });\n\n  // --- Insert a user ---\n  const insertUser = new DatabaseNode();\n  insertUser.setParams({\n    adapter: 'sqlite',\n    connection: ':memory:',\n    action: 'insert',\n    table: 'users',\n    data: { '$name': 'Alice', '$email': 'alice@example.com' }\n  });\n\n  // --- Bulk Insert users ---\n  const bulkInsertUsers = new DatabaseNode();\n  bulkInsertUsers.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'bulk_insert',\n      table: 'users',\n      data: [\n          { '$name': 'Bob', '$email': 'bob@example.com' },\n          { '$name': 'Charlie', '$email': 'charlie@example.com' }\n      ]\n  });\n\n  // --- Query users ---\n  const queryUsers = new DatabaseNode();\n  queryUsers.setParams({\n    adapter: 'sqlite',\n    connection: ':memory:',\n    action: 'query',\n    query: 'SELECT * FROM users;'\n  });\n\n  queryUsers.postAsync = async (shared, prepRes, execRes) => {\n    console.log('--- Users after insert ---');\n    console.log(execRes);\n    return 'default';\n  };\n\n  // --- Update a user ---\n  const updateUser = new DatabaseNode();\n  updateUser.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'update',\n      table: 'users',\n      data: { '$email': 'alice.new@example.com' },\n      where: 'name = \\'Alice\\''\n  });\n\n  // --- Query users after update ---\n  const queryUsersAfterUpdate = new DatabaseNode();\n  queryUsersAfterUpdate.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'query',\n      query: 'SELECT * FROM users;'\n  });\n\n  queryUsersAfterUpdate.postAsync = async (shared, prepRes, execRes) => {\n      console.log('--- Users after update ---');\n      console.log(execRes);\n      return 'default';\n  };\n\n  // --- Delete a user ---\n  const deleteUser = new DatabaseNode();\n  deleteUser.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'delete',\n      table: 'users',\n      where: 'name = \\'Bob\\''\n  });\n\n  // --- Query users after delete ---\n  const queryUsersAfterDelete = new DatabaseNode();\n  queryUsersAfterDelete.setParams({\n      adapter: 'sqlite',\n      connection: ':memory:',\n      action: 'query',\n      query: 'SELECT * FROM users;'\n  });\n\n  queryUsersAfterDelete.postAsync = async (shared, prepRes, execRes) => {\n      console.log('--- Users after delete ---');\n      console.log(execRes);\n      return 'default';\n  };\n\n\n\n  createTable.next(insertUser)\n      .next(bulkInsertUsers)\n      .next(queryUsers)\n      .next(updateUser)\n      .next(queryUsersAfterUpdate)\n      .next(deleteUser)\n      .next(queryUsersAfterDelete);\n\n  const flow = new AsyncFlow(createTable);\n  await flow.runAsync({});\n})();\n```"
  },
  "deepseek_llm": {
    "title": "DeepSeekLLMNode",
    "description": "",
    "parameters": "*   `apiKey`: Your DeepSeek API key.\n*   `prompt`: The prompt to send to the model.\n*   `model`: The model to use.\n*   `temperature`: The temperature to use for the generation.\n*   `max_tokens`: The maximum number of tokens to generate.",
    "returns": "Returns the generated response text.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DeepSeekLLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst DEEPSEEK_API_KEY = process.env.DEEPSEEK_API_KEY;\n\nclass SimpleLLMNode extends DeepSeekLLMNode {\n  // The prompt will be set dynamically from user input\n  preparePrompt() {\n    // No need to prepare here, as the prompt is already in this.params.prompt\n  }\n}\n\n(async () => {\n  if (!DEEPSEEK_API_KEY) {\n    console.warn(`\n      *****************************************************************\n      * WARNING: DeepSeek API key is not set.                         *\n      * Please set the DEEPSEEK_API_KEY environment variable or       *\n      * ensure it's in your .env file to run this example.            *\n      *****************************************************************\n    `);\n    return;\n  }\n\n  console.log('--- Starting Simple DeepSeek LLM Workflow ---');\n\n  const llmNode = new SimpleLLMNode();\n  llmNode.setParams({\n    apiKey: DEEPSEEK_API_KEY,\n    prompt: \"Tell me a short story about a space cat\", // Static prompt for documentation\n    keyword: 'user_query' // A placeholder keyword for logging\n  });\n\n  const llmFlow = new AsyncFlow(llmNode);\n\n  try {\n    console.log(`Sending your prompt to DeepSeek...`);\n    // Use runAsync() directly, passing initial shared state if needed (empty object here)\n    const result = await llmFlow.runAsync({});\n    console.log('\\n--- DeepSeek Response ---');\n    console.log(result);\n    console.log('\\n--- Workflow Finished ---');\n  } catch (error) {\n    console.error('\\n--- Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DeepSeekLLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst DEEPSEEK_API_KEY = process.env.DEEPSEEK_API_KEY;\n\nclass SimpleLLMNode extends DeepSeekLLMNode {\n  // The prompt will be set dynamically from user input\n  preparePrompt() {\n    // No need to prepare here, as the prompt is already in this.params.prompt\n  }\n}\n\n(async () => {\n  if (!DEEPSEEK_API_KEY) {\n    console.warn(`\n      *****************************************************************\n      * WARNING: DeepSeek API key is not set.                         *\n      * Please set the DEEPSEEK_API_KEY environment variable or       *\n      * ensure it's in your .env file to run this example.            *\n      *****************************************************************\n    `);\n    return;\n  }\n\n  console.log('--- Starting Simple DeepSeek LLM Workflow ---');\n\n  const llmNode = new SimpleLLMNode();\n  llmNode.setParams({\n    apiKey: DEEPSEEK_API_KEY,\n    prompt: \"Tell me a short story about a space cat\", // Static prompt for documentation\n    keyword: 'user_query' // A placeholder keyword for logging\n  });\n\n  const llmFlow = new AsyncFlow(llmNode);\n\n  try {\n    console.log(`Sending your prompt to DeepSeek...`);\n    // Use runAsync() directly, passing initial shared state if needed (empty object here)\n    const result = await llmFlow.runAsync({});\n    console.log('\\n--- DeepSeek Response ---');\n    console.log(result);\n    console.log('\\n--- Workflow Finished ---');\n  } catch (error) {\n    console.error('\\n--- Workflow Failed ---', error);\n  }\n})();",
    "raw": "## DeepSeekLLMNode\n\nThe `DeepSeekLLMNode` is used to interact with the DeepSeek API.\n\n### Parameters\n\n*   `apiKey`: Your DeepSeek API key.\n*   `prompt`: The prompt to send to the model.\n*   `model`: The model to use.\n*   `temperature`: The temperature to use for the generation.\n*   `max_tokens`: The maximum number of tokens to generate.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DeepSeekLLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst DEEPSEEK_API_KEY = process.env.DEEPSEEK_API_KEY;\n\nclass SimpleLLMNode extends DeepSeekLLMNode {\n  // The prompt will be set dynamically from user input\n  preparePrompt() {\n    // No need to prepare here, as the prompt is already in this.params.prompt\n  }\n}\n\n(async () => {\n  if (!DEEPSEEK_API_KEY) {\n    console.warn(`\n      *****************************************************************\n      * WARNING: DeepSeek API key is not set.                         *\n      * Please set the DEEPSEEK_API_KEY environment variable or       *\n      * ensure it's in your .env file to run this example.            *\n      *****************************************************************\n    `);\n    return;\n  }\n\n  console.log('--- Starting Simple DeepSeek LLM Workflow ---');\n\n  const llmNode = new SimpleLLMNode();\n  llmNode.setParams({\n    apiKey: DEEPSEEK_API_KEY,\n    prompt: \"Tell me a short story about a space cat\", // Static prompt for documentation\n    keyword: 'user_query' // A placeholder keyword for logging\n  });\n\n  const llmFlow = new AsyncFlow(llmNode);\n\n  try {\n    console.log(`Sending your prompt to DeepSeek...`);\n    // Use runAsync() directly, passing initial shared state if needed (empty object here)\n    const result = await llmFlow.runAsync({});\n    console.log('\\n--- DeepSeek Response ---');\n    console.log(result);\n    console.log('\\n--- Workflow Finished ---');\n  } catch (error) {\n    console.error('\\n--- Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "display_image": {
    "title": "DisplayImageNode",
    "description": "",
    "parameters": "*   `imagePath`: The absolute path to the image file to display.",
    "returns": "Returns { imagePath, stdout, stderr } when the viewer launches.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DisplayImageNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {  \n    console.log('--- Running DisplayImageNode Example ---');  \n    const tempDir = os.tmpdir();  \n    const dummyImagePath = path.join(tempDir, `dummy_image_${Date.now()}.png`);  \n    // --- Create a dummy image file ---  \n    // This is a very simple 1x1 transparent PNG base64 encoded.  \n    // In a real scenario, this would be a generated plot or an existing image.  \n    \n    const base64Png = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNkYAAAAAYAAjCB0C8AAAAASUVORK5CYII=';  \n    const imageBuffer = Buffer.from(base64Png, 'base64');  \n    \n    try {    \n        await fs.writeFile(dummyImagePath, imageBuffer);    \n        console.log(`[Setup] Created dummy image file: ${dummyImagePath}`);  \n    } catch (error) {    \n        console.error('[Setup] Failed to create dummy image file:', error);    \n        return;  \n    }  \n    \n    // --- Display the image ---  \n    console.log('\\n--- Displaying Image ---');  \n    const displayNode = new DisplayImageNode();  \n    displayNode.setParams({    \n        imagePath: dummyImagePath  \n    });  \n\n    try {    \n        await new AsyncFlow(displayNode).runAsync({});    \n        console.log('Image display workflow finished.');  \n    } catch (error) {\n        console.error('Image display workflow Failed:', error.message);\n    } finally {\n        // --- Cleanup ---\n        try {\n            console.log(`\\n[Cleanup] Removing dummy image file: ${dummyImagePath}`);\n            await fs.unlink(dummyImagePath);\n            console.log('[Cleanup] Cleanup complete.');\n        } catch (e) {\n            console.warn('[Cleanup] Failed to remove dummy image file:', e.message);\n        }\n    }\n    console.log('\\n--- DisplayImageNode Example Finished ---');\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DisplayImageNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {  \n    console.log('--- Running DisplayImageNode Example ---');  \n    const tempDir = os.tmpdir();  \n    const dummyImagePath = path.join(tempDir, `dummy_image_${Date.now()}.png`);  \n    // --- Create a dummy image file ---  \n    // This is a very simple 1x1 transparent PNG base64 encoded.  \n    // In a real scenario, this would be a generated plot or an existing image.  \n    \n    const base64Png = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNkYAAAAAYAAjCB0C8AAAAASUVORK5CYII=';  \n    const imageBuffer = Buffer.from(base64Png, 'base64');  \n    \n    try {    \n        await fs.writeFile(dummyImagePath, imageBuffer);    \n        console.log(`[Setup] Created dummy image file: ${dummyImagePath}`);  \n    } catch (error) {    \n        console.error('[Setup] Failed to create dummy image file:', error);    \n        return;  \n    }  \n    \n    // --- Display the image ---  \n    console.log('\\n--- Displaying Image ---');  \n    const displayNode = new DisplayImageNode();  \n    displayNode.setParams({    \n        imagePath: dummyImagePath  \n    });  \n\n    try {    \n        await new AsyncFlow(displayNode).runAsync({});    \n        console.log('Image display workflow finished.');  \n    } catch (error) {\n        console.error('Image display workflow Failed:', error.message);\n    } finally {\n        // --- Cleanup ---\n        try {\n            console.log(`\\n[Cleanup] Removing dummy image file: ${dummyImagePath}`);\n            await fs.unlink(dummyImagePath);\n            console.log('[Cleanup] Cleanup complete.');\n        } catch (e) {\n            console.warn('[Cleanup] Failed to remove dummy image file:', e.message);\n        }\n    }\n    console.log('\\n--- DisplayImageNode Example Finished ---');\n})();",
    "raw": "## DisplayImageNode\n\nThe `DisplayImageNode` displays an image file using the system's default image viewer.\n\n### Parameters\n\n*   `imagePath`: The absolute path to the image file to display.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DisplayImageNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {  \n    console.log('--- Running DisplayImageNode Example ---');  \n    const tempDir = os.tmpdir();  \n    const dummyImagePath = path.join(tempDir, `dummy_image_${Date.now()}.png`);  \n    // --- Create a dummy image file ---  \n    // This is a very simple 1x1 transparent PNG base64 encoded.  \n    // In a real scenario, this would be a generated plot or an existing image.  \n    \n    const base64Png = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNkYAAAAAYAAjCB0C8AAAAASUVORK5CYII=';  \n    const imageBuffer = Buffer.from(base64Png, 'base64');  \n    \n    try {    \n        await fs.writeFile(dummyImagePath, imageBuffer);    \n        console.log(`[Setup] Created dummy image file: ${dummyImagePath}`);  \n    } catch (error) {    \n        console.error('[Setup] Failed to create dummy image file:', error);    \n        return;  \n    }  \n    \n    // --- Display the image ---  \n    console.log('\\n--- Displaying Image ---');  \n    const displayNode = new DisplayImageNode();  \n    displayNode.setParams({    \n        imagePath: dummyImagePath  \n    });  \n\n    try {    \n        await new AsyncFlow(displayNode).runAsync({});    \n        console.log('Image display workflow finished.');  \n    } catch (error) {\n        console.error('Image display workflow Failed:', error.message);\n    } finally {\n        // --- Cleanup ---\n        try {\n            console.log(`\\n[Cleanup] Removing dummy image file: ${dummyImagePath}`);\n            await fs.unlink(dummyImagePath);\n            console.log('[Cleanup] Cleanup complete.');\n        } catch (e) {\n            console.warn('[Cleanup] Failed to remove dummy image file:', e.message);\n        }\n    }\n    console.log('\\n--- DisplayImageNode Example Finished ---');\n})();\n```"
  },
  "duckduckgo_search": {
    "title": "DuckDuckGoSearchNode",
    "description": "/**",
    "parameters": "*   `query`: The search query.",
    "returns": "{Promise<object[]>} A promise that resolves to an array of search result objects.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DuckDuckGoSearchNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  // --- Test 1: DuckDuckGoSearchNode (No API Key Needed) ---\n  console.log('--- Running DuckDuckGo Search Test ---');\n  const ddgNode = new DuckDuckGoSearchNode();\n  ddgNode.setParams({ query: 'What is qflow?' });\n\n  ddgNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('\\n--- DuckDuckGo Search Results ---');\n    console.log(execRes.slice(0, 5)); // Log top 5 results\n    console.log('----------------------------\\n');\n    return 'default';\n  };\n\n  const ddgFlow = new AsyncFlow(ddgNode);\n  try {\n    await ddgFlow.runAsync({});\n    console.log('--- DuckDuckGo Search Test Finished ---');\n  } catch (error) {\n    console.error('--- DuckDuckGo Search Test Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DuckDuckGoSearchNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  // --- Test 1: DuckDuckGoSearchNode (No API Key Needed) ---\n  console.log('--- Running DuckDuckGo Search Test ---');\n  const ddgNode = new DuckDuckGoSearchNode();\n  ddgNode.setParams({ query: 'What is qflow?' });\n\n  ddgNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('\\n--- DuckDuckGo Search Results ---');\n    console.log(execRes.slice(0, 5)); // Log top 5 results\n    console.log('----------------------------\\n');\n    return 'default';\n  };\n\n  const ddgFlow = new AsyncFlow(ddgNode);\n  try {\n    await ddgFlow.runAsync({});\n    console.log('--- DuckDuckGo Search Test Finished ---');\n  } catch (error) {\n    console.error('--- DuckDuckGo Search Test Failed ---', error);\n  }\n})();",
    "raw": "## DuckDuckGoSearchNode\n\nThe `DuckDuckGoSearchNode` performs a web search using DuckDuckGo.\n\n### Parameters\n\n*   `query`: The search query.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { DuckDuckGoSearchNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  // --- Test 1: DuckDuckGoSearchNode (No API Key Needed) ---\n  console.log('--- Running DuckDuckGo Search Test ---');\n  const ddgNode = new DuckDuckGoSearchNode();\n  ddgNode.setParams({ query: 'What is qflow?' });\n\n  ddgNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('\\n--- DuckDuckGo Search Results ---');\n    console.log(execRes.slice(0, 5)); // Log top 5 results\n    console.log('----------------------------\\n');\n    return 'default';\n  };\n\n  const ddgFlow = new AsyncFlow(ddgNode);\n  try {\n    await ddgFlow.runAsync({});\n    console.log('--- DuckDuckGo Search Test Finished ---');\n  } catch (error) {\n    console.error('--- DuckDuckGo Search Test Failed ---', error);\n  }\n})();\n```"
  },
  "embedding": {
    "title": "EmbeddingNode",
    "description": "",
    "parameters": "*   `text`: The text string to generate an embedding for.\n*   `model`: Optional. The Ollama embedding model name to use (e.g., 'nomic-embed-text'). Defaults to 'nomic-embed-text'.\n*   `baseUrl`: Optional. The base URL of the Ollama API (e.g., 'http://localhost:11434'). Defaults to 'http://localhost:11434'.",
    "returns": "Returns execution result. Writes shared.embeddingResult.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { EmbeddingNode } from '@fractal-solutions/qflow/nodes';\n\n// Ollama typically runs locally, so no API key is strictly needed unless configured otherwise.\n// const OLLAMA_BASE_URL = process.env.OLLAMA_BASE_URL || 'http://localhost:11434';\n\n(async () => {\n  console.log('--- Running EmbeddingNode Example (Ollama) ---');\n\n  const embeddingNode = new EmbeddingNode();\n  embeddingNode.setParams({\n    text: \"The quick brown fox jumps over the lazy dog.\",\n    model: \"nomic-embed-text\", // Default Ollama embedding model\n    // baseUrl: OLLAMA_BASE_URL // Uncomment if Ollama is not on default URL\n  });\n\n  const embeddingFlow = new AsyncFlow(embeddingNode);\n\n  try {\n    const result = await embeddingFlow.runAsync({});\n    console.log('--- Embedding Result ---');\n    console.log('Embedding vector (first 5 elements):', result.embedding.slice(0, 5));\n    console.log('Vector length:', result.embedding.length);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { EmbeddingNode } from '@fractal-solutions/qflow/nodes';\n\n// Ollama typically runs locally, so no API key is strictly needed unless configured otherwise.\n// const OLLAMA_BASE_URL = process.env.OLLAMA_BASE_URL || 'http://localhost:11434';\n\n(async () => {\n  console.log('--- Running EmbeddingNode Example (Ollama) ---');\n\n  const embeddingNode = new EmbeddingNode();\n  embeddingNode.setParams({\n    text: \"The quick brown fox jumps over the lazy dog.\",\n    model: \"nomic-embed-text\", // Default Ollama embedding model\n    // baseUrl: OLLAMA_BASE_URL // Uncomment if Ollama is not on default URL\n  });\n\n  const embeddingFlow = new AsyncFlow(embeddingNode);\n\n  try {\n    const result = await embeddingFlow.runAsync({});\n    console.log('--- Embedding Result ---');\n    console.log('Embedding vector (first 5 elements):', result.embedding.slice(0, 5));\n    console.log('Vector length:', result.embedding.length);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();",
    "raw": "## EmbeddingNode\n\nThe `EmbeddingNode` generates vector embeddings for text (requires Ollama).\n\n### Parameters\n\n*   `text`: The text string to generate an embedding for.\n*   `model`: Optional. The Ollama embedding model name to use (e.g., 'nomic-embed-text'). Defaults to 'nomic-embed-text'.\n*   `baseUrl`: Optional. The base URL of the Ollama API (e.g., 'http://localhost:11434'). Defaults to 'http://localhost:11434'.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { EmbeddingNode } from '@fractal-solutions/qflow/nodes';\n\n// Ollama typically runs locally, so no API key is strictly needed unless configured otherwise.\n// const OLLAMA_BASE_URL = process.env.OLLAMA_BASE_URL || 'http://localhost:11434';\n\n(async () => {\n  console.log('--- Running EmbeddingNode Example (Ollama) ---');\n\n  const embeddingNode = new EmbeddingNode();\n  embeddingNode.setParams({\n    text: \"The quick brown fox jumps over the lazy dog.\",\n    model: \"nomic-embed-text\", // Default Ollama embedding model\n    // baseUrl: OLLAMA_BASE_URL // Uncomment if Ollama is not on default URL\n  });\n\n  const embeddingFlow = new AsyncFlow(embeddingNode);\n\n  try {\n    const result = await embeddingFlow.runAsync({});\n    console.log('--- Embedding Result ---');\n    console.log('Embedding vector (first 5 elements):', result.embedding.slice(0, 5));\n    console.log('Vector length:', result.embedding.length);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "gemini_llm": {
    "title": "GeminiLLMNode",
    "description": "",
    "parameters": "*   `apiKey`: Your Google Gemini API key.\n*   `prompt`: The prompt to send to the model.\n*   `model`: The model to use.\n*   `temperature`: The temperature to use for the generation.\n*   `max_tokens`: The maximum number of tokens to generate.",
    "returns": "Returns the generated response text.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { GeminiLLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst GEMINI_API_KEY = process.env.GEMINI_API_KEY;\n\n(async () => {\n  if (!GEMINI_API_KEY) {\n    console.warn(\"WARNING: GEMINI_API_KEY is not set. Please set it to run the GeminiLLMNode example.\");\n    return;\n  }\n\n  console.log('--- Running GeminiLLMNode Example ---');\n\n  const geminiNode = new GeminiLLMNode();\n  geminiNode.setParams({\n    apiKey: GEMINI_API_KEY,\n    prompt: \"Tell me a short poem about a cloud.\",\n    model: \"gemini-pro\" // Common Gemini model\n  });\n\n  const geminiFlow = new AsyncFlow(geminiNode);\n\n  try {\n    const result = await geminiFlow.runAsync({});\n    console.log('--- Gemini Response ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { GeminiLLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst GEMINI_API_KEY = process.env.GEMINI_API_KEY;\n\n(async () => {\n  if (!GEMINI_API_KEY) {\n    console.warn(\"WARNING: GEMINI_API_KEY is not set. Please set it to run the GeminiLLMNode example.\");\n    return;\n  }\n\n  console.log('--- Running GeminiLLMNode Example ---');\n\n  const geminiNode = new GeminiLLMNode();\n  geminiNode.setParams({\n    apiKey: GEMINI_API_KEY,\n    prompt: \"Tell me a short poem about a cloud.\",\n    model: \"gemini-pro\" // Common Gemini model\n  });\n\n  const geminiFlow = new AsyncFlow(geminiNode);\n\n  try {\n    const result = await geminiFlow.runAsync({});\n    console.log('--- Gemini Response ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();",
    "raw": "## GeminiLLMNode\n\nThe `GeminiLLMNode` is used to interact with the Google Gemini API.\n\n### Parameters\n\n*   `apiKey`: Your Google Gemini API key.\n*   `prompt`: The prompt to send to the model.\n*   `model`: The model to use.\n*   `temperature`: The temperature to use for the generation.\n*   `max_tokens`: The maximum number of tokens to generate.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { GeminiLLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst GEMINI_API_KEY = process.env.GEMINI_API_KEY;\n\n(async () => {\n  if (!GEMINI_API_KEY) {\n    console.warn(\"WARNING: GEMINI_API_KEY is not set. Please set it to run the GeminiLLMNode example.\");\n    return;\n  }\n\n  console.log('--- Running GeminiLLMNode Example ---');\n\n  const geminiNode = new GeminiLLMNode();\n  geminiNode.setParams({\n    apiKey: GEMINI_API_KEY,\n    prompt: \"Tell me a short poem about a cloud.\",\n    model: \"gemini-pro\" // Common Gemini model\n  });\n\n  const geminiFlow = new AsyncFlow(geminiNode);\n\n  try {\n    const result = await geminiFlow.runAsync({});\n    console.log('--- Gemini Response ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "gis": {
    "title": "GISNode",
    "description": "",
    "parameters": "*   `action`: The GIS action to perform.\n*   `provider`: The GIS provider to use (OpenStreetMap, Google Maps).\n*   `apiKey`: Your Google Maps API key (if using Google Maps provider).\n*   `address`: The address to geocode.\n*   `latitude`: The latitude for reverse geocoding.\n*   `longitude`: The longitude for reverse geocoding.",
    "returns": "Returns geocoding or reverse geocoding results depending on action.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { GISNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running GISNode Example ---');\n\n  // --- OpenStreetMap Provider Examples ---\n\n  // Example 1: Geocode an address using OpenStreetMap\n  console.log('\\n--- OpenStreetMap: Geocoding \"Eiffel Tower, Paris\" ---');\n  const osmGeocodeNode = new GISNode();\n  osmGeocodeNode.setParams({\n    operation: 'geocode',\n    provider: 'openstreetmap',\n    params: { address: 'Eiffel Tower, Paris' }\n  });\n\n  try {\n    const result = await new AsyncFlow(osmGeocodeNode).runAsync({});\n    console.log('OpenStreetMap Geocode Result:', result);\n  } catch (error) {\n    console.error('OpenStreetMap Geocode Failed:', error.message);\n  }\n\n  console.log('\\n--- GISNode Example Finished ---');\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { GISNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running GISNode Example ---');\n\n  // --- OpenStreetMap Provider Examples ---\n\n  // Example 1: Geocode an address using OpenStreetMap\n  console.log('\\n--- OpenStreetMap: Geocoding \"Eiffel Tower, Paris\" ---');\n  const osmGeocodeNode = new GISNode();\n  osmGeocodeNode.setParams({\n    operation: 'geocode',\n    provider: 'openstreetmap',\n    params: { address: 'Eiffel Tower, Paris' }\n  });\n\n  try {\n    const result = await new AsyncFlow(osmGeocodeNode).runAsync({});\n    console.log('OpenStreetMap Geocode Result:', result);\n  } catch (error) {\n    console.error('OpenStreetMap Geocode Failed:', error.message);\n  }\n\n  console.log('\\n--- GISNode Example Finished ---');\n})();",
    "raw": "## GISNode\n\nThe `GISNode` performs Geographic Information System operations like geocoding and reverse geocoding.\n\n### Parameters\n\n*   `action`: The GIS action to perform.\n*   `provider`: The GIS provider to use (OpenStreetMap, Google Maps).\n*   `apiKey`: Your Google Maps API key (if using Google Maps provider).\n*   `address`: The address to geocode.\n*   `latitude`: The latitude for reverse geocoding.\n*   `longitude`: The longitude for reverse geocoding.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { GISNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running GISNode Example ---');\n\n  // --- OpenStreetMap Provider Examples ---\n\n  // Example 1: Geocode an address using OpenStreetMap\n  console.log('\\n--- OpenStreetMap: Geocoding \"Eiffel Tower, Paris\" ---');\n  const osmGeocodeNode = new GISNode();\n  osmGeocodeNode.setParams({\n    operation: 'geocode',\n    provider: 'openstreetmap',\n    params: { address: 'Eiffel Tower, Paris' }\n  });\n\n  try {\n    const result = await new AsyncFlow(osmGeocodeNode).runAsync({});\n    console.log('OpenStreetMap Geocode Result:', result);\n  } catch (error) {\n    console.error('OpenStreetMap Geocode Failed:', error.message);\n  }\n\n  console.log('\\n--- GISNode Example Finished ---');\n})();\n```"
  },
  "git": {
    "title": "GitNode",
    "description": "",
    "parameters": "*   `action`: The Git action to perform.\n*   `repoPath`: The local path to the repository.\n*   `remoteUrl`: The URL of the remote repository (for 'clone' action).\n*   `files`: An array of file paths to add to the staging area (for 'add' action).\n*   `message`: The commit message (for 'commit' action).\n*   `branch`: The branch to push to or pull from.\n*   `remote`: The name of the remote (e.g., 'origin').",
    "returns": "Returns an action result object (status, commit info, or message).",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { GitNode, WriteFileNode, ShellCommandNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\n\n(async () => {\n  console.log('--- Running Git Test Workflow ---');\n\n  const localPath = './git_test_repo';\n  const newFilePath = path.join(localPath, 'new-file.txt');\n\n  // 1. Create the directory for the repository\n  const makeDir = new ShellCommandNode();\n  makeDir.setParams({\n    command: `mkdir -p ${localPath}`,\n  });\n\n  // 2. Initialize a new Git repository\n  const initRepo = new GitNode();\n  initRepo.setParams({\n    action: 'init',\n    repoPath: localPath,\n  });\n\n  // 3. Create a new file in the repository\n  const writeFile = new WriteFileNode();\n  writeFile.setParams({\n    filePath: newFilePath,\n    content: 'Hello from qflow GitNode!',\n  });\n\n  // 4. Add the new file to staging\n  const addFile = new GitNode();\n  addFile.setParams({\n    action: 'add',\n    repoPath: localPath,\n    files: ['new-file.txt'],\n  });\n\n  // 5. Commit the change\n  const commitChange = new GitNode();\n  commitChange.setParams({\n    action: 'commit',\n    repoPath: localPath,\n    message: 'feat: Add new file via qflow agent',\n  });\n\n  // 6. Get the status of the repository\n  const getStatus = new GitNode();\n  getStatus.setParams({\n    action: 'status',\n    repoPath: localPath,\n  });\n\n  // Chain the nodes\n  makeDir.next(initRepo);\n  initRepo.next(writeFile);\n  writeFile.next(addFile);\n  addFile.next(commitChange);\n  commitChange.next(getStatus);\n\n  // Create and run the flow\n  const gitFlow = new AsyncFlow(makeDir);\n\n  try {\n    const finalResult = await gitFlow.runAsync({});\n    console.log('Git workflow finished successfully.');\n    console.log('Final Status:', finalResult);\n  } catch (error) {\n    console.error('Git workflow failed:', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { GitNode, WriteFileNode, ShellCommandNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\n\n(async () => {\n  console.log('--- Running Git Test Workflow ---');\n\n  const localPath = './git_test_repo';\n  const newFilePath = path.join(localPath, 'new-file.txt');\n\n  // 1. Create the directory for the repository\n  const makeDir = new ShellCommandNode();\n  makeDir.setParams({\n    command: `mkdir -p ${localPath}`,\n  });\n\n  // 2. Initialize a new Git repository\n  const initRepo = new GitNode();\n  initRepo.setParams({\n    action: 'init',\n    repoPath: localPath,\n  });\n\n  // 3. Create a new file in the repository\n  const writeFile = new WriteFileNode();\n  writeFile.setParams({\n    filePath: newFilePath,\n    content: 'Hello from qflow GitNode!',\n  });\n\n  // 4. Add the new file to staging\n  const addFile = new GitNode();\n  addFile.setParams({\n    action: 'add',\n    repoPath: localPath,\n    files: ['new-file.txt'],\n  });\n\n  // 5. Commit the change\n  const commitChange = new GitNode();\n  commitChange.setParams({\n    action: 'commit',\n    repoPath: localPath,\n    message: 'feat: Add new file via qflow agent',\n  });\n\n  // 6. Get the status of the repository\n  const getStatus = new GitNode();\n  getStatus.setParams({\n    action: 'status',\n    repoPath: localPath,\n  });\n\n  // Chain the nodes\n  makeDir.next(initRepo);\n  initRepo.next(writeFile);\n  writeFile.next(addFile);\n  addFile.next(commitChange);\n  commitChange.next(getStatus);\n\n  // Create and run the flow\n  const gitFlow = new AsyncFlow(makeDir);\n\n  try {\n    const finalResult = await gitFlow.runAsync({});\n    console.log('Git workflow finished successfully.');\n    console.log('Final Status:', finalResult);\n  } catch (error) {\n    console.error('Git workflow failed:', error);\n  }\n})();",
    "raw": "## GitNode\n\nThe `GitNode` performs Git operations like clone, add, commit, and push.\n\n### Parameters\n\n*   `action`: The Git action to perform.\n*   `repoPath`: The local path to the repository.\n*   `remoteUrl`: The URL of the remote repository (for 'clone' action).\n*   `files`: An array of file paths to add to the staging area (for 'add' action).\n*   `message`: The commit message (for 'commit' action).\n*   `branch`: The branch to push to or pull from.\n*   `remote`: The name of the remote (e.g., 'origin').\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { GitNode, WriteFileNode, ShellCommandNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\n\n(async () => {\n  console.log('--- Running Git Test Workflow ---');\n\n  const localPath = './git_test_repo';\n  const newFilePath = path.join(localPath, 'new-file.txt');\n\n  // 1. Create the directory for the repository\n  const makeDir = new ShellCommandNode();\n  makeDir.setParams({\n    command: `mkdir -p ${localPath}`,\n  });\n\n  // 2. Initialize a new Git repository\n  const initRepo = new GitNode();\n  initRepo.setParams({\n    action: 'init',\n    repoPath: localPath,\n  });\n\n  // 3. Create a new file in the repository\n  const writeFile = new WriteFileNode();\n  writeFile.setParams({\n    filePath: newFilePath,\n    content: 'Hello from qflow GitNode!',\n  });\n\n  // 4. Add the new file to staging\n  const addFile = new GitNode();\n  addFile.setParams({\n    action: 'add',\n    repoPath: localPath,\n    files: ['new-file.txt'],\n  });\n\n  // 5. Commit the change\n  const commitChange = new GitNode();\n  commitChange.setParams({\n    action: 'commit',\n    repoPath: localPath,\n    message: 'feat: Add new file via qflow agent',\n  });\n\n  // 6. Get the status of the repository\n  const getStatus = new GitNode();\n  getStatus.setParams({\n    action: 'status',\n    repoPath: localPath,\n  });\n\n  // Chain the nodes\n  makeDir.next(initRepo);\n  initRepo.next(writeFile);\n  writeFile.next(addFile);\n  addFile.next(commitChange);\n  commitChange.next(getStatus);\n\n  // Create and run the flow\n  const gitFlow = new AsyncFlow(makeDir);\n\n  try {\n    const finalResult = await gitFlow.runAsync({});\n    console.log('Git workflow finished successfully.');\n    console.log('Final Status:', finalResult);\n  } catch (error) {\n    console.error('Git workflow failed:', error);\n  }\n})();\n```"
  },
  "github": {
    "title": "GitHubNode",
    "description": "",
    "parameters": "*   `action`: The GitHub action to perform.\n*   `owner`: The owner of the repository.\n*   `repo`: The name of the repository.\n*   `token`: Your GitHub personal access token.\n*   `issueNumber`: The issue number.\n*   `title`: The title of the issue.\n*   `body`: The body of the issue.\n*   `assignees`: An array of assignees for the issue.\n*   `labels`: An array of labels for the issue.",
    "returns": "Returns the created/updated issue data or API response.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { CreateIssueNode, GetIssueNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Configuration ---\n// IMPORTANT: Replace with your actual GitHub token, owner, and repo\nconst GITHUB_TOKEN = process.env.GITHUB_TOKEN || 'YOUR_GITHUB_TOKEN_HERE';\nconst GITHUB_OWNER = 'YOUR_GITHUB_OWNER_HERE';\nconst GITHUB_REPO = 'YOUR_GITHUB_REPO_HERE';\n\n// --- Test Workflow for GitHub Nodes ---\n\n(async () => {\n  if (GITHUB_TOKEN === 'YOUR_GITHUB_TOKEN_HERE' || GITHUB_OWNER === 'YOUR_GITHUB_OWNER_HERE' || GITHUB_REPO === 'YOUR_GITHUB_REPO_HERE') {\n    console.warn(\"WARNING: GitHub token, owner, or repo is not set. Please set them to run the flow.\");\n    return;\n  }\n\n  console.log('--- Running GitHub Test Workflow ---');\n\n  // 1. Create instances of the nodes\n  const createIssue = new CreateIssueNode();\n  createIssue.setParams({\n    token: GITHUB_TOKEN,\n    owner: GITHUB_OWNER,\n    repo: GITHUB_REPO,\n    title: '[qflow] Test Issue',\n    body: 'This is a test issue created by the qflow GitHub integration test.'\n  });\n\n  const getIssue = new GetIssueNode();\n  getIssue.setParams({\n    token: GITHUB_TOKEN,\n    owner: GITHUB_OWNER,\n    repo: GITHUB_REPO\n  });\n\n  // 2. Define the workflow\n  createIssue.next(getIssue);\n\n  // 3. Create and run the flow\n  const githubFlow = new AsyncFlow(createIssue);\n\n  try {\n    const result = await githubFlow.runAsync({});\n    console.log('--- GitHub Test Workflow Finished ---');\n    console.log('Final Result:', result);\n  } catch (error) {\n    console.error('--- GitHub Test Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { CreateIssueNode, GetIssueNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Configuration ---\n// IMPORTANT: Replace with your actual GitHub token, owner, and repo\nconst GITHUB_TOKEN = process.env.GITHUB_TOKEN || 'YOUR_GITHUB_TOKEN_HERE';\nconst GITHUB_OWNER = 'YOUR_GITHUB_OWNER_HERE';\nconst GITHUB_REPO = 'YOUR_GITHUB_REPO_HERE';\n\n// --- Test Workflow for GitHub Nodes ---\n\n(async () => {\n  if (GITHUB_TOKEN === 'YOUR_GITHUB_TOKEN_HERE' || GITHUB_OWNER === 'YOUR_GITHUB_OWNER_HERE' || GITHUB_REPO === 'YOUR_GITHUB_REPO_HERE') {\n    console.warn(\"WARNING: GitHub token, owner, or repo is not set. Please set them to run the flow.\");\n    return;\n  }\n\n  console.log('--- Running GitHub Test Workflow ---');\n\n  // 1. Create instances of the nodes\n  const createIssue = new CreateIssueNode();\n  createIssue.setParams({\n    token: GITHUB_TOKEN,\n    owner: GITHUB_OWNER,\n    repo: GITHUB_REPO,\n    title: '[qflow] Test Issue',\n    body: 'This is a test issue created by the qflow GitHub integration test.'\n  });\n\n  const getIssue = new GetIssueNode();\n  getIssue.setParams({\n    token: GITHUB_TOKEN,\n    owner: GITHUB_OWNER,\n    repo: GITHUB_REPO\n  });\n\n  // 2. Define the workflow\n  createIssue.next(getIssue);\n\n  // 3. Create and run the flow\n  const githubFlow = new AsyncFlow(createIssue);\n\n  try {\n    const result = await githubFlow.runAsync({});\n    console.log('--- GitHub Test Workflow Finished ---');\n    console.log('Final Result:', result);\n  } catch (error) {\n    console.error('--- GitHub Test Workflow Failed ---', error);\n  }\n})();",
    "raw": "## GitHubNode\n\nThe `GitHubNode` performs GitHub operations like creating and managing issues.\n\n### Parameters\n\n*   `action`: The GitHub action to perform.\n*   `owner`: The owner of the repository.\n*   `repo`: The name of the repository.\n*   `token`: Your GitHub personal access token.\n*   `issueNumber`: The issue number.\n*   `title`: The title of the issue.\n*   `body`: The body of the issue.\n*   `assignees`: An array of assignees for the issue.\n*   `labels`: An array of labels for the issue.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { CreateIssueNode, GetIssueNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Configuration ---\n// IMPORTANT: Replace with your actual GitHub token, owner, and repo\nconst GITHUB_TOKEN = process.env.GITHUB_TOKEN || 'YOUR_GITHUB_TOKEN_HERE';\nconst GITHUB_OWNER = 'YOUR_GITHUB_OWNER_HERE';\nconst GITHUB_REPO = 'YOUR_GITHUB_REPO_HERE';\n\n// --- Test Workflow for GitHub Nodes ---\n\n(async () => {\n  if (GITHUB_TOKEN === 'YOUR_GITHUB_TOKEN_HERE' || GITHUB_OWNER === 'YOUR_GITHUB_OWNER_HERE' || GITHUB_REPO === 'YOUR_GITHUB_REPO_HERE') {\n    console.warn(\"WARNING: GitHub token, owner, or repo is not set. Please set them to run the flow.\");\n    return;\n  }\n\n  console.log('--- Running GitHub Test Workflow ---');\n\n  // 1. Create instances of the nodes\n  const createIssue = new CreateIssueNode();\n  createIssue.setParams({\n    token: GITHUB_TOKEN,\n    owner: GITHUB_OWNER,\n    repo: GITHUB_REPO,\n    title: '[qflow] Test Issue',\n    body: 'This is a test issue created by the qflow GitHub integration test.'\n  });\n\n  const getIssue = new GetIssueNode();\n  getIssue.setParams({\n    token: GITHUB_TOKEN,\n    owner: GITHUB_OWNER,\n    repo: GITHUB_REPO\n  });\n\n  // 2. Define the workflow\n  createIssue.next(getIssue);\n\n  // 3. Create and run the flow\n  const githubFlow = new AsyncFlow(createIssue);\n\n  try {\n    const result = await githubFlow.runAsync({});\n    console.log('--- GitHub Test Workflow Finished ---');\n    console.log('Final Result:', result);\n  } catch (error) {\n    console.error('--- GitHub Test Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "google_search": {
    "title": "GoogleSearchNode",
    "description": "/**",
    "parameters": "*   `query`: The search query.\n*   `apiKey`: Your Google API Key.\n*   `cseId`: Your Custom Search Engine ID.",
    "returns": "{Promise<object[]>} A promise that resolves to an array of search result objects.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { GoogleSearchNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Configuration for Google Search ---\n// IMPORTANT: Replace with your actual Google API Key and Custom Search Engine ID\nconst GOOGLE_API_KEY = process.env.GOOGLE_API_KEY || 'YOUR_GOOGLE_API_KEY_HERE';\nconst GOOGLE_CSE_ID = process.env.GOOGLE_CSE_ID || 'YOUR_GOOGLE_CSE_ID_HERE';\n\n(async () => {\n  console.log('\\n--- Running Google Search Test ---');\n\n  if (GOOGLE_API_KEY === 'YOUR_GOOGLE_API_KEY_HERE' || GOOGLE_CSE_ID === 'YOUR_GOOGLE_CSE_ID_HERE') {\n    console.warn(\"WARNING: Google API Key or CSE ID is not set. Please set them to run the flow.\");\n    return;\n  }\n\n  const googleNode = new GoogleSearchNode();\n  googleNode.setParams({\n    query: 'What is an autonomous agent?',\n    apiKey: GOOGLE_API_KEY,\n    cseId: GOOGLE_CSE_ID,\n  });\n\n  googleNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('\\n--- Google Search Results ---');\n    console.log(execRes.slice(0, 5)); // Log top 5 results\n    console.log('---------------------------\\n');\n    return 'default';\n  };\n\n  const googleFlow = new AsyncFlow(googleNode);\n  try {\n    await googleFlow.runAsync({});\n    console.log('--- Google Search Test Finished ---');\n  } catch (error) {\n    console.error('--- Google Search Test Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { GoogleSearchNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Configuration for Google Search ---\n// IMPORTANT: Replace with your actual Google API Key and Custom Search Engine ID\nconst GOOGLE_API_KEY = process.env.GOOGLE_API_KEY || 'YOUR_GOOGLE_API_KEY_HERE';\nconst GOOGLE_CSE_ID = process.env.GOOGLE_CSE_ID || 'YOUR_GOOGLE_CSE_ID_HERE';\n\n(async () => {\n  console.log('\\n--- Running Google Search Test ---');\n\n  if (GOOGLE_API_KEY === 'YOUR_GOOGLE_API_KEY_HERE' || GOOGLE_CSE_ID === 'YOUR_GOOGLE_CSE_ID_HERE') {\n    console.warn(\"WARNING: Google API Key or CSE ID is not set. Please set them to run the flow.\");\n    return;\n  }\n\n  const googleNode = new GoogleSearchNode();\n  googleNode.setParams({\n    query: 'What is an autonomous agent?',\n    apiKey: GOOGLE_API_KEY,\n    cseId: GOOGLE_CSE_ID,\n  });\n\n  googleNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('\\n--- Google Search Results ---');\n    console.log(execRes.slice(0, 5)); // Log top 5 results\n    console.log('---------------------------\\n');\n    return 'default';\n  };\n\n  const googleFlow = new AsyncFlow(googleNode);\n  try {\n    await googleFlow.runAsync({});\n    console.log('--- Google Search Test Finished ---');\n  } catch (error) {\n    console.error('--- Google Search Test Failed ---', error);\n  }\n})();",
    "raw": "## GoogleSearchNode\n\nThe `GoogleSearchNode` performs a web search using the Google Custom Search API.\n\n### Parameters\n\n*   `query`: The search query.\n*   `apiKey`: Your Google API Key.\n*   `cseId`: Your Custom Search Engine ID.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { GoogleSearchNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Configuration for Google Search ---\n// IMPORTANT: Replace with your actual Google API Key and Custom Search Engine ID\nconst GOOGLE_API_KEY = process.env.GOOGLE_API_KEY || 'YOUR_GOOGLE_API_KEY_HERE';\nconst GOOGLE_CSE_ID = process.env.GOOGLE_CSE_ID || 'YOUR_GOOGLE_CSE_ID_HERE';\n\n(async () => {\n  console.log('\\n--- Running Google Search Test ---');\n\n  if (GOOGLE_API_KEY === 'YOUR_GOOGLE_API_KEY_HERE' || GOOGLE_CSE_ID === 'YOUR_GOOGLE_CSE_ID_HERE') {\n    console.warn(\"WARNING: Google API Key or CSE ID is not set. Please set them to run the flow.\");\n    return;\n  }\n\n  const googleNode = new GoogleSearchNode();\n  googleNode.setParams({\n    query: 'What is an autonomous agent?',\n    apiKey: GOOGLE_API_KEY,\n    cseId: GOOGLE_CSE_ID,\n  });\n\n  googleNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('\\n--- Google Search Results ---');\n    console.log(execRes.slice(0, 5)); // Log top 5 results\n    console.log('---------------------------\\n');\n    return 'default';\n  };\n\n  const googleFlow = new AsyncFlow(googleNode);\n  try {\n    await googleFlow.runAsync({});\n    console.log('--- Google Search Test Finished ---');\n  } catch (error) {\n    console.error('--- Google Search Test Failed ---', error);\n  }\n})();\n```"
  },
  "hackernews": {
    "title": "HackerNewsNode",
    "description": "",
    "parameters": "*   `action`: The Hacker News action to perform: 'top_stories' or 'item_details'.\n*   `itemId`: The ID of the item to fetch details for.",
    "returns": "Returns top story IDs or item details depending on action.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow, AsyncBatchNode } from '@fractal-solutions/qflow';\nimport { GetTopStoriesNode, GetItemNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Test Workflow for Hacker News Nodes ---\n\nclass ProcessStoriesNode extends AsyncBatchNode {\n  async prepAsync(shared) {\n    return shared.storyIds;\n  }\n\n  async execAsync(item) {\n    const getItem = new GetItemNode();\n    getItem.setParams({ id: item });\n    return await getItem.runAsync({});\n  }\n}\n\n(async () => {\n  console.log('--- Running Hacker News Test Workflow ---');\n\n  // 1. Create instances of the nodes\n  const getTopStories = new GetTopStoriesNode();\n  getTopStories.setParams({ limit: 5 }); // Fetch the top 5 stories\n\n  const processStories = new ProcessStoriesNode();\n\n  // 2. Define the workflow\n  getTopStories.next(processStories);\n\n  // 3. Create and run the flow\n  const hnFlow = new AsyncFlow(getTopStories);\n\n  try {\n    const result = await hnFlow.runAsync({});\n    console.log('\\n--- Hacker News Test Workflow Finished ---');\n    console.log('Final Result:', result);\n  } catch (error) {\n    console.error('\\n--- Hacker News Test Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow, AsyncBatchNode } from '@fractal-solutions/qflow';\nimport { GetTopStoriesNode, GetItemNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Test Workflow for Hacker News Nodes ---\n\nclass ProcessStoriesNode extends AsyncBatchNode {\n  async prepAsync(shared) {\n    return shared.storyIds;\n  }\n\n  async execAsync(item) {\n    const getItem = new GetItemNode();\n    getItem.setParams({ id: item });\n    return await getItem.runAsync({});\n  }\n}\n\n(async () => {\n  console.log('--- Running Hacker News Test Workflow ---');\n\n  // 1. Create instances of the nodes\n  const getTopStories = new GetTopStoriesNode();\n  getTopStories.setParams({ limit: 5 }); // Fetch the top 5 stories\n\n  const processStories = new ProcessStoriesNode();\n\n  // 2. Define the workflow\n  getTopStories.next(processStories);\n\n  // 3. Create and run the flow\n  const hnFlow = new AsyncFlow(getTopStories);\n\n  try {\n    const result = await hnFlow.runAsync({});\n    console.log('\\n--- Hacker News Test Workflow Finished ---');\n    console.log('Final Result:', result);\n  } catch (error) {\n    console.error('\\n--- Hacker News Test Workflow Failed ---', error);\n  }\n})();",
    "raw": "## HackerNewsNode\n\nThe `HackerNewsNode` fetches top stories and item details from Hacker News.\n\n### Parameters\n\n*   `action`: The Hacker News action to perform: 'top_stories' or 'item_details'.\n*   `itemId`: The ID of the item to fetch details for.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow, AsyncBatchNode } from '@fractal-solutions/qflow';\nimport { GetTopStoriesNode, GetItemNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Test Workflow for Hacker News Nodes ---\n\nclass ProcessStoriesNode extends AsyncBatchNode {\n  async prepAsync(shared) {\n    return shared.storyIds;\n  }\n\n  async execAsync(item) {\n    const getItem = new GetItemNode();\n    getItem.setParams({ id: item });\n    return await getItem.runAsync({});\n  }\n}\n\n(async () => {\n  console.log('--- Running Hacker News Test Workflow ---');\n\n  // 1. Create instances of the nodes\n  const getTopStories = new GetTopStoriesNode();\n  getTopStories.setParams({ limit: 5 }); // Fetch the top 5 stories\n\n  const processStories = new ProcessStoriesNode();\n\n  // 2. Define the workflow\n  getTopStories.next(processStories);\n\n  // 3. Create and run the flow\n  const hnFlow = new AsyncFlow(getTopStories);\n\n  try {\n    const result = await hnFlow.runAsync({});\n    console.log('\\n--- Hacker News Test Workflow Finished ---');\n    console.log('Final Result:', result);\n  } catch (error) {\n    console.error('\\n--- Hacker News Test Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "hardware_interaction": {
    "title": "HardwareInteractionNode",
    "description": "",
    "parameters": "*   `action`: The action to perform: 'write', 'read_line', 'list_ports'.\n*   `portPath`: Required for 'write'/'read_line'. The path to the serial port (e.g., '/dev/ttyUSB0', 'COM1').\n*   `baudRate`: Optional. The baud rate for serial communication. Defaults to 9600.\n*   `dataToWrite`: Required for 'write'. The data string to send to the serial port.\n*   `timeout`: Optional. Timeout in milliseconds for 'read_line' action. Defaults to 5000.",
    "returns": "Returns serial communication output or confirmation of write.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { HardwareInteractionNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running HardwareInteractionNode Example (Serial Port) ---');\n\n  // --- IMPORTANT: Configuration ---\n  // Replace with your actual serial port path (e.g., '/dev/ttyUSB0' on Linux, 'COM1' on Windows)\n  const MY_SERIAL_PORT_PATH = '/dev/ttyUSB0'; // <<< CHANGE THIS TO YOUR PORT >>>\n  const BAUD_RATE = 9600;\n\n  // --- Example: Write data to serial port ---\n  console.log('\\n--- Writing \"Hello Device!\" to serial port ---');\n  const writeNode = new HardwareInteractionNode();\n  writeNode.setParams({\n    action: 'write',\n    portPath: MY_SERIAL_PORT_PATH,\n    baudRate: BAUD_RATE,\n    dataToWrite: 'Hello Device!\\n' // Send a newline if your device expects it\n  });\n\n  try {\n    const writeResult = await new AsyncFlow(writeNode).runAsync({});\n    console.log('Write operation successful:', writeResult);\n  } catch (error) {\n    console.error('Write operation failed:', error.message);\n  }\n\n  console.log('\\n--- HardwareInteractionNode Example Finished ---');\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { HardwareInteractionNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running HardwareInteractionNode Example (Serial Port) ---');\n\n  // --- IMPORTANT: Configuration ---\n  // Replace with your actual serial port path (e.g., '/dev/ttyUSB0' on Linux, 'COM1' on Windows)\n  const MY_SERIAL_PORT_PATH = '/dev/ttyUSB0'; // <<< CHANGE THIS TO YOUR PORT >>>\n  const BAUD_RATE = 9600;\n\n  // --- Example: Write data to serial port ---\n  console.log('\\n--- Writing \"Hello Device!\" to serial port ---');\n  const writeNode = new HardwareInteractionNode();\n  writeNode.setParams({\n    action: 'write',\n    portPath: MY_SERIAL_PORT_PATH,\n    baudRate: BAUD_RATE,\n    dataToWrite: 'Hello Device!\\n' // Send a newline if your device expects it\n  });\n\n  try {\n    const writeResult = await new AsyncFlow(writeNode).runAsync({});\n    console.log('Write operation successful:', writeResult);\n  } catch (error) {\n    console.error('Write operation failed:', error.message);\n  }\n\n  console.log('\\n--- HardwareInteractionNode Example Finished ---');\n})();",
    "raw": "## HardwareInteractionNode\n\nThe `HardwareInteractionNode` communicates with local hardware via serial port (UART).\n\n### Parameters\n\n*   `action`: The action to perform: 'write', 'read_line', 'list_ports'.\n*   `portPath`: Required for 'write'/'read_line'. The path to the serial port (e.g., '/dev/ttyUSB0', 'COM1').\n*   `baudRate`: Optional. The baud rate for serial communication. Defaults to 9600.\n*   `dataToWrite`: Required for 'write'. The data string to send to the serial port.\n*   `timeout`: Optional. Timeout in milliseconds for 'read_line' action. Defaults to 5000.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { HardwareInteractionNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running HardwareInteractionNode Example (Serial Port) ---');\n\n  // --- IMPORTANT: Configuration ---\n  // Replace with your actual serial port path (e.g., '/dev/ttyUSB0' on Linux, 'COM1' on Windows)\n  const MY_SERIAL_PORT_PATH = '/dev/ttyUSB0'; // <<< CHANGE THIS TO YOUR PORT >>>\n  const BAUD_RATE = 9600;\n\n  // --- Example: Write data to serial port ---\n  console.log('\\n--- Writing \"Hello Device!\" to serial port ---');\n  const writeNode = new HardwareInteractionNode();\n  writeNode.setParams({\n    action: 'write',\n    portPath: MY_SERIAL_PORT_PATH,\n    baudRate: BAUD_RATE,\n    dataToWrite: 'Hello Device!\\n' // Send a newline if your device expects it\n  });\n\n  try {\n    const writeResult = await new AsyncFlow(writeNode).runAsync({});\n    console.log('Write operation successful:', writeResult);\n  } catch (error) {\n    console.error('Write operation failed:', error.message);\n  }\n\n  console.log('\\n--- HardwareInteractionNode Example Finished ---');\n})();\n```"
  },
  "http_request": {
    "title": "HttpRequestNode",
    "description": "/**",
    "parameters": "*   `url`: The full URL of the API endpoint.\n*   `method`: The HTTP method to use (e.g., 'GET', 'POST').\n*   `headers`: Custom headers for the request.\n*   `body`: The request payload.\n*   `auth`: Authentication configuration.",
    "returns": "{Promise<object>} A promise that resolves to a structured object containing the status, headers, and body of the response.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { HttpRequestNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running HTTP Request Test Workflow ---');\n\n  // Example: GET request to a public API\n  const getPosts = new HttpRequestNode();\n  getPosts.setParams({\n    url: 'https://jsonplaceholder.typicode.com/posts/1',\n    method: 'GET',\n  });\n\n  getPosts.postAsync = async (shared, prepRes, execRes) => {\n    console.log('\\n--- GET Request Result ---');\n    console.log('Status:', execRes.status);\n    console.log('Body:', execRes.body);\n    console.log('------------------------\\n');\n    return 'default';\n  };\n\n  // Example: POST request to create a new post\n  const createPost = new HttpRequestNode();\n  createPost.setParams({\n    url: 'https://jsonplaceholder.typicode.com/posts',\n    method: 'POST',\n    body: {\n      title: 'qflow test',\n      body: 'This is a post created by the HttpRequestNode!',\n      userId: 1,\n    },\n  });\n\n  createPost.postAsync = async (shared, prepRes, execRes) => {\n    console.log('--- POST Request Result ---');\n    console.log('Status:', execRes.status);\n    console.log('Body:', execRes.body);\n    console.log('-------------------------\\n');\n    return 'default';\n  };\n\n  // Chain the nodes\n  getPosts.next(createPost);\n\n  const httpFlow = new AsyncFlow(getPosts);\n\n  try {\n    await httpFlow.runAsync({});\n    console.log('--- HTTP Request Test Workflow Finished ---');\n  } catch (error) {\n    console.error('--- HTTP Request Test Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { HttpRequestNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running HTTP Request Test Workflow ---');\n\n  // Example: GET request to a public API\n  const getPosts = new HttpRequestNode();\n  getPosts.setParams({\n    url: 'https://jsonplaceholder.typicode.com/posts/1',\n    method: 'GET',\n  });\n\n  getPosts.postAsync = async (shared, prepRes, execRes) => {\n    console.log('\\n--- GET Request Result ---');\n    console.log('Status:', execRes.status);\n    console.log('Body:', execRes.body);\n    console.log('------------------------\\n');\n    return 'default';\n  };\n\n  // Example: POST request to create a new post\n  const createPost = new HttpRequestNode();\n  createPost.setParams({\n    url: 'https://jsonplaceholder.typicode.com/posts',\n    method: 'POST',\n    body: {\n      title: 'qflow test',\n      body: 'This is a post created by the HttpRequestNode!',\n      userId: 1,\n    },\n  });\n\n  createPost.postAsync = async (shared, prepRes, execRes) => {\n    console.log('--- POST Request Result ---');\n    console.log('Status:', execRes.status);\n    console.log('Body:', execRes.body);\n    console.log('-------------------------\\n');\n    return 'default';\n  };\n\n  // Chain the nodes\n  getPosts.next(createPost);\n\n  const httpFlow = new AsyncFlow(getPosts);\n\n  try {\n    await httpFlow.runAsync({});\n    console.log('--- HTTP Request Test Workflow Finished ---');\n  } catch (error) {\n    console.error('--- HTTP Request Test Workflow Failed ---', error);\n  }\n})();",
    "raw": "## HttpRequestNode\n\nThe `HttpRequestNode` makes a generic HTTP request to any URL.\n\n### Parameters\n\n*   `url`: The full URL of the API endpoint.\n*   `method`: The HTTP method to use (e.g., 'GET', 'POST').\n*   `headers`: Custom headers for the request.\n*   `body`: The request payload.\n*   `auth`: Authentication configuration.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { HttpRequestNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running HTTP Request Test Workflow ---');\n\n  // Example: GET request to a public API\n  const getPosts = new HttpRequestNode();\n  getPosts.setParams({\n    url: 'https://jsonplaceholder.typicode.com/posts/1',\n    method: 'GET',\n  });\n\n  getPosts.postAsync = async (shared, prepRes, execRes) => {\n    console.log('\\n--- GET Request Result ---');\n    console.log('Status:', execRes.status);\n    console.log('Body:', execRes.body);\n    console.log('------------------------\\n');\n    return 'default';\n  };\n\n  // Example: POST request to create a new post\n  const createPost = new HttpRequestNode();\n  createPost.setParams({\n    url: 'https://jsonplaceholder.typicode.com/posts',\n    method: 'POST',\n    body: {\n      title: 'qflow test',\n      body: 'This is a post created by the HttpRequestNode!',\n      userId: 1,\n    },\n  });\n\n  createPost.postAsync = async (shared, prepRes, execRes) => {\n    console.log('--- POST Request Result ---');\n    console.log('Status:', execRes.status);\n    console.log('Body:', execRes.body);\n    console.log('-------------------------\\n');\n    return 'default';\n  };\n\n  // Chain the nodes\n  getPosts.next(createPost);\n\n  const httpFlow = new AsyncFlow(getPosts);\n\n  try {\n    await httpFlow.runAsync({});\n    console.log('--- HTTP Request Test Workflow Finished ---');\n  } catch (error) {\n    console.error('--- HTTP Request Test Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "http_server": {
    "title": "HttpServerNode",
    "description": "Use the `HttpServerNode` to listen for incoming HTTP requests and delegate them to specific `AsyncFlow` instances based on the request's path and method. This turns your qflow workflows into web-accessible endpoints.",
    "parameters": "| Parameter   | Type     | Description                                                                                                                                                           | Default     |\n|-------------|----------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------|\n| `port`      | `number` | The port number for the server to listen on.                                                                                                                          | `3000`      |\n| `routes`    | `object` | An object defining the dynamic routes. The keys are URL paths (e.g., `/`, `/users/:id`), and the values are objects mapping HTTP methods (e.g., `GET`, `POST`) to `AsyncFlow` instances. | `{}`        |\n| `staticDir` | `string` | The absolute or relative path to a directory from which to serve static files (e.g., HTML, CSS, images).                                                               | `undefined` |\n| `cors`      | `object` | A configuration object for enabling and customizing Cross-Origin Resource Sharing (CORS) headers.                                                                     | `undefined` |",
    "returns": "Returns server status and continues listening until stopped.",
    "notes": "",
    "requirements": "",
    "examplesText": "",
    "example": "const serverRoutes = {\n    // A simple GET route\n    '/': {\n        'GET': welcomeFlow\n    },\n    // A route with a path parameter\n    '/users/:id': {\n        'GET': userFlow\n    },\n    // A route that handles POST requests\n    '/items': {\n        'POST': createItemFlow\n    }\n};",
    "raw": "# HttpServerNode\n\nThe `HttpServerNode` is a powerful node that allows you to create a fully-featured HTTP server within a qflow workflow. It can handle dynamic routes by triggering other flows, serve static files, and manage CORS headers, making it ideal for building simple APIs or web backends.\n\n## Purpose\n\nUse the `HttpServerNode` to listen for incoming HTTP requests and delegate them to specific `AsyncFlow` instances based on the request's path and method. This turns your qflow workflows into web-accessible endpoints.\n\n## Parameters\n\n| Parameter   | Type     | Description                                                                                                                                                           | Default     |\n|-------------|----------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------|-------------|\n| `port`      | `number` | The port number for the server to listen on.                                                                                                                          | `3000`      |\n| `routes`    | `object` | An object defining the dynamic routes. The keys are URL paths (e.g., `/`, `/users/:id`), and the values are objects mapping HTTP methods (e.g., `GET`, `POST`) to `AsyncFlow` instances. | `{}`        |\n| `staticDir` | `string` | The absolute or relative path to a directory from which to serve static files (e.g., HTML, CSS, images).                                                               | `undefined` |\n| `cors`      | `object` | A configuration object for enabling and customizing Cross-Origin Resource Sharing (CORS) headers.                                                                     | `undefined` |\n\n### `routes` Object Structure\n\nThe `routes` object maps URL paths to method-specific flow handlers.\n\n```javascript\nconst serverRoutes = {\n    // A simple GET route\n    '/': {\n        'GET': welcomeFlow\n    },\n    // A route with a path parameter\n    '/users/:id': {\n        'GET': userFlow\n    },\n    // A route that handles POST requests\n    '/items': {\n        'POST': createItemFlow\n    }\n};\n```\n\n### `cors` Object Structure\n\nThe `cors` object allows you to specify standard CORS headers.\n\n```javascript\nconst corsConfig = {\n    origin: '*', // or 'https://your-frontend-domain.com'\n    methods: 'GET,POST,PUT,DELETE,OPTIONS',\n    headers: 'Content-Type,Authorization'\n};\n```\n\n## Triggered Flow Context\n\nWhen a dynamic route is matched, the corresponding `AsyncFlow` is triggered. The flow receives a `request` object within its `shared` state (`shared.request`).\n\nThe `shared.request` object contains:\n- `headers`: An object of request headers.\n- `body`: The parsed JSON payload (if the request body is valid JSON) or the raw string body.\n- `method`: The HTTP request method (e.g., `GET`, `POST`).\n- `path`: The URL path.\n- `query`: An object containing the URL query string parameters.\n- `params`: An object containing parameters extracted from the URL path (e.g., `{ id: '123' }` for a `/users/123` request to a `/users/:id` route).\n\n## Triggered Flow Response\n\nThe triggered flow is expected to return a response object that the `HttpServerNode` will use to reply to the client.\n\nThe response object should have the following structure:\n- `statusCode` (optional, `number`): The HTTP status code. Defaults to `200`.\n- `headers` (optional, `object`): An object of response headers. Defaults to `{ 'Content-Type': 'application/json' }` if the body is an object.\n- `body` (optional, `string | object | Buffer`): The response body. Objects will be automatically stringified as JSON.\n\nIf the flow returns `null` or `undefined`, a `204 No Content` response will be sent.\n\n## Example: Mock API with Static Files\n\nHere is a more robust example demonstrating how to build a simple mock API that also serves static files (e.g., an `index.html`).\n\n### 1. File Structure\n\nFor this example, assume you have the following file structure. The server will serve dynamic API routes and also serve any files placed in the `public` directory.\n\n```\n.\n├── your_script.js\n└── public/\n    └── index.html\n```\n\nCreate a `public` directory and place an `index.html` file inside it with some content, like `<h1>Welcome to the Static Site!</h1>`.\n\n### 2. Server Script (`your_script.js`)\n\nThe script will define the API routes and tell the server to serve static files from the `public` directory.\n\n```javascript\nimport { AsyncFlow, AsyncNode } from '../src/qflow.js';\nimport { HttpServerNode } from '../src/nodes/http_server.js';\nimport path from 'path'; // Import path module\n\n// --- In-Memory \"Database\" ---\nconst users = [\n    { id: 1, name: 'Alice' },\n    { id: 2, name: 'Bob' }\n];\nlet nextUserId = 3;\n\n// --- API Logic Flows ---\n\n// Flow to handle GET /api/users\nclass ListUsersFlow extends AsyncNode {\n    async execAsync() {\n        console.log('[API] Responding with all users.');\n        return { statusCode: 200, body: users };\n    }\n}\nconst listUsersFlow = new AsyncFlow(new ListUsersFlow());\n\n// Flow to handle GET /api/users/:id\nclass GetUserFlow extends AsyncNode {\n    async execAsync(prepRes, shared) {\n        const userId = parseInt(shared.request.params.id, 10);\n        const user = users.find(u => u.id === userId);\n        console.log(`[API] Searching for user with ID: ${userId}`);\n\n        if (user) {\n            return { statusCode: 200, body: user };\n        } else {\n            return { statusCode: 404, body: { error: 'User not found' } };\n        }\n    }\n}\nconst getUserFlow = new AsyncFlow(new GetUserFlow());\n\n// Flow to handle POST /api/users\nclass CreateUserFlow extends AsyncNode {\n    async execAsync(prepRes, shared) {\n        const { name } = shared.request.body;\n        if (!name) {\n            return { statusCode: 400, body: { error: 'Name is required' } };\n        }\n        const newUser = { id: nextUserId++, name };\n        users.push(newUser);\n        console.log('[API] Created new user:', newUser);\n        return { statusCode: 201, body: newUser };\n    }\n}\nconst createUserFlow = new AsyncFlow(new CreateUserFlow());\n\n\n// --- Server Setup ---\n\n// 1. Define the routes for our User API\nconst apiRoutes = {\n    '/api/users': {\n        'GET': listUsersFlow,\n        'POST': createUserFlow\n    },\n    '/api/users/:id': {\n        'GET': getUserFlow\n    }\n};\n\n// 2. Create a flow to start the server\n(async () => {\n    const serverNode = new HttpServerNode();\n    serverNode.setParams({\n        port: 3001,\n        routes: apiRoutes,\n        staticDir: path.join(process.cwd(), 'public'), // Serve files from the 'public' directory\n        cors: { origin: '*' } // Allow all origins for the example\n    });\n\n    const serverFlow = new AsyncFlow(serverNode);\n\n    try {\n        // This will start the server and keep the process running\n        await serverFlow.runAsync({});\n        console.log('API server and static file server is running on http://localhost:3001. Press Ctrl+C to stop.');\n    } catch (error) {\n        console.error('Failed to start server:', error);\n    }\n})();\n```\n\n### Testing the API and Static Files\n\nYou can test this server with `curl` in another terminal:\n\n**Get the static `index.html` file:**\n```sh\ncurl http://localhost:3001/index.html\n```\n\n**Get all users (dynamic API route):**\n```sh\ncurl http://localhost:3001/api/users\n```\n\n**Get user with ID 1 (dynamic API route):**\n```sh\ncurl http://localhost:3001/api/users/1\n```\n\n**Create a new user named \"Charlie\" (dynamic API route):**\n```sh\ncurl -X POST -H \"Content-Type: application/json\" -d '{\"name\": \"Charlie\"}' http://localhost:3001/api/users\n```"
  },
  "huggingface_llm": {
    "title": "HuggingFaceLLMNode",
    "description": "",
    "parameters": "*   `hfToken`: Your Hugging Face API token.\n*   `prompt`: The prompt to send to the model.\n*   `model`: The Hugging Face model ID (e.g., 'HuggingFaceH4/zephyr-7b-beta', 'openai/gpt-oss-20b:novita').\n*   `temperature`: Optional. Controls randomness. Defaults to 0.7.\n*   `max_new_tokens`: Optional. Maximum number of tokens to generate. Defaults to 500.\n*   `baseUrl`: Optional. The base URL of the Hugging Face router API. Defaults to 'https://router.huggingface.co/v1'.",
    "returns": "Returns the generated response text.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { HuggingFaceLLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst HF_TOKEN = process.env.HF_TOKEN; // Your Hugging Face API token\nconst HF_MODEL = process.env.HF_MODEL || \"openai/gpt-oss-20b:novita\"; // Example model\n\n(async () => {\n  if (!HF_TOKEN) {\n    console.warn(\"WARNING: HF_TOKEN is not set. Please set it to run the HuggingFaceLLMNode example.\");\n    return;\n  }\n\n  console.log('--- Running HuggingFaceLLMNode Example ---');\n  console.log(`Using model: ${HF_MODEL}`);\n\n  const hfLLMNode = new HuggingFaceLLMNode();\n  hfLLMNode.setParams({\n    model: HF_MODEL,\n    hfToken: HF_TOKEN,\n    prompt: \"Explain the concept of machine learning in a few sentences.\",\n    temperature: 0.7,\n    max_new_tokens: 100,\n    baseUrl: 'https://router.huggingface.co/v1' // Use OpenAI-compatible router\n  });\n\n  const hfFlow = new AsyncFlow(hfLLMNode);\n\n  try {\n    const result = await hfFlow.runAsync({});\n    console.log('--- HuggingFace Response ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { HuggingFaceLLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst HF_TOKEN = process.env.HF_TOKEN; // Your Hugging Face API token\nconst HF_MODEL = process.env.HF_MODEL || \"openai/gpt-oss-20b:novita\"; // Example model\n\n(async () => {\n  if (!HF_TOKEN) {\n    console.warn(\"WARNING: HF_TOKEN is not set. Please set it to run the HuggingFaceLLMNode example.\");\n    return;\n  }\n\n  console.log('--- Running HuggingFaceLLMNode Example ---');\n  console.log(`Using model: ${HF_MODEL}`);\n\n  const hfLLMNode = new HuggingFaceLLMNode();\n  hfLLMNode.setParams({\n    model: HF_MODEL,\n    hfToken: HF_TOKEN,\n    prompt: \"Explain the concept of machine learning in a few sentences.\",\n    temperature: 0.7,\n    max_new_tokens: 100,\n    baseUrl: 'https://router.huggingface.co/v1' // Use OpenAI-compatible router\n  });\n\n  const hfFlow = new AsyncFlow(hfLLMNode);\n\n  try {\n    const result = await hfFlow.runAsync({});\n    console.log('--- HuggingFace Response ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();",
    "raw": "## HuggingFaceLLMNode\n\nThe `HuggingFaceLLMNode` is used to interact with the Hugging Face API.\n\n### Parameters\n\n*   `hfToken`: Your Hugging Face API token.\n*   `prompt`: The prompt to send to the model.\n*   `model`: The Hugging Face model ID (e.g., 'HuggingFaceH4/zephyr-7b-beta', 'openai/gpt-oss-20b:novita').\n*   `temperature`: Optional. Controls randomness. Defaults to 0.7.\n*   `max_new_tokens`: Optional. Maximum number of tokens to generate. Defaults to 500.\n*   `baseUrl`: Optional. The base URL of the Hugging Face router API. Defaults to 'https://router.huggingface.co/v1'.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { HuggingFaceLLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst HF_TOKEN = process.env.HF_TOKEN; // Your Hugging Face API token\nconst HF_MODEL = process.env.HF_MODEL || \"openai/gpt-oss-20b:novita\"; // Example model\n\n(async () => {\n  if (!HF_TOKEN) {\n    console.warn(\"WARNING: HF_TOKEN is not set. Please set it to run the HuggingFaceLLMNode example.\");\n    return;\n  }\n\n  console.log('--- Running HuggingFaceLLMNode Example ---');\n  console.log(`Using model: ${HF_MODEL}`);\n\n  const hfLLMNode = new HuggingFaceLLMNode();\n  hfLLMNode.setParams({\n    model: HF_MODEL,\n    hfToken: HF_TOKEN,\n    prompt: \"Explain the concept of machine learning in a few sentences.\",\n    temperature: 0.7,\n    max_new_tokens: 100,\n    baseUrl: 'https://router.huggingface.co/v1' // Use OpenAI-compatible router\n  });\n\n  const hfFlow = new AsyncFlow(hfLLMNode);\n\n  try {\n    const result = await hfFlow.runAsync({});\n    console.log('--- HuggingFace Response ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "image_gallery": {
    "title": "ImageGalleryNode",
    "description": "",
    "parameters": "*   `imagePaths`: An array of absolute paths to the image files to display in the gallery.\n*   `title`: Optional. The title of the HTML gallery page. Defaults to 'Image Gallery'.\n*   `description`: Optional. A short description to display on the gallery page. Defaults to 'Generated by QFlow Agent'.\n*   `outputDir`: Optional. Directory to save the generated HTML file and copied images. Defaults to a temporary directory.",
    "returns": "Returns gallery HTML path and list of images.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ImageGalleryNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {\n  console.log('--- Running ImageGalleryNode Example ---');\n\n  const tempDir = os.tmpdir();\n  const dummyImageDir = path.join(tempDir, `qflow_gallery_test_images_${Date.now()}`);\n  await fs.mkdir(dummyImageDir, { recursive: true });\n\n  const imagePaths = [];\n  const base64Png1x1 = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNkYAAAAAYAAjCB0C8AAAAASUVORK5CYII='; // 1x1 transparent\n  const base64PngRed = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg=='; // 1x1 red\n  const base64PngBlue = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYGD4z8DAwMAAAwAB/w401QAAAABJRU5CYII='; // 1x1 blue\n\n  // Create dummy image files\n  for (let i = 0; i < 3; i++) {\n    const imgPath = path.join(dummyImageDir, `image_${i + 1}.png`);\n    let buffer;\n    if (i === 0) buffer = Buffer.from(base64Png1x1, 'base64');\n    else if (i === 1) buffer = Buffer.from(base64PngRed, 'base64');\n    else buffer = Buffer.from(base64PngBlue, 'base64');\n\n    await fs.writeFile(imgPath, buffer);\n    imagePaths.push(imgPath);\n    console.log(`[Setup] Created dummy image: ${imgPath}`);\n  }\n\n  // --- Display the image gallery ---\n  console.log('\\n--- Displaying Image Gallery ---');\n  const galleryNode = new ImageGalleryNode();\n  galleryNode.setParams({\n    imagePaths: imagePaths,\n    title: 'My QFlow Generated Images',\n    description: 'A collection of images created by the agent.'\n  });\n\n  let galleryHtmlPath = null;\n  try {\n    const result = await new AsyncFlow(galleryNode).runAsync({});\n    console.log('Image gallery workflow finished:', result);\n    galleryHtmlPath = result.htmlFilePath;\n  } catch (error) {\n    console.error('Image gallery workflow Failed:', error.message);\n  } finally {\n    // --- Cleanup ---\n    try {\n      console.log('\\n[Cleanup] Cleaning up dummy images and generated HTML...');\n      if (galleryHtmlPath) {\n        await fs.unlink(galleryHtmlPath).catch(() => {}); // Delete HTML file\n      }\n      await fs.rm(dummyImageDir, { recursive: true, force: true }).catch(() => {}); // Delete image directory\n      console.log('[Cleanup] Cleanup complete.');\n    } catch (e) {\n      console.warn('[Cleanup] Failed to remove some temporary files/directories:', e.message);\n    }\n  }\n\n  console.log('\\n--- ImageGalleryNode Example Finished ---');\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ImageGalleryNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {\n  console.log('--- Running ImageGalleryNode Example ---');\n\n  const tempDir = os.tmpdir();\n  const dummyImageDir = path.join(tempDir, `qflow_gallery_test_images_${Date.now()}`);\n  await fs.mkdir(dummyImageDir, { recursive: true });\n\n  const imagePaths = [];\n  const base64Png1x1 = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNkYAAAAAYAAjCB0C8AAAAASUVORK5CYII='; // 1x1 transparent\n  const base64PngRed = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg=='; // 1x1 red\n  const base64PngBlue = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYGD4z8DAwMAAAwAB/w401QAAAABJRU5CYII='; // 1x1 blue\n\n  // Create dummy image files\n  for (let i = 0; i < 3; i++) {\n    const imgPath = path.join(dummyImageDir, `image_${i + 1}.png`);\n    let buffer;\n    if (i === 0) buffer = Buffer.from(base64Png1x1, 'base64');\n    else if (i === 1) buffer = Buffer.from(base64PngRed, 'base64');\n    else buffer = Buffer.from(base64PngBlue, 'base64');\n\n    await fs.writeFile(imgPath, buffer);\n    imagePaths.push(imgPath);\n    console.log(`[Setup] Created dummy image: ${imgPath}`);\n  }\n\n  // --- Display the image gallery ---\n  console.log('\\n--- Displaying Image Gallery ---');\n  const galleryNode = new ImageGalleryNode();\n  galleryNode.setParams({\n    imagePaths: imagePaths,\n    title: 'My QFlow Generated Images',\n    description: 'A collection of images created by the agent.'\n  });\n\n  let galleryHtmlPath = null;\n  try {\n    const result = await new AsyncFlow(galleryNode).runAsync({});\n    console.log('Image gallery workflow finished:', result);\n    galleryHtmlPath = result.htmlFilePath;\n  } catch (error) {\n    console.error('Image gallery workflow Failed:', error.message);\n  } finally {\n    // --- Cleanup ---\n    try {\n      console.log('\\n[Cleanup] Cleaning up dummy images and generated HTML...');\n      if (galleryHtmlPath) {\n        await fs.unlink(galleryHtmlPath).catch(() => {}); // Delete HTML file\n      }\n      await fs.rm(dummyImageDir, { recursive: true, force: true }).catch(() => {}); // Delete image directory\n      console.log('[Cleanup] Cleanup complete.');\n    } catch (e) {\n      console.warn('[Cleanup] Failed to remove some temporary files/directories:', e.message);\n    }\n  }\n\n  console.log('\\n--- ImageGalleryNode Example Finished ---');\n})();",
    "raw": "## ImageGalleryNode\n\nThe `ImageGalleryNode` generates an HTML gallery from multiple image files and opens it in a web browser.\n\n### Parameters\n\n*   `imagePaths`: An array of absolute paths to the image files to display in the gallery.\n*   `title`: Optional. The title of the HTML gallery page. Defaults to 'Image Gallery'.\n*   `description`: Optional. A short description to display on the gallery page. Defaults to 'Generated by QFlow Agent'.\n*   `outputDir`: Optional. Directory to save the generated HTML file and copied images. Defaults to a temporary directory.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ImageGalleryNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {\n  console.log('--- Running ImageGalleryNode Example ---');\n\n  const tempDir = os.tmpdir();\n  const dummyImageDir = path.join(tempDir, `qflow_gallery_test_images_${Date.now()}`);\n  await fs.mkdir(dummyImageDir, { recursive: true });\n\n  const imagePaths = [];\n  const base64Png1x1 = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAQAAAC1HAwCAAAAC0lEQVR42mNkYAAAAAYAAjCB0C8AAAAASUVORK5CYII='; // 1x1 transparent\n  const base64PngRed = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mP8/5+hHgAHggJ/PchI7wAAAABJRU5ErkJggg=='; // 1x1 red\n  const base64PngBlue = 'iVBORw0KGgoAAAANSUhEUgAAAAEAAAABCAYAAAAfFcSJAAAADUlEQVR42mNkYGD4z8DAwMAAAwAB/w401QAAAABJRU5CYII='; // 1x1 blue\n\n  // Create dummy image files\n  for (let i = 0; i < 3; i++) {\n    const imgPath = path.join(dummyImageDir, `image_${i + 1}.png`);\n    let buffer;\n    if (i === 0) buffer = Buffer.from(base64Png1x1, 'base64');\n    else if (i === 1) buffer = Buffer.from(base64PngRed, 'base64');\n    else buffer = Buffer.from(base64PngBlue, 'base64');\n\n    await fs.writeFile(imgPath, buffer);\n    imagePaths.push(imgPath);\n    console.log(`[Setup] Created dummy image: ${imgPath}`);\n  }\n\n  // --- Display the image gallery ---\n  console.log('\\n--- Displaying Image Gallery ---');\n  const galleryNode = new ImageGalleryNode();\n  galleryNode.setParams({\n    imagePaths: imagePaths,\n    title: 'My QFlow Generated Images',\n    description: 'A collection of images created by the agent.'\n  });\n\n  let galleryHtmlPath = null;\n  try {\n    const result = await new AsyncFlow(galleryNode).runAsync({});\n    console.log('Image gallery workflow finished:', result);\n    galleryHtmlPath = result.htmlFilePath;\n  } catch (error) {\n    console.error('Image gallery workflow Failed:', error.message);\n  } finally {\n    // --- Cleanup ---\n    try {\n      console.log('\\n[Cleanup] Cleaning up dummy images and generated HTML...');\n      if (galleryHtmlPath) {\n        await fs.unlink(galleryHtmlPath).catch(() => {}); // Delete HTML file\n      }\n      await fs.rm(dummyImageDir, { recursive: true, force: true }).catch(() => {}); // Delete image directory\n      console.log('[Cleanup] Cleanup complete.');\n    } catch (e) {\n      console.warn('[Cleanup] Failed to remove some temporary files/directories:', e.message);\n    }\n  }\n\n  console.log('\\n--- ImageGalleryNode Example Finished ---');\n})();\n```"
  },
  "index": {
    "title": "Node Documentation",
    "description": "",
    "parameters": "",
    "returns": "",
    "notes": "",
    "requirements": "",
    "examplesText": "",
    "example": "",
    "raw": "## Node Documentation\n\nThis section contains detailed documentation for each of the integrated nodes in qflow.\n\n### Core\n\n*   [AgentNode](agent.md)\n\n### LLMs\n\n*   [DeepSeekLLMNode](deepseek_llm.md)\n*   [OpenAILLMNode](openai_llm.md)\n*   [GeminiLLMNode](gemini_llm.md)\n*   [OllamaLLMNode](ollama_llm.md)\n*   [HuggingFaceLLMNode](huggingface_llm.md)\n*   [OpenRouterLLMNode](openrouter_llm.md)\n\n### Data\n\n*   [CodeInterpreterNode](code_interpreter.md)\n*   [DataExtractorNode](data_extractor.md)\n*   [DatabaseNode](database.md)\n*   [EmbeddingNode](embedding.md)\n*   [MemoryNode](memory.md)\n*   [SemanticMemoryNode](semantic_memory.md)\n*   [TransformNode](transform.md)\n*   [PDFProcessorNode](pdf_processor.md)\n*   [SpreadsheetNode](spreadsheet.md)\n*   [DataValidationNode](data_validation.md)\n\n### System\n\n*   [ShellCommandNode](shell_command.md)\n*   [ReadFileNode](read_file.md)\n*   [WriteFileNode](write_file.md)\n*   [AppendFileNode](append_file.md)\n*   [ListDirectoryNode](list_directory.md)\n*   [SystemNotificationNode](system_notification.md)\n*   [DisplayImageNode](display_image.md)\n*   [HardwareInteractionNode](hardware_interaction.md)\n*   [ImageGalleryNode](image_gallery.md)\n*   [SpeechSynthesisNode](speech_synthesis.md)\n*   [MultimediaProcessingNode](multimedia_processing.md)\n*   [RemoteExecutionNode](remote_execution.md)\n\n### Web\n\n*   [HttpRequestNode](http_request.md)\n*   [WebScraperNode](web_scraper.md)\n*   [DuckDuckGoSearchNode](duckduckgo_search.md)\n*   [GoogleSearchNode](google_search.md)\n*   [BrowserControlNode](browser_control.md)\n*   [NavigatorNode](navigator.md)\n*   [WebSocketsNode](websockets.md)\n*   [WebhookNode](webhook.md)\n*   [HttpServerNode](http_server.md)\n\n### Flow Control\n\n*   [SubFlowNode](sub_flow.md)\n*   [IteratorNode](iterator.md)\n*   [SchedulerNode](scheduler.md)\n\n### Other\n\n*   [GitNode](git.md)\n*   [GISNode](gis.md)\n*   [GitHubNode](github.md)\n*   [HackerNewsNode](hackernews.md)\n*   [StripeNode](stripe.md)"
  },
  "interactive_webview": {
    "title": "InteractiveWebviewNode",
    "description": "/**",
    "parameters": "*   `mode` (string, required): The mode of the interactive webview. Can be one of `'notification'`, `'dialog'`, `'input'`, `'custom'`, `'custom-dialog'`, or `'custom-input'`.\n    *   `'notification'`: Displays a message with a close button.\n    *   `'dialog'`: Displays a message with predefined options as buttons.\n    *   `'input'`: Displays a prompt with a text input field.\n    *   `'custom'`: Displays fully custom HTML content. This mode offers complete control over the webview's HTML, CSS, and JavaScript, allowing for entirely custom layouts and styling. However, you must implement your own JavaScript to call the `qflow()` function to return data to the flow.\n    *   `'custom-dialog'`: Displays custom HTML content along with predefined options as buttons.\n    *   `'custom-input'`: Displays custom HTML content along with a text input field and a submit button.\n*   `message` (string, optional): The message to display in the webview. Required for `'notification'` and `'dialog'` modes. Can be used as fallback content for `'custom-dialog'` if `html` is not provided.\n*   `options` (array, optional): An array of strings to be displayed as buttons in `'dialog'` and `'custom-dialog'` modes.\n*   `prompt` (string, optional): The prompt to display in `'input'` mode. Can be used as fallback content for `'custom-input'` if `html` is not provided.\n*   `html` (string, optional): The full HTML content to display in the webview. Required for `'custom'` mode. Used as the primary content for `'custom-dialog'` and `'custom-input'` modes, overriding `message` or `prompt`. For `'custom-dialog'` and `'custom-input'`, your provided HTML will be embedded within the node's default themed container, meaning the overall theme (light/dark) and basic layout are still applied.\n*   `title` (string, optional): The title of the webview window. Defaults to `'Qflow'`.\n*   `width` (number, optional): The width of the webview window. Defaults to `400`.\n*   `height` (number, optional): The height of the webview window. Defaults to `220`.\n*   `theme` (string, optional): The theme of the webview. Can be `'light'` or `'dark'`. Defaults to `'light'`.\n*   `multilineInput` (boolean, optional): If `true` and `mode` is `'input'` or `'custom-input'`, displays a multi-line textarea instead of a single-line input field. Defaults to `false`.",
    "returns": "The result of the user's interaction is passed to the next node in the flow.\n*   For `'dialog'` and `'custom-dialog'` modes, the result is the text of the button the user clicked.\n*   For `'input'` and `'custom-input'` modes, the result is the text the user entered.\n*   For `'notification'` mode, the result is the string `'closed'`.\n*   For `'custom'` mode, the result is whatever value is passed to the `qflow()` JavaScript function called from within your custom HTML.",
    "notes": "",
    "requirements": "",
    "examplesText": "",
    "example": "import { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { InteractiveWebviewNode } from '@fractal-solutions/qflow/nodes';\n\nasync function runInteractiveDemo() {\n  const showDialog = new InteractiveWebviewNode();\n  showDialog.setParams({\n    mode: 'dialog',\n    message: 'What is your favorite color?',\n    options: ['Red', 'Green', 'Blue'],\n    title: 'Color Picker',\n    theme: 'dark',\n    width: 500,\n    height: 300,\n  });\n\n  const askForName = new InteractiveWebviewNode();\n  askForName.setParams({\n    mode: 'input',\n    prompt: 'Please enter your name:',\n    title: 'Name Input',\n    theme: 'dark',\n    width: 500,\n    height: 300,\n  });\n\n  const showNotification = new InteractiveWebviewNode();\n\n  class ProcessResultsNode extends AsyncNode {\n    async execAsync(prepRes, shared) {\n      showNotification.setParams({\n        mode: 'notification',\n        message: `Hello, ${shared.name}! Your favorite color is ${shared.color}.`,\n        title: 'Summary',\n        theme: 'dark',\n        width: 500,\n        height: 300,\n      });\n      return 'default';\n    }\n  }\n  const processResults = new ProcessResultsNode();\n\n  showDialog.next(askForName);\n  askForName.next(processResults);\n  processResults.next(showNotification);\n\n  const flow = new AsyncFlow(showDialog);\n\n  showDialog.postAsync = async (shared, _, execRes) => { shared.color = execRes; return 'default'; };\n  askForName.postAsync = async (shared, _, execRes) => { shared.name = execRes; return 'default'; };\n\n  await flow.runAsync({});\n}\n\nawait runInteractiveDemo();",
    "raw": "# InteractiveWebviewNode\n\nThe `InteractiveWebviewNode` is a versatile node that allows for user interaction through a webview window. It can be used to display notifications, ask for user input, present a dialog with options, or display fully custom HTML content with interactive elements.\n\n## Parameters\n\n*   `mode` (string, required): The mode of the interactive webview. Can be one of `'notification'`, `'dialog'`, `'input'`, `'custom'`, `'custom-dialog'`, or `'custom-input'`.\n    *   `'notification'`: Displays a message with a close button.\n    *   `'dialog'`: Displays a message with predefined options as buttons.\n    *   `'input'`: Displays a prompt with a text input field.\n    *   `'custom'`: Displays fully custom HTML content. This mode offers complete control over the webview's HTML, CSS, and JavaScript, allowing for entirely custom layouts and styling. However, you must implement your own JavaScript to call the `qflow()` function to return data to the flow.\n    *   `'custom-dialog'`: Displays custom HTML content along with predefined options as buttons.\n    *   `'custom-input'`: Displays custom HTML content along with a text input field and a submit button.\n*   `message` (string, optional): The message to display in the webview. Required for `'notification'` and `'dialog'` modes. Can be used as fallback content for `'custom-dialog'` if `html` is not provided.\n*   `options` (array, optional): An array of strings to be displayed as buttons in `'dialog'` and `'custom-dialog'` modes.\n*   `prompt` (string, optional): The prompt to display in `'input'` mode. Can be used as fallback content for `'custom-input'` if `html` is not provided.\n*   `html` (string, optional): The full HTML content to display in the webview. Required for `'custom'` mode. Used as the primary content for `'custom-dialog'` and `'custom-input'` modes, overriding `message` or `prompt`. For `'custom-dialog'` and `'custom-input'`, your provided HTML will be embedded within the node's default themed container, meaning the overall theme (light/dark) and basic layout are still applied.\n*   `title` (string, optional): The title of the webview window. Defaults to `'Qflow'`.\n*   `width` (number, optional): The width of the webview window. Defaults to `400`.\n*   `height` (number, optional): The height of the webview window. Defaults to `220`.\n*   `theme` (string, optional): The theme of the webview. Can be `'light'` or `'dark'`. Defaults to `'light'`.\n*   `multilineInput` (boolean, optional): If `true` and `mode` is `'input'` or `'custom-input'`, displays a multi-line textarea instead of a single-line input field. Defaults to `false`.\n\n## Output\n\nThe result of the user's interaction is passed to the next node in the flow.\n*   For `'dialog'` and `'custom-dialog'` modes, the result is the text of the button the user clicked.\n*   For `'input'` and `'custom-input'` modes, the result is the text the user entered.\n*   For `'notification'` mode, the result is the string `'closed'`.\n*   For `'custom'` mode, the result is whatever value is passed to the `qflow()` JavaScript function called from within your custom HTML.\n\n## Examples\n\n### Basic Interactive Demo\n\n```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { InteractiveWebviewNode } from '@fractal-solutions/qflow/nodes';\n\nasync function runInteractiveDemo() {\n  const showDialog = new InteractiveWebviewNode();\n  showDialog.setParams({\n    mode: 'dialog',\n    message: 'What is your favorite color?',\n    options: ['Red', 'Green', 'Blue'],\n    title: 'Color Picker',\n    theme: 'dark',\n    width: 500,\n    height: 300,\n  });\n\n  const askForName = new InteractiveWebviewNode();\n  askForName.setParams({\n    mode: 'input',\n    prompt: 'Please enter your name:',\n    title: 'Name Input',\n    theme: 'dark',\n    width: 500,\n    height: 300,\n  });\n\n  const showNotification = new InteractiveWebviewNode();\n\n  class ProcessResultsNode extends AsyncNode {\n    async execAsync(prepRes, shared) {\n      showNotification.setParams({\n        mode: 'notification',\n        message: `Hello, ${shared.name}! Your favorite color is ${shared.color}.`,\n        title: 'Summary',\n        theme: 'dark',\n        width: 500,\n        height: 300,\n      });\n      return 'default';\n    }\n  }\n  const processResults = new ProcessResultsNode();\n\n  showDialog.next(askForName);\n  askForName.next(processResults);\n  processResults.next(showNotification);\n\n  const flow = new AsyncFlow(showDialog);\n\n  showDialog.postAsync = async (shared, _, execRes) => { shared.color = execRes; return 'default'; };\n  askForName.postAsync = async (shared, _, execRes) => { shared.name = execRes; return 'default'; };\n\n  await flow.runAsync({});\n}\n\nawait runInteractiveDemo();\n```\n\n### Custom Dialog Example\n\nThis example demonstrates using `custom-dialog` mode to display custom HTML content along with predefined options.\n\n```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { InteractiveWebviewNode } from '@fractal-solutions/qflow/nodes';\n\nasync function runCustomDialogDemo() {\n  const customDialog = new InteractiveWebviewNode();\n  customDialog.setParams({\n    mode: 'custom-dialog',\n    title: 'Custom Action Required',\n    html: `\n      <div style=\"text-align: center; padding: 20px;\">\n        <h2 style=\"color: #00ffff;\">System Alert!</h2>\n        <p style=\"color: #e0e0e0;\">A critical process requires your immediate attention.</p>\n        <p style=\"color: #e0e0e0;\">Please choose an action to proceed.</p>\n      </div>\n    `,\n    options: ['Acknowledge', 'Investigate', 'Ignore'],\n    theme: 'dark',\n    width: 600,\n    height: 350,\n  });\n\n  class ProcessCustomDialogResult extends AsyncNode {\n    async execAsync(prepRes, shared) {\n      console.log('Custom Dialog Result:', shared.customActionResult);\n      // Further processing based on the user's choice\n      return 'default';\n    }\n  }\n  const processResult = new ProcessCustomDialogResult();\n\n  customDialog.next(processResult);\n\n  const flow = new AsyncFlow(customDialog);\n  customDialog.postAsync = async (shared, _, execRes) => { shared.customActionResult = execRes; return 'default'; };\n\n  await flow.runAsync({});\n}\n\nawait runCustomDialogDemo();\n```\n\n### Custom Input Example\n\nThis example demonstrates using `custom-input` mode to display custom HTML content along with a text input field.\n\n```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { InteractiveWebviewNode } from '@fractal-solutions/qflow/nodes';\n\nasync function runCustomInputDemo() {\n  const customInput = new InteractiveWebviewNode();\n  customInput.setParams({\n    mode: 'custom-input',\n    title: 'User Feedback',\n    html: `\n      <div style=\"text-align: center; padding: 20px;\">\n        <h2 style=\"color: #00ffff;\">We Value Your Feedback</h2>\n        <p style=\"color: #e0e0e0;\">Please enter your suggestions or comments below:</p>\n      </div>\n    `,\n    multilineInput: true,\n    theme: 'dark',\n    width: 700,\n    height: 400,\n  });\n\n  class ProcessCustomInputResult extends AsyncNode {\n    async execAsync(prepRes, shared) {\n      console.log('User Feedback:', shared.userFeedback);\n      // Further processing of the user's input\n      return 'default';\n    }\n  }\n  const processResult = new ProcessCustomInputResult();\n\n  customInput.next(processResult);\n\n  const flow = new AsyncFlow(customInput);\n  customInput.postAsync = async (shared, _, execRes) => { shared.userFeedback = execRes; return 'default'; };\n\n  await flow.runAsync({});\n}\n\nawait runCustomInputDemo();\n```"
  },
  "iterator": {
    "title": "IteratorNode",
    "description": "",
    "parameters": "*   `items`: The list of items to iterate over.\n*   `flow`: The name of the flow to execute for each item, as registered in the flow registry.",
    "returns": "Returns execution result. Writes shared.iteratorResult.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { IteratorNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Sub-Flow Definition ---\n// This flow will be executed for each item by the IteratorNode\nclass ProcessItemNode extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    // 'shared.item' will contain the current item from the iterator\n    const item = shared.item;\n    const processedValue = item * 2 + 5; // Example calculation: double and add 5\n    console.log(`  [SubFlow] Processing item: ${item}, Result: ${processedValue}`);\n    // Store the result back in shared for the main flow to collect\n    shared.processedResult = processedValue;\n    return processedValue; // Return the result of this node\n  }\n}\n\nconst processItemFlow = new AsyncFlow(new ProcessItemNode());\n\n// --- Main Flow Definition ---\n(async () => {\n  console.log('--- Running Iterator Example ---');\n\n  // 1. Define the list of items to iterate over\n  const dataItems = [10, 20, 30];\n\n  // 2. Instantiate the IteratorNode\n  const iteratorNode = new IteratorNode();\n  iteratorNode.setParams({\n    items: dataItems,\n    flow: processItemFlow // Pass the sub-flow instance directly\n  });\n\n  // 3. Define a node to collect results after iteration (optional, but good for demonstration)\n  class CollectResultsNode extends AsyncNode {\n    async execAsync(prepRes, execRes) {\n      // execRes is now the shared object from the previous node (IteratorNode)\n      // The actual results are in execRes.iteratorResult\n      const collectedResults = execRes.iteratorResult;\n\n      console.log('\\n[MainFlow] All items processed. Collected results:');\n      collectedResults.forEach((result, index) => {\n        // dataItems is still out of scope here, so we'll just print the result\n        console.log(`  Result ${index + 1}: ${result}`);\n      });\n      return collectedResults; // Return the actual array of results\n    }\n  }\n  const collectResultsNode = new CollectResultsNode();\n\n  // Chain the nodes\n  iteratorNode.next(collectResultsNode);\n\n  // 4. Create and run the main flow\n  const mainFlow = new AsyncFlow(iteratorNode);\n\n  try {\n    const finalResults = await mainFlow.runAsync({});\n    console.log('\\n--- Iterator Example Finished ---');\n    console.log('Final collected results from main flow:', finalResults);\n  } catch (error) {\n    console.error('\\n--- Iterator Example Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { IteratorNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Sub-Flow Definition ---\n// This flow will be executed for each item by the IteratorNode\nclass ProcessItemNode extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    // 'shared.item' will contain the current item from the iterator\n    const item = shared.item;\n    const processedValue = item * 2 + 5; // Example calculation: double and add 5\n    console.log(`  [SubFlow] Processing item: ${item}, Result: ${processedValue}`);\n    // Store the result back in shared for the main flow to collect\n    shared.processedResult = processedValue;\n    return processedValue; // Return the result of this node\n  }\n}\n\nconst processItemFlow = new AsyncFlow(new ProcessItemNode());\n\n// --- Main Flow Definition ---\n(async () => {\n  console.log('--- Running Iterator Example ---');\n\n  // 1. Define the list of items to iterate over\n  const dataItems = [10, 20, 30];\n\n  // 2. Instantiate the IteratorNode\n  const iteratorNode = new IteratorNode();\n  iteratorNode.setParams({\n    items: dataItems,\n    flow: processItemFlow // Pass the sub-flow instance directly\n  });\n\n  // 3. Define a node to collect results after iteration (optional, but good for demonstration)\n  class CollectResultsNode extends AsyncNode {\n    async execAsync(prepRes, execRes) {\n      // execRes is now the shared object from the previous node (IteratorNode)\n      // The actual results are in execRes.iteratorResult\n      const collectedResults = execRes.iteratorResult;\n\n      console.log('\\n[MainFlow] All items processed. Collected results:');\n      collectedResults.forEach((result, index) => {\n        // dataItems is still out of scope here, so we'll just print the result\n        console.log(`  Result ${index + 1}: ${result}`);\n      });\n      return collectedResults; // Return the actual array of results\n    }\n  }\n  const collectResultsNode = new CollectResultsNode();\n\n  // Chain the nodes\n  iteratorNode.next(collectResultsNode);\n\n  // 4. Create and run the main flow\n  const mainFlow = new AsyncFlow(iteratorNode);\n\n  try {\n    const finalResults = await mainFlow.runAsync({});\n    console.log('\\n--- Iterator Example Finished ---');\n    console.log('Final collected results from main flow:', finalResults);\n  } catch (error) {\n    console.error('\\n--- Iterator Example Failed ---', error);\n  }\n})();",
    "raw": "## IteratorNode\n\nThe `IteratorNode` iterates items, executes sub-flow for each.\n\n### Parameters\n\n*   `items`: The list of items to iterate over.\n*   `flow`: The name of the flow to execute for each item, as registered in the flow registry.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { IteratorNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Sub-Flow Definition ---\n// This flow will be executed for each item by the IteratorNode\nclass ProcessItemNode extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    // 'shared.item' will contain the current item from the iterator\n    const item = shared.item;\n    const processedValue = item * 2 + 5; // Example calculation: double and add 5\n    console.log(`  [SubFlow] Processing item: ${item}, Result: ${processedValue}`);\n    // Store the result back in shared for the main flow to collect\n    shared.processedResult = processedValue;\n    return processedValue; // Return the result of this node\n  }\n}\n\nconst processItemFlow = new AsyncFlow(new ProcessItemNode());\n\n// --- Main Flow Definition ---\n(async () => {\n  console.log('--- Running Iterator Example ---');\n\n  // 1. Define the list of items to iterate over\n  const dataItems = [10, 20, 30];\n\n  // 2. Instantiate the IteratorNode\n  const iteratorNode = new IteratorNode();\n  iteratorNode.setParams({\n    items: dataItems,\n    flow: processItemFlow // Pass the sub-flow instance directly\n  });\n\n  // 3. Define a node to collect results after iteration (optional, but good for demonstration)\n  class CollectResultsNode extends AsyncNode {\n    async execAsync(prepRes, execRes) {\n      // execRes is now the shared object from the previous node (IteratorNode)\n      // The actual results are in execRes.iteratorResult\n      const collectedResults = execRes.iteratorResult;\n\n      console.log('\\n[MainFlow] All items processed. Collected results:');\n      collectedResults.forEach((result, index) => {\n        // dataItems is still out of scope here, so we'll just print the result\n        console.log(`  Result ${index + 1}: ${result}`);\n      });\n      return collectedResults; // Return the actual array of results\n    }\n  }\n  const collectResultsNode = new CollectResultsNode();\n\n  // Chain the nodes\n  iteratorNode.next(collectResultsNode);\n\n  // 4. Create and run the main flow\n  const mainFlow = new AsyncFlow(iteratorNode);\n\n  try {\n    const finalResults = await mainFlow.runAsync({});\n    console.log('\\n--- Iterator Example Finished ---');\n    console.log('Final collected results from main flow:', finalResults);\n  } catch (error) {\n    console.error('\\n--- Iterator Example Failed ---', error);\n  }\n})();\n```"
  },
  "list_directory": {
    "title": "ListDirectoryNode",
    "description": "/**",
    "parameters": "*   `directoryPath`: The absolute path to the directory to list.",
    "returns": "{Promise<string[]>} A promise that resolves to an array of file names.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ListDirectoryNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running ListDirectoryNode Example ---');\n\n  const listDir = new ListDirectoryNode();\n  listDir.setParams({\n    directoryPath: '.' // List current directory\n  });\n\n  const listDirFlow = new AsyncFlow(listDir);\n\n  try {\n    const result = await listDirFlow.runAsync({});\n    console.log('--- Directory Listing Result ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ListDirectoryNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running ListDirectoryNode Example ---');\n\n  const listDir = new ListDirectoryNode();\n  listDir.setParams({\n    directoryPath: '.' // List current directory\n  });\n\n  const listDirFlow = new AsyncFlow(listDir);\n\n  try {\n    const result = await listDirFlow.runAsync({});\n    console.log('--- Directory Listing Result ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();",
    "raw": "## ListDirectoryNode\n\nThe `ListDirectoryNode` lists the files and subdirectories within a specified directory.\n\n### Parameters\n\n*   `directoryPath`: The absolute path to the directory to list.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ListDirectoryNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running ListDirectoryNode Example ---');\n\n  const listDir = new ListDirectoryNode();\n  listDir.setParams({\n    directoryPath: '.' // List current directory\n  });\n\n  const listDirFlow = new AsyncFlow(listDir);\n\n  try {\n    const result = await listDirFlow.runAsync({});\n    console.log('--- Directory Listing Result ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "memory": {
    "title": "MemoryNode",
    "description": "",
    "parameters": "*   `action`: The action to perform: 'store' or 'retrieve'.\n*   `content`: Required for 'store' action. The text content of the memory to store.\n*   `query`: Required for 'retrieve' action. Keywords to search for within stored memories.\n*   `id`: Optional for 'store' action. A unique identifier for the memory. If not provided, one will be generated.\n*   `memoryPath`: Optional. The directory path where memories are stored. Defaults to './agent_memies'.",
    "returns": "Returns execution result. Writes shared.memoryResult.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { MemoryNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running MemoryNode Example ---');\n\n  // --- Example 1: Store a memory ---\n  console.log('\\n--- Storing a Memory ---');\n  const storeMemoryNode = new MemoryNode();\n  storeMemoryNode.setParams({\n    action: 'store',\n    content: 'The capital of France is Paris. It is known for the Eiffel Tower.',\n    id: 'france_capital'\n  });\n\n  const storeFlow = new AsyncFlow(storeMemoryNode);\n  try {\n    const result = await storeFlow.runAsync({});\n    console.log('Store Memory Result:', result);\n  } catch (error) {\n    console.error('Store Memory Flow Failed:', error);\n  }\n\n  // --- Example 2: Retrieve a memory ---\n  console.log('\\n--- Retrieving a Memory ---');\n  const retrieveMemoryNode = new MemoryNode();\n  retrieveMemoryNode.setParams({\n    action: 'retrieve',\n    query: 'Eiffel Tower'\n  });\n\n  const retrieveFlow = new AsyncFlow(retrieveMemoryNode);\n  try {\n    const result = await retrieveFlow.runAsync({});\n    console.log('Retrieve Memory Result:', result);\n  } catch (error) {\n    console.error('Retrieve Memory Flow Failed:', error);\n  }\n\n  console.log('\\n--- MemoryNode Example Finished ---');\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { MemoryNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running MemoryNode Example ---');\n\n  // --- Example 1: Store a memory ---\n  console.log('\\n--- Storing a Memory ---');\n  const storeMemoryNode = new MemoryNode();\n  storeMemoryNode.setParams({\n    action: 'store',\n    content: 'The capital of France is Paris. It is known for the Eiffel Tower.',\n    id: 'france_capital'\n  });\n\n  const storeFlow = new AsyncFlow(storeMemoryNode);\n  try {\n    const result = await storeFlow.runAsync({});\n    console.log('Store Memory Result:', result);\n  } catch (error) {\n    console.error('Store Memory Flow Failed:', error);\n  }\n\n  // --- Example 2: Retrieve a memory ---\n  console.log('\\n--- Retrieving a Memory ---');\n  const retrieveMemoryNode = new MemoryNode();\n  retrieveMemoryNode.setParams({\n    action: 'retrieve',\n    query: 'Eiffel Tower'\n  });\n\n  const retrieveFlow = new AsyncFlow(retrieveMemoryNode);\n  try {\n    const result = await retrieveFlow.runAsync({});\n    console.log('Retrieve Memory Result:', result);\n  } catch (error) {\n    console.error('Retrieve Memory Flow Failed:', error);\n  }\n\n  console.log('\\n--- MemoryNode Example Finished ---');\n})();",
    "raw": "## MemoryNode\n\nThe `MemoryNode` stores and retrieves text memories (keyword-based).\n\n### Parameters\n\n*   `action`: The action to perform: 'store' or 'retrieve'.\n*   `content`: Required for 'store' action. The text content of the memory to store.\n*   `query`: Required for 'retrieve' action. Keywords to search for within stored memories.\n*   `id`: Optional for 'store' action. A unique identifier for the memory. If not provided, one will be generated.\n*   `memoryPath`: Optional. The directory path where memories are stored. Defaults to './agent_memies'.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { MemoryNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running MemoryNode Example ---');\n\n  // --- Example 1: Store a memory ---\n  console.log('\\n--- Storing a Memory ---');\n  const storeMemoryNode = new MemoryNode();\n  storeMemoryNode.setParams({\n    action: 'store',\n    content: 'The capital of France is Paris. It is known for the Eiffel Tower.',\n    id: 'france_capital'\n  });\n\n  const storeFlow = new AsyncFlow(storeMemoryNode);\n  try {\n    const result = await storeFlow.runAsync({});\n    console.log('Store Memory Result:', result);\n  } catch (error) {\n    console.error('Store Memory Flow Failed:', error);\n  }\n\n  // --- Example 2: Retrieve a memory ---\n  console.log('\\n--- Retrieving a Memory ---');\n  const retrieveMemoryNode = new MemoryNode();\n  retrieveMemoryNode.setParams({\n    action: 'retrieve',\n    query: 'Eiffel Tower'\n  });\n\n  const retrieveFlow = new AsyncFlow(retrieveMemoryNode);\n  try {\n    const result = await retrieveFlow.runAsync({});\n    console.log('Retrieve Memory Result:', result);\n  } catch (error) {\n    console.error('Retrieve Memory Flow Failed:', error);\n  }\n\n  console.log('\\n--- MemoryNode Example Finished ---');\n})();\n```"
  },
  "multimedia_processing": {
    "title": "MultimediaProcessingNode",
    "description": "",
    "parameters": "*   `action`: The multimedia operation to perform.\n*   `inputPath`: Path to the input multimedia file.\n*   `outputPath`: Path for the output file.\n*   `format`: Required for 'convert' and 'extract_audio'. The output format (e.g., 'mp4', 'mp3', 'gif', 'wav').\n*   `startTime`: Required for 'trim'. Start time in HH:MM:SS or seconds (e.g., '00:00:10', '10').\n*   `duration`: Required for 'trim'. Duration in HH:MM:SS or seconds (e.g., '00:00:05', '5').\n*   `resolution`: Optional for video 'convert'. Resolution (e.g., '1280x720').\n*   `frameTime`: Required for 'extract_frame'. Time to extract frame from in HH:MM:SS or seconds (e.g., '00:00:05').\n*   `ffmpegArgs`: Required for 'custom'. Raw ffmpeg arguments to execute directly.",
    "returns": "Returns processing status and output file path(s).",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { MultimediaProcessingNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {  \n        console.log('--- Running MultimediaProcessingNode Example ---');\n        const tempDir = os.tmpdir();\n        const inputVideoPath = path.join('sample_video.mp4'); // User should provide this  \n        const outputGifPath = path.join(tempDir, 'output_video.gif');  \n\n        // --- IMPORTANT: Prerequisites ---\n        console.log(`[Setup] This example requires an input video file: ${inputVideoPath}`);  \n        console.log(\"[Setup] Please place a 'sample_video.mp4' file in your project root directory.\");  \n        console.log(\"[Setup] Also, ensure 'ffmpeg' is installed and in your system's PATH.\");  \n\n        // Check if input video exists  \n        try {    \n            await fs.access(inputVideoPath);    \n            console.log(`[Setup] Input video found: ${inputVideoPath}`);  \n        } catch (error) {    \n            console.error(`[Setup] Input video not found at ${inputVideoPath}. Please place it there to run the example.`);    \n            return;  \n        }  \n\n        // --- Example: Convert video to GIF ---  \n        console.log('\\n--- Converting video to GIF ---');  \n        const convertNode = new MultimediaProcessingNode();  \n        convertNode.setParams({    \n            action: 'convert',    \n            inputPath: inputVideoPath,    \n            outputPath: outputGifPath,    \n            format: 'gif',    \n            resolution: '320x240' // Smaller resolution for GIF  \n        });  \n        \n        try {    \n            const result = await new AsyncFlow(convertNode).runAsync({});    \n            console.log('Video to GIF conversion successful:', result);  \n        } catch (error) {    \n            console.error('Video to GIF conversion failed:', error.message);  \n        }  \n        \n        console.log('\\n--- MultimediaProcessingNode Example Finished ---');  \n        \n        // --- Cleanup ---  \n        try {    \n            console.log('\\n[Cleanup] Removing generated files...');    \n            await fs.unlink(outputGifPath).catch(() => {});    \n            console.log('[Cleanup] Cleanup complete.');  \n        } catch (e) {    \n            console.warn('[Cleanup] Failed to remove some temporary files:', e.message);  \n        } \n})();",
    "example": "",
    "raw": "## MultimediaProcessingNode\n\nThe `MultimediaProcessingNode` performs various multimedia operations on audio and video files using ffmpeg.\n\n### Parameters\n\n*   `action`: The multimedia operation to perform.\n*   `inputPath`: Path to the input multimedia file.\n*   `outputPath`: Path for the output file.\n*   `format`: Required for 'convert' and 'extract_audio'. The output format (e.g., 'mp4', 'mp3', 'gif', 'wav').\n*   `startTime`: Required for 'trim'. Start time in HH:MM:SS or seconds (e.g., '00:00:10', '10').\n*   `duration`: Required for 'trim'. Duration in HH:MM:SS or seconds (e.g., '00:00:05', '5').\n*   `resolution`: Optional for video 'convert'. Resolution (e.g., '1280x720').\n*   `frameTime`: Required for 'extract_frame'. Time to extract frame from in HH:MM:SS or seconds (e.g., '00:00:05').\n*   `ffmpegArgs`: Required for 'custom'. Raw ffmpeg arguments to execute directly.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { MultimediaProcessingNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {  \n        console.log('--- Running MultimediaProcessingNode Example ---');\n        const tempDir = os.tmpdir();\n        const inputVideoPath = path.join('sample_video.mp4'); // User should provide this  \n        const outputGifPath = path.join(tempDir, 'output_video.gif');  \n\n        // --- IMPORTANT: Prerequisites ---\n        console.log(`[Setup] This example requires an input video file: ${inputVideoPath}`);  \n        console.log(\"[Setup] Please place a 'sample_video.mp4' file in your project root directory.\");  \n        console.log(\"[Setup] Also, ensure 'ffmpeg' is installed and in your system's PATH.\");  \n\n        // Check if input video exists  \n        try {    \n            await fs.access(inputVideoPath);    \n            console.log(`[Setup] Input video found: ${inputVideoPath}`);  \n        } catch (error) {    \n            console.error(`[Setup] Input video not found at ${inputVideoPath}. Please place it there to run the example.`);    \n            return;  \n        }  \n\n        // --- Example: Convert video to GIF ---  \n        console.log('\\n--- Converting video to GIF ---');  \n        const convertNode = new MultimediaProcessingNode();  \n        convertNode.setParams({    \n            action: 'convert',    \n            inputPath: inputVideoPath,    \n            outputPath: outputGifPath,    \n            format: 'gif',    \n            resolution: '320x240' // Smaller resolution for GIF  \n        });  \n        \n        try {    \n            const result = await new AsyncFlow(convertNode).runAsync({});    \n            console.log('Video to GIF conversion successful:', result);  \n        } catch (error) {    \n            console.error('Video to GIF conversion failed:', error.message);  \n        }  \n        \n        console.log('\\n--- MultimediaProcessingNode Example Finished ---');  \n        \n        // --- Cleanup ---  \n        try {    \n            console.log('\\n[Cleanup] Removing generated files...');    \n            await fs.unlink(outputGifPath).catch(() => {});    \n            console.log('[Cleanup] Cleanup complete.');  \n        } catch (e) {    \n            console.warn('[Cleanup] Failed to remove some temporary files:', e.message);  \n        } \n})();"
  },
  "navigator": {
    "title": "NavigatorNode",
    "description": "",
    "parameters": "* `action` (required): Operation to run.\n* `baseUrl`: PinchTab orchestrator URL. Default: `http://localhost:9867`.\n* `autoStartOrchestrator`: Start PinchTab automatically if unreachable.\n* `orchestratorWaitMs`: Startup wait window in milliseconds.\n* `pinchtabBinaryPath`: Optional full path to the PinchTab binary.\n\nCommon scoped identifiers:\n* `profileId`, `profileName`\n* `instanceId`\n* `tabId`\n\nCommon action payload fields:\n* `url`, `timeout`, `blockImages`, `blockAds`\n* `snapshotOptions` (`interactive`, `compact`, `depth`, `maxTokens`, `filter`)\n* `interaction` (single action payload)\n* `interactions` (array for batch actions)\n* `expression`, `awaitExpression`\n* `screenshotOptions`, `pdfOptions`\n* `outputPath`, `includeBase64`\n* `owner`, `ttl` (locking)\n* `cookies`\n* `waitTimeoutMs`, `pollIntervalMs`",
    "returns": "After execution, the node writes:\n* `shared.navigatorLastResult`\n\nFor creation actions:\n* `shared.navigatorProfileId` (after `create_profile`)\n* `shared.navigatorInstanceId` (after `start_instance`)\n* `shared.navigatorTabId` (after `open_tab`)",
    "notes": "",
    "requirements": "* PinchTab must be installed and accessible.\n* Default orchestrator URL is `http://localhost:9867`.\n* You can set `autoStartOrchestrator: true` to let the node start PinchTab if it is not already running.",
    "examplesText": "See full example: [examples/navigator_node_example.js](../../examples/navigator_node_example.js)\n\n```javascript\nimport { NavigatorNode } from '@fractal-solutions/qflow/nodes';\n\nconst shared = {};\n\nconst ensure = new NavigatorNode();\nensure.setParams({\n  action: 'ensure_orchestrator',\n  baseUrl: 'http://localhost:9867',\n  autoStartOrchestrator: true\n});\nawait ensure.runAsync(shared);\n\nconst start = new NavigatorNode();\nstart.setParams({ action: 'start_instance', mode: 'headless' });\nawait start.runAsync(shared);\n\nconst open = new NavigatorNode();\nopen.setParams({\n  action: 'open_tab',\n  instanceId: shared.navigatorInstanceId,\n  url: 'https://example.com'\n});\nawait open.runAsync(shared);\n\n// Continue with snapshot/action/text/screenshot operations using shared.navigatorTabId.\n```",
    "example": "import { NavigatorNode } from '@fractal-solutions/qflow/nodes';\n\nconst shared = {};\n\nconst ensure = new NavigatorNode();\nensure.setParams({\n  action: 'ensure_orchestrator',\n  baseUrl: 'http://localhost:9867',\n  autoStartOrchestrator: true\n});\nawait ensure.runAsync(shared);\n\nconst start = new NavigatorNode();\nstart.setParams({ action: 'start_instance', mode: 'headless' });\nawait start.runAsync(shared);\n\nconst open = new NavigatorNode();\nopen.setParams({\n  action: 'open_tab',\n  instanceId: shared.navigatorInstanceId,\n  url: 'https://example.com'\n});\nawait open.runAsync(shared);\n\n// Continue with snapshot/action/text/screenshot operations using shared.navigatorTabId.",
    "raw": "## NavigatorNode\n\n`NavigatorNode` integrates qflow with PinchTab for tab-centric browser automation. It supports profile/instance/tab lifecycle operations, page interaction, extraction, media export, locking, and fingerprint operations.\n\n### Requirements\n\n* PinchTab must be installed and accessible.\n* Default orchestrator URL is `http://localhost:9867`.\n* You can set `autoStartOrchestrator: true` to let the node start PinchTab if it is not already running.\n\n### Parameters\n\n* `action` (required): Operation to run.\n* `baseUrl`: PinchTab orchestrator URL. Default: `http://localhost:9867`.\n* `autoStartOrchestrator`: Start PinchTab automatically if unreachable.\n* `orchestratorWaitMs`: Startup wait window in milliseconds.\n* `pinchtabBinaryPath`: Optional full path to the PinchTab binary.\n\nCommon scoped identifiers:\n* `profileId`, `profileName`\n* `instanceId`\n* `tabId`\n\nCommon action payload fields:\n* `url`, `timeout`, `blockImages`, `blockAds`\n* `snapshotOptions` (`interactive`, `compact`, `depth`, `maxTokens`, `filter`)\n* `interaction` (single action payload)\n* `interactions` (array for batch actions)\n* `expression`, `awaitExpression`\n* `screenshotOptions`, `pdfOptions`\n* `outputPath`, `includeBase64`\n* `owner`, `ttl` (locking)\n* `cookies`\n* `waitTimeoutMs`, `pollIntervalMs`\n\n### Supported Actions\n\nOrchestrator:\n* `ensure_orchestrator`\n\nProfiles:\n* `create_profile`\n* `list_profiles`\n* `get_profile`\n* `delete_profile`\n\nInstances:\n* `start_instance`\n* `list_instances`\n* `get_instance`\n* `wait_instance_ready`\n* `stop_instance`\n* `get_instance_logs`\n\nTabs and page operations:\n* `open_tab`\n* `list_tabs`\n* `get_tab`\n* `close_tab`\n* `navigate`\n* `snapshot`\n* `text`\n* `evaluate`\n* `action`\n* `actions`\n* `screenshot`\n* `pdf`\n\nAdvanced tab controls:\n* `get_cookies`\n* `set_cookies`\n* `delete_cookies`\n* `lock_tab`\n* `unlock_tab`\n* `tab_lock_status`\n* `rotate_fingerprint`\n* `fingerprint_status`\n\n### Shared State Outputs\n\nAfter execution, the node writes:\n* `shared.navigatorLastResult`\n\nFor creation actions:\n* `shared.navigatorProfileId` (after `create_profile`)\n* `shared.navigatorInstanceId` (after `start_instance`)\n* `shared.navigatorTabId` (after `open_tab`)\n\n### Example Usage\n\nSee full example: [examples/navigator_node_example.js](../../examples/navigator_node_example.js)\n\n```javascript\nimport { NavigatorNode } from '@fractal-solutions/qflow/nodes';\n\nconst shared = {};\n\nconst ensure = new NavigatorNode();\nensure.setParams({\n  action: 'ensure_orchestrator',\n  baseUrl: 'http://localhost:9867',\n  autoStartOrchestrator: true\n});\nawait ensure.runAsync(shared);\n\nconst start = new NavigatorNode();\nstart.setParams({ action: 'start_instance', mode: 'headless' });\nawait start.runAsync(shared);\n\nconst open = new NavigatorNode();\nopen.setParams({\n  action: 'open_tab',\n  instanceId: shared.navigatorInstanceId,\n  url: 'https://example.com'\n});\nawait open.runAsync(shared);\n\n// Continue with snapshot/action/text/screenshot operations using shared.navigatorTabId.\n```"
  },
  "ollama_llm": {
    "title": "OllamaLLMNode",
    "description": "",
    "parameters": "*   `prompt`: The prompt to send to the model.\n*   `model`: The model to use.\n*   `baseUrl`: The base URL of the Ollama API (e.g., 'http://localhost:11434').",
    "returns": "Returns the generated response text.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { OllamaLLMNode } from '@fractal-solutions/qflow/nodes';\n\n// Configuration for Ollama\nconst OLLAMA_BASE_URL = process.env.OLLAMA_BASE_URL || 'http://localhost:11434';\nconst OLLAMA_MODEL = process.env.OLLAMA_MODEL || 'gemma3:1b'; \n\n(async () => {\n  console.log('--- Running Direct OllamaLLMNode Test Workflow ---');\n\n  const ollamaDirectLLM = new OllamaLLMNode();\n  ollamaDirectLLM.setParams({\n    prompt: 'Tell me a short, funny story about a cat.',\n    model: OLLAMA_MODEL,\n    baseUrl: OLLAMA_BASE_URL\n  });\n\n  ollamaDirectLLM.postAsync = async (shared, prepRes, execRes) => {\n    console.log('Ollama Direct LLM Response:', execRes);\n    return 'default';\n  };\n\n  const directFlow = new AsyncFlow(ollamaDirectLLM);\n  try {\n    await directFlow.runAsync({});\n    console.log('\\n--- Direct OllamaLLMNode Test Workflow Finished ---');\n  } catch (error) {\n    console.error('\\n--- Direct OllamaLLMNode Flow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { OllamaLLMNode } from '@fractal-solutions/qflow/nodes';\n\n// Configuration for Ollama\nconst OLLAMA_BASE_URL = process.env.OLLAMA_BASE_URL || 'http://localhost:11434';\nconst OLLAMA_MODEL = process.env.OLLAMA_MODEL || 'gemma3:1b'; \n\n(async () => {\n  console.log('--- Running Direct OllamaLLMNode Test Workflow ---');\n\n  const ollamaDirectLLM = new OllamaLLMNode();\n  ollamaDirectLLM.setParams({\n    prompt: 'Tell me a short, funny story about a cat.',\n    model: OLLAMA_MODEL,\n    baseUrl: OLLAMA_BASE_URL\n  });\n\n  ollamaDirectLLM.postAsync = async (shared, prepRes, execRes) => {\n    console.log('Ollama Direct LLM Response:', execRes);\n    return 'default';\n  };\n\n  const directFlow = new AsyncFlow(ollamaDirectLLM);\n  try {\n    await directFlow.runAsync({});\n    console.log('\\n--- Direct OllamaLLMNode Test Workflow Finished ---');\n  } catch (error) {\n    console.error('\\n--- Direct OllamaLLMNode Flow Failed ---', error);\n  }\n})();",
    "raw": "## OllamaLLMNode\n\nThe `OllamaLLMNode` is used to interact with a local Ollama instance.\n\n### Parameters\n\n*   `prompt`: The prompt to send to the model.\n*   `model`: The model to use.\n*   `baseUrl`: The base URL of the Ollama API (e.g., 'http://localhost:11434').\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { OllamaLLMNode } from '@fractal-solutions/qflow/nodes';\n\n// Configuration for Ollama\nconst OLLAMA_BASE_URL = process.env.OLLAMA_BASE_URL || 'http://localhost:11434';\nconst OLLAMA_MODEL = process.env.OLLAMA_MODEL || 'gemma3:1b'; \n\n(async () => {\n  console.log('--- Running Direct OllamaLLMNode Test Workflow ---');\n\n  const ollamaDirectLLM = new OllamaLLMNode();\n  ollamaDirectLLM.setParams({\n    prompt: 'Tell me a short, funny story about a cat.',\n    model: OLLAMA_MODEL,\n    baseUrl: OLLAMA_BASE_URL\n  });\n\n  ollamaDirectLLM.postAsync = async (shared, prepRes, execRes) => {\n    console.log('Ollama Direct LLM Response:', execRes);\n    return 'default';\n  };\n\n  const directFlow = new AsyncFlow(ollamaDirectLLM);\n  try {\n    await directFlow.runAsync({});\n    console.log('\\n--- Direct OllamaLLMNode Test Workflow Finished ---');\n  } catch (error) {\n    console.error('\\n--- Direct OllamaLLMNode Flow Failed ---', error);\n  }\n})();\n```"
  },
  "openai_llm": {
    "title": "OpenAILLMNode",
    "description": "",
    "parameters": "*   `apiKey`: Your OpenAI API key.\n*   `prompt`: The prompt to send to the model.\n*   `model`: The model to use.\n*   `temperature`: The temperature to use for the generation.\n*   `max_tokens`: The maximum number of tokens to generate.",
    "returns": "Returns the generated response text.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { OpenAILLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst OPENAI_API_KEY = process.env.OPENAI_API_KEY;\n\n(async () => {\n  if (!OPENAI_API_KEY) {\n    console.warn(\"WARNING: OPENAI_API_KEY is not set. Please set it to run the OpenAILLMNode example.\");\n    return;\n  }\n\n  console.log('--- Running OpenAILLMNode Example ---');\n\n  const openaiNode = new OpenAILLMNode();\n  openaiNode.setParams({\n    apiKey: OPENAI_API_KEY,\n    prompt: \"Write a haiku about a sunny day.\",\n    model: \"gpt-3.5-turbo\" // Common OpenAI model\n  });\n\n  const openaiFlow = new AsyncFlow(openaiNode);\n\n  try {\n    const result = await openaiFlow.runAsync({});\n    console.log('--- OpenAI Response ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { OpenAILLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst OPENAI_API_KEY = process.env.OPENAI_API_KEY;\n\n(async () => {\n  if (!OPENAI_API_KEY) {\n    console.warn(\"WARNING: OPENAI_API_KEY is not set. Please set it to run the OpenAILLMNode example.\");\n    return;\n  }\n\n  console.log('--- Running OpenAILLMNode Example ---');\n\n  const openaiNode = new OpenAILLMNode();\n  openaiNode.setParams({\n    apiKey: OPENAI_API_KEY,\n    prompt: \"Write a haiku about a sunny day.\",\n    model: \"gpt-3.5-turbo\" // Common OpenAI model\n  });\n\n  const openaiFlow = new AsyncFlow(openaiNode);\n\n  try {\n    const result = await openaiFlow.runAsync({});\n    console.log('--- OpenAI Response ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();",
    "raw": "## OpenAILLMNode\n\nThe `OpenAILLMNode` is used to interact with the OpenAI API.\n\n### Parameters\n\n*   `apiKey`: Your OpenAI API key.\n*   `prompt`: The prompt to send to the model.\n*   `model`: The model to use.\n*   `temperature`: The temperature to use for the generation.\n*   `max_tokens`: The maximum number of tokens to generate.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { OpenAILLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst OPENAI_API_KEY = process.env.OPENAI_API_KEY;\n\n(async () => {\n  if (!OPENAI_API_KEY) {\n    console.warn(\"WARNING: OPENAI_API_KEY is not set. Please set it to run the OpenAILLMNode example.\");\n    return;\n  }\n\n  console.log('--- Running OpenAILLMNode Example ---');\n\n  const openaiNode = new OpenAILLMNode();\n  openaiNode.setParams({\n    apiKey: OPENAI_API_KEY,\n    prompt: \"Write a haiku about a sunny day.\",\n    model: \"gpt-3.5-turbo\" // Common OpenAI model\n  });\n\n  const openaiFlow = new AsyncFlow(openaiNode);\n\n  try {\n    const result = await openaiFlow.runAsync({});\n    console.log('--- OpenAI Response ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "openrouter_llm": {
    "title": "OpenRouterLLMNode",
    "description": "",
    "parameters": "*   `apiKey`: Your OpenRouter API key.\n*   `prompt`: The prompt to send to the model.\n*   `model`: The OpenRouter model ID (e.g., 'openai/gpt-4o', 'mistralai/mistral-7b-instruct').\n*   `siteUrl`: Optional. Site URL for rankings on openrouter.ai.\n*   `siteTitle`: Optional. Site title for rankings on openrouter.ai.",
    "returns": "Returns the generated response text.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { OpenRouterLLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst OPENROUTER_API_KEY = process.env.OPENROUTER_API_KEY;\n\n(async () => {\n  if (!OPENROUTER_API_KEY) {\n    console.warn(\"WARNING: OPENROUTER_API_KEY is not set. Please set it to run the OpenRouterLLMNode example.\");\n    return;\n  }\n\n  console.log('--- Running OpenRouterLLMNode Example ---');\n\n  const openrouterNode = new OpenRouterLLMNode();\n  openrouterNode.setParams({\n    apiKey: OPENROUTER_API_KEY,\n    prompt: \"What is the capital of Canada?\",\n    model: \"mistralai/mistral-7b-instruct\" // A common OpenRouter model\n  });\n\n  const openrouterFlow = new AsyncFlow(openrouterNode);\n\n  try {\n    const result = await openrouterFlow.runAsync({});\n    console.log('--- OpenRouter Response ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { OpenRouterLLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst OPENROUTER_API_KEY = process.env.OPENROUTER_API_KEY;\n\n(async () => {\n  if (!OPENROUTER_API_KEY) {\n    console.warn(\"WARNING: OPENROUTER_API_KEY is not set. Please set it to run the OpenRouterLLMNode example.\");\n    return;\n  }\n\n  console.log('--- Running OpenRouterLLMNode Example ---');\n\n  const openrouterNode = new OpenRouterLLMNode();\n  openrouterNode.setParams({\n    apiKey: OPENROUTER_API_KEY,\n    prompt: \"What is the capital of Canada?\",\n    model: \"mistralai/mistral-7b-instruct\" // A common OpenRouter model\n  });\n\n  const openrouterFlow = new AsyncFlow(openrouterNode);\n\n  try {\n    const result = await openrouterFlow.runAsync({});\n    console.log('--- OpenRouter Response ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();",
    "raw": "## OpenRouterLLMNode\n\nThe `OpenRouterLLMNode` is used to interact with the OpenRouter API.\n\n### Parameters\n\n*   `apiKey`: Your OpenRouter API key.\n*   `prompt`: The prompt to send to the model.\n*   `model`: The OpenRouter model ID (e.g., 'openai/gpt-4o', 'mistralai/mistral-7b-instruct').\n*   `siteUrl`: Optional. Site URL for rankings on openrouter.ai.\n*   `siteTitle`: Optional. Site title for rankings on openrouter.ai.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { OpenRouterLLMNode } from '@fractal-solutions/qflow/nodes';\n\nconst OPENROUTER_API_KEY = process.env.OPENROUTER_API_KEY;\n\n(async () => {\n  if (!OPENROUTER_API_KEY) {\n    console.warn(\"WARNING: OPENROUTER_API_KEY is not set. Please set it to run the OpenRouterLLMNode example.\");\n    return;\n  }\n\n  console.log('--- Running OpenRouterLLMNode Example ---');\n\n  const openrouterNode = new OpenRouterLLMNode();\n  openrouterNode.setParams({\n    apiKey: OPENROUTER_API_KEY,\n    prompt: \"What is the capital of Canada?\",\n    model: \"mistralai/mistral-7b-instruct\" // A common OpenRouter model\n  });\n\n  const openrouterFlow = new AsyncFlow(openrouterNode);\n\n  try {\n    const result = await openrouterFlow.runAsync({});\n    console.log('--- OpenRouter Response ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "pdf_processor": {
    "title": "PDFProcessorNode",
    "description": "",
    "parameters": "*   `filePath`: The absolute path to the PDF file.\n*   `action`: The action to perform: 'extract_text' or 'extract_images'.\n*   `outputDir`: Optional. Directory to save extracted files. If not provided, a temporary directory will be used.\n*   `pageRange`: Optional. Page range to process (e.g., {start: 1, end: 5}).\n*   `password`: Optional. Password for encrypted PDFs.",
    "returns": "Returns extracted text/images depending on action.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { PDFProcessorNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {  \n    console.log('--- Running PDFProcessorNode Example ---');  \n    const currentDir = process.cwd(); // Get current working directory\n  const tempDir = os.tmpdir(); // Used for temporary output files\n  const dummyPdfPath = path.join(currentDir, 'example.pdf'); // Use example.pdf in current directory\n\n  console.log(\"[Setup] Please ensure you have an 'example.pdf' file in your current directory:\");\n  console.log(`[Setup] ${dummyPdfPath}`);\n  console.log(\"[Setup] This example will attempt to process that file.\");\n  console.log(\"[Setup] If 'example.pdf' does not exist, the example will fail.\");\n\n  // --- Example: Extract All Text ---\n  console.log('\\n--- Extracting All Text from PDF ---');\n  const extractAllTextNode = new PDFProcessorNode();\n  extractAllTextNode.setParams({\n    filePath: dummyPdfPath,\n    action: 'extract_text',\n    outputDir: tempDir // Save extracted text to tempDir\n    // password: 'your_password' // Uncomment and set if PDF is password protected\n  });\n\n  try {\n    const textResult = await new AsyncFlow(extractAllTextNode).runAsync({});\n    console.log('Extracted All Text (first 200 chars):', textResult.text.substring(0, 200) + '...');\n    console.log('Extracted All Text saved to:', textResult.outputFilePath);\n  } catch (error) {\n    console.error('All Text Extraction Failed:', error.message);\n  }\n\n  console.log('\\n--- PDFProcessorNode Example Finished ---');\n\n  // --- Cleanup ---\n  // Clean up all generated files and directories\n  try {\n    console.log('\\n[Cleanup] Cleaning up generated files and directories...');\n    await fs.rm(path.join(tempDir, 'example_all_extracted.txt'), { force: true }).catch(() => {});\n    console.log('[Cleanup] Cleanup complete.');\n  } catch (e) {\n    console.warn('[Cleanup] Failed to remove some temporary files/directories:', e.message);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { PDFProcessorNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {  \n    console.log('--- Running PDFProcessorNode Example ---');  \n    const currentDir = process.cwd(); // Get current working directory\n  const tempDir = os.tmpdir(); // Used for temporary output files\n  const dummyPdfPath = path.join(currentDir, 'example.pdf'); // Use example.pdf in current directory\n\n  console.log(\"[Setup] Please ensure you have an 'example.pdf' file in your current directory:\");\n  console.log(`[Setup] ${dummyPdfPath}`);\n  console.log(\"[Setup] This example will attempt to process that file.\");\n  console.log(\"[Setup] If 'example.pdf' does not exist, the example will fail.\");\n\n  // --- Example: Extract All Text ---\n  console.log('\\n--- Extracting All Text from PDF ---');\n  const extractAllTextNode = new PDFProcessorNode();\n  extractAllTextNode.setParams({\n    filePath: dummyPdfPath,\n    action: 'extract_text',\n    outputDir: tempDir // Save extracted text to tempDir\n    // password: 'your_password' // Uncomment and set if PDF is password protected\n  });\n\n  try {\n    const textResult = await new AsyncFlow(extractAllTextNode).runAsync({});\n    console.log('Extracted All Text (first 200 chars):', textResult.text.substring(0, 200) + '...');\n    console.log('Extracted All Text saved to:', textResult.outputFilePath);\n  } catch (error) {\n    console.error('All Text Extraction Failed:', error.message);\n  }\n\n  console.log('\\n--- PDFProcessorNode Example Finished ---');\n\n  // --- Cleanup ---\n  // Clean up all generated files and directories\n  try {\n    console.log('\\n[Cleanup] Cleaning up generated files and directories...');\n    await fs.rm(path.join(tempDir, 'example_all_extracted.txt'), { force: true }).catch(() => {});\n    console.log('[Cleanup] Cleanup complete.');\n  } catch (e) {\n    console.warn('[Cleanup] Failed to remove some temporary files/directories:', e.message);\n  }\n})();",
    "raw": "## PDFProcessorNode\n\nThe `PDFProcessorNode` extracts text or images from PDF documents.\n\n### Parameters\n\n*   `filePath`: The absolute path to the PDF file.\n*   `action`: The action to perform: 'extract_text' or 'extract_images'.\n*   `outputDir`: Optional. Directory to save extracted files. If not provided, a temporary directory will be used.\n*   `pageRange`: Optional. Page range to process (e.g., {start: 1, end: 5}).\n*   `password`: Optional. Password for encrypted PDFs.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { PDFProcessorNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {  \n    console.log('--- Running PDFProcessorNode Example ---');  \n    const currentDir = process.cwd(); // Get current working directory\n  const tempDir = os.tmpdir(); // Used for temporary output files\n  const dummyPdfPath = path.join(currentDir, 'example.pdf'); // Use example.pdf in current directory\n\n  console.log(\"[Setup] Please ensure you have an 'example.pdf' file in your current directory:\");\n  console.log(`[Setup] ${dummyPdfPath}`);\n  console.log(\"[Setup] This example will attempt to process that file.\");\n  console.log(\"[Setup] If 'example.pdf' does not exist, the example will fail.\");\n\n  // --- Example: Extract All Text ---\n  console.log('\\n--- Extracting All Text from PDF ---');\n  const extractAllTextNode = new PDFProcessorNode();\n  extractAllTextNode.setParams({\n    filePath: dummyPdfPath,\n    action: 'extract_text',\n    outputDir: tempDir // Save extracted text to tempDir\n    // password: 'your_password' // Uncomment and set if PDF is password protected\n  });\n\n  try {\n    const textResult = await new AsyncFlow(extractAllTextNode).runAsync({});\n    console.log('Extracted All Text (first 200 chars):', textResult.text.substring(0, 200) + '...');\n    console.log('Extracted All Text saved to:', textResult.outputFilePath);\n  } catch (error) {\n    console.error('All Text Extraction Failed:', error.message);\n  }\n\n  console.log('\\n--- PDFProcessorNode Example Finished ---');\n\n  // --- Cleanup ---\n  // Clean up all generated files and directories\n  try {\n    console.log('\\n[Cleanup] Cleaning up generated files and directories...');\n    await fs.rm(path.join(tempDir, 'example_all_extracted.txt'), { force: true }).catch(() => {});\n    console.log('[Cleanup] Cleanup complete.');\n  } catch (e) {\n    console.warn('[Cleanup] Failed to remove some temporary files/directories:', e.message);\n  }\n})();\n```"
  },
  "read_file": {
    "title": "ReadFileNode",
    "description": "/**",
    "parameters": "*   `filePath`: The absolute path to the file to read.",
    "returns": "{Promise<string>} A promise that resolves to the content of the file.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ReadFileNode, WriteFileNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running ReadFileNode Example ---');\n\n  // First, create a dummy file to read\n  const writeFile = new WriteFileNode();\n  writeFile.setParams({\n    filePath: './temp_read_test.txt',\n    content: 'This is a test file for ReadFileNode.'\n  });\n\n  // Then, read the content of the dummy file\n  const readFile = new ReadFileNode();\n  readFile.setParams({\n    filePath: './temp_read_test.txt'\n  });\n\n  writeFile.next(readFile);\n\n  const readFlow = new AsyncFlow(writeFile);\n\n  try {\n    const result = await readFlow.runAsync({});\n    console.log('--- ReadFileNode Result ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ReadFileNode, WriteFileNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running ReadFileNode Example ---');\n\n  // First, create a dummy file to read\n  const writeFile = new WriteFileNode();\n  writeFile.setParams({\n    filePath: './temp_read_test.txt',\n    content: 'This is a test file for ReadFileNode.'\n  });\n\n  // Then, read the content of the dummy file\n  const readFile = new ReadFileNode();\n  readFile.setParams({\n    filePath: './temp_read_test.txt'\n  });\n\n  writeFile.next(readFile);\n\n  const readFlow = new AsyncFlow(writeFile);\n\n  try {\n    const result = await readFlow.runAsync({});\n    console.log('--- ReadFileNode Result ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();",
    "raw": "## ReadFileNode\n\nThe `ReadFileNode` reads the content of a specified file.\n\n### Parameters\n\n*   `filePath`: The absolute path to the file to read.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ReadFileNode, WriteFileNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running ReadFileNode Example ---');\n\n  // First, create a dummy file to read\n  const writeFile = new WriteFileNode();\n  writeFile.setParams({\n    filePath: './temp_read_test.txt',\n    content: 'This is a test file for ReadFileNode.'\n  });\n\n  // Then, read the content of the dummy file\n  const readFile = new ReadFileNode();\n  readFile.setParams({\n    filePath: './temp_read_test.txt'\n  });\n\n  writeFile.next(readFile);\n\n  const readFlow = new AsyncFlow(writeFile);\n\n  try {\n    const result = await readFlow.runAsync({});\n    console.log('--- ReadFileNode Result ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "remote_execution": {
    "title": "RemoteExecutionNode",
    "description": "",
    "parameters": "*   `host`: The hostname or IP address of the remote machine.\n*   `port`: Optional. The SSH port. Defaults to 22.\n*   `username`: The username for SSH authentication.\n*   `password`: Optional. The password for SSH authentication (use with caution, prefer privateKey).\n*   `privateKey`: Optional. The content of the private SSH key or its absolute path.\n*   `passphrase`: Optional. The passphrase for an encrypted private key.\n*   `action`: The action to perform. Currently only 'execute_command' is supported.\n*   `command`: The command string to execute on the remote machine.\n*   `timeout`: Optional. Timeout in milliseconds for the command execution. Defaults to 30000 (30 seconds).",
    "returns": "Returns stdout, stderr, and exit status of the remote command.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { RemoteExecutionNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\n\n(async () => {\n  console.log('--- Running RemoteExecutionNode Example ---');\n\n  // --- IMPORTANT: Configuration ---\n  // Replace with your actual remote server details.\n  // For security, prefer using SSH keys over passwords.\n  const REMOTE_HOST = 'your_remote_host_or_ip'; // e.g., '192.168.1.100', 'my-server.example.com'\n  const REMOTE_USERNAME = 'your_username'; // e.g., 'ubuntu', 'ec2-user'\n  // const REMOTE_PASSWORD = process.env.REMOTE_SSH_PASSWORD; // Set as environment variable (use with caution!)\n  const REMOTE_PRIVATE_KEY_PATH = path.join(os.homedir(), '.ssh', 'id_rsa'); // Path to your private key\n  // const REMOTE_PASSPHRASE = process.env.REMOTE_SSH_PASSPHRASE; // Set as environment variable if key is encrypted\n\n  console.log(`\\n[Setup] This example requires a remote SSH server and proper authentication configured.`);\n  console.log(\"[Setup] Ensure 'ssh2' library is installed (`npm install ssh2` or `bun add ssh2`).\");\n\n  // --- Example: Execute a simple command ---\n  console.log('\\n--- Executing \"ls -l /\" on remote host ---');\n  const lsCommandNode = new RemoteExecutionNode();\n  lsCommandNode.setParams({\n    host: REMOTE_HOST,\n    username: REMOTE_USERNAME,\n    // password: REMOTE_PASSWORD, // Uncomment if using password auth\n    privateKey: REMOTE_PRIVATE_KEY_PATH, // Uncomment if using private key auth\n    // passphrase: REMOTE_PASSPHRASE, // Uncomment if private key is encrypted\n    action: 'execute_command',\n    command: 'ls -l /'\n  });\n\n  try {\n    const result = await new AsyncFlow(lsCommandNode).runAsync({});\n    console.log('Command executed successfully:');\n    console.log('  Stdout:\\n', result.stdout);\n    if (result.stderr) console.warn('  Stderr:\\n', result.stderr);\n    console.log('  Exit Code:', result.exitCode);\n  } catch (error) {\n    console.error('Command execution failed:', error.message);\n  }\n\n  console.log('\\n--- RemoteExecutionNode Example Finished ---');\n})();\n```\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { RemoteExecutionNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\n\n(async () => {\n  console.log('--- Running RemoteExecutionNode Example ---');\n\n  // --- IMPORTANT: Configuration ---\n  // Replace with your actual remote server details.\n  // For security, prefer using SSH keys over passwords.\n  const REMOTE_HOST = 'your_remote_host_or_ip'; // e.g., '192.168.1.100', 'my-server.example.com'\n  const REMOTE_USERNAME = 'your_username'; // e.g., 'ubuntu', 'ec2-user'\n  // const REMOTE_PASSWORD = process.env.REMOTE_SSH_PASSWORD; // Set as environment variable (use with caution!)\n  const REMOTE_PRIVATE_KEY_PATH = path.join(os.homedir(), '.ssh', 'id_rsa'); // Path to your private key\n  // const REMOTE_PASSPHRASE = process.env.REMOTE_SSH_PASSPHRASE; // Set as environment variable if key is encrypted\n\n  console.log(`\\n[Setup] This example requires a remote SSH server and proper authentication configured.`);\n  console.log(\"[Setup] Ensure 'ssh2' library is installed (`npm install ssh2` or `bun add ssh2`).\");\n\n  // --- Example: Execute a simple command ---\n  console.log('\\n--- Executing \"ls -l /\" on remote host ---');\n  const lsCommandNode = new RemoteExecutionNode();\n  lsCommandNode.setParams({\n    host: REMOTE_HOST,\n    username: REMOTE_USERNAME,\n    // password: REMOTE_PASSWORD, // Uncomment if using password auth\n    privateKey: REMOTE_PRIVATE_KEY_PATH, // Uncomment if using private key auth\n    // passphrase: REMOTE_PASSPHRASE, // Uncomment if private key is encrypted\n    action: 'execute_command',\n    command: 'ls -l /'\n  });\n\n  try {\n    const result = await new AsyncFlow(lsCommandNode).runAsync({});\n    console.log('Command executed successfully:');\n    console.log('  Stdout:\\n', result.stdout);\n    if (result.stderr) console.warn('  Stderr:\\n', result.stderr);\n    console.log('  Exit Code:', result.exitCode);\n  } catch (error) {\n    console.error('Command execution failed:', error.message);\n  }\n\n  console.log('\\n--- RemoteExecutionNode Example Finished ---');\n})();",
    "raw": "## RemoteExecutionNode\n\nThe `RemoteExecutionNode` executes commands on remote machines via SSH.\n\n### Parameters\n\n*   `host`: The hostname or IP address of the remote machine.\n*   `port`: Optional. The SSH port. Defaults to 22.\n*   `username`: The username for SSH authentication.\n*   `password`: Optional. The password for SSH authentication (use with caution, prefer privateKey).\n*   `privateKey`: Optional. The content of the private SSH key or its absolute path.\n*   `passphrase`: Optional. The passphrase for an encrypted private key.\n*   `action`: The action to perform. Currently only 'execute_command' is supported.\n*   `command`: The command string to execute on the remote machine.\n*   `timeout`: Optional. Timeout in milliseconds for the command execution. Defaults to 30000 (30 seconds).\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { RemoteExecutionNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\n\n(async () => {\n  console.log('--- Running RemoteExecutionNode Example ---');\n\n  // --- IMPORTANT: Configuration ---\n  // Replace with your actual remote server details.\n  // For security, prefer using SSH keys over passwords.\n  const REMOTE_HOST = 'your_remote_host_or_ip'; // e.g., '192.168.1.100', 'my-server.example.com'\n  const REMOTE_USERNAME = 'your_username'; // e.g., 'ubuntu', 'ec2-user'\n  // const REMOTE_PASSWORD = process.env.REMOTE_SSH_PASSWORD; // Set as environment variable (use with caution!)\n  const REMOTE_PRIVATE_KEY_PATH = path.join(os.homedir(), '.ssh', 'id_rsa'); // Path to your private key\n  // const REMOTE_PASSPHRASE = process.env.REMOTE_SSH_PASSPHRASE; // Set as environment variable if key is encrypted\n\n  console.log(`\\n[Setup] This example requires a remote SSH server and proper authentication configured.`);\n  console.log(\"[Setup] Ensure 'ssh2' library is installed (`npm install ssh2` or `bun add ssh2`).\");\n\n  // --- Example: Execute a simple command ---\n  console.log('\\n--- Executing \"ls -l /\" on remote host ---');\n  const lsCommandNode = new RemoteExecutionNode();\n  lsCommandNode.setParams({\n    host: REMOTE_HOST,\n    username: REMOTE_USERNAME,\n    // password: REMOTE_PASSWORD, // Uncomment if using password auth\n    privateKey: REMOTE_PRIVATE_KEY_PATH, // Uncomment if using private key auth\n    // passphrase: REMOTE_PASSPHRASE, // Uncomment if private key is encrypted\n    action: 'execute_command',\n    command: 'ls -l /'\n  });\n\n  try {\n    const result = await new AsyncFlow(lsCommandNode).runAsync({});\n    console.log('Command executed successfully:');\n    console.log('  Stdout:\\n', result.stdout);\n    if (result.stderr) console.warn('  Stderr:\\n', result.stderr);\n    console.log('  Exit Code:', result.exitCode);\n  } catch (error) {\n    console.error('Command execution failed:', error.message);\n  }\n\n  console.log('\\n--- RemoteExecutionNode Example Finished ---');\n})();\n```\n```"
  },
  "scheduler": {
    "title": "SchedulerNode",
    "description": "",
    "parameters": "*   `action`: The action to perform: 'start' a new schedule or 'stop' an existing one.\n*   `schedule`: Required for 'start'. A cron string (e.g., '0 3 * * *') or a number in milliseconds for a one-time delay.\n*   `flow`: Required for 'start'. The name of the qflow AsyncFlow instance to trigger.\n*   `flowParams`: Optional. Parameters (shared object) to pass to the triggered flow's runAsync.\n*   `id`: Optional. A unique ID for the scheduled task (required for 'stop' action). If not provided for 'start', a random one will be generated.",
    "returns": "Returns scheduler handle or status for scheduled jobs.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { SchedulerNode, SystemNotificationNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Define a flow to be scheduled ---\nclass ScheduledTaskFlow extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    const message = shared.message || 'Hello from a scheduled task!';\n    console.log(`\n[ScheduledTaskFlow] Executing: ${message} (Timestamp: ${new Date().toLocaleTimeString()})`);\n\n    // Example: Send a system notification\n    const notifyNode = new SystemNotificationNode();\n    notifyNode.setParams({\n      message: `Scheduled task executed: ${message}`,\n      title: 'QFlow Scheduler Alert'\n    });\n    await new AsyncFlow(notifyNode).runAsync({});\n\n    return { status: 'executed', message: message, timestamp: new Date().toISOString() };\n  }\n}\n\nconst myScheduledFlow = new AsyncFlow(new ScheduledTaskFlow());\n\n// --- Main Flow: Schedule and manage tasks ---\n(async () => {\n  console.log('--- Running SchedulerNode Example ---');\n  console.log(\"[Setup] Please ensure you have the 'node-cron' library installed (`npm install node-cron` or `bun add node-cron`).\");\n\n  // --- Example 1: Schedule a recurring task (every 10 seconds) ---\n  console.log('\\n--- Scheduling a recurring task (every 10 seconds) ---');\n  const recurringTaskId = 'my-recurring-task';\n  const scheduleRecurringNode = new SchedulerNode();\n  scheduleRecurringNode.setParams({\n    action: 'start',\n    id: recurringTaskId,\n    schedule: '*/10 * * * * *', // Every 10 seconds\n    flow: myScheduledFlow,\n    flowParams: { message: 'This is a recurring message!' }\n  });\n\n  try {\n    const recurringResult = await new AsyncFlow(scheduleRecurringNode).runAsync({});\n    console.log('Recurring task scheduled:', recurringResult);\n  } catch (error) {\n    console.error('Failed to schedule recurring task:', error.message);\n  }\n\n  // --- Example 2: Schedule a one-time task (in 5 seconds) ---\n  console.log('\\n--- Scheduling a one-time task (in 5 seconds) ---');\n  const oneTimeTaskId = 'my-one-time-task';\n  const scheduleOneTimeNode = new SchedulerNode();\n  scheduleOneTimeNode.setParams({\n    action: 'start',\n    id: oneTimeTaskId,\n    schedule: 5000, // 5000 milliseconds = 5 seconds\n    flow: myScheduledFlow,\n    flowParams: { message: 'This is a one-time message!' }\n  });\n\n  try {\n    const oneTimeResult = await new AsyncFlow(scheduleOneTimeNode).runAsync({});\n    console.log('One-time task scheduled:', oneTimeResult);\n  } catch (error) {\n    console.error('Failed to schedule one-time task:', error.message);\n  }\n\n  // --- Wait for a bit to see tasks execute ---\n  console.log('\\n--- Waiting for 25 seconds to observe scheduled tasks ---');\n  await new Promise(resolve => setTimeout(resolve, 25000));\n\n  // --- Example 3: Stop the recurring task ---\n  console.log('\\n--- Stopping the recurring task ---');\n  const stopRecurringNode = new SchedulerNode();\n  stopRecurringNode.setParams({\n    action: 'stop',\n    id: recurringTaskId\n  });\n\n  try {\n    const stopResult = await new AsyncFlow(stopRecurringNode).runAsync({});\n    console.log('Recurring task stop result:', stopResult);\n  } catch (error) {\n    console.error('Failed to stop recurring task:', error.message);\n  }\n\n  console.log('\\n--- SchedulerNode Example Finished ---');\n  console.log('Note: If the process exits, in-memory scheduled tasks will be lost.');\n})();\n```",
    "example": "import { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { SchedulerNode, SystemNotificationNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Define a flow to be scheduled ---\nclass ScheduledTaskFlow extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    const message = shared.message || 'Hello from a scheduled task!';\n    console.log(`\n[ScheduledTaskFlow] Executing: ${message} (Timestamp: ${new Date().toLocaleTimeString()})`);\n\n    // Example: Send a system notification\n    const notifyNode = new SystemNotificationNode();\n    notifyNode.setParams({\n      message: `Scheduled task executed: ${message}`,\n      title: 'QFlow Scheduler Alert'\n    });\n    await new AsyncFlow(notifyNode).runAsync({});\n\n    return { status: 'executed', message: message, timestamp: new Date().toISOString() };\n  }\n}\n\nconst myScheduledFlow = new AsyncFlow(new ScheduledTaskFlow());\n\n// --- Main Flow: Schedule and manage tasks ---\n(async () => {\n  console.log('--- Running SchedulerNode Example ---');\n  console.log(\"[Setup] Please ensure you have the 'node-cron' library installed (`npm install node-cron` or `bun add node-cron`).\");\n\n  // --- Example 1: Schedule a recurring task (every 10 seconds) ---\n  console.log('\\n--- Scheduling a recurring task (every 10 seconds) ---');\n  const recurringTaskId = 'my-recurring-task';\n  const scheduleRecurringNode = new SchedulerNode();\n  scheduleRecurringNode.setParams({\n    action: 'start',\n    id: recurringTaskId,\n    schedule: '*/10 * * * * *', // Every 10 seconds\n    flow: myScheduledFlow,\n    flowParams: { message: 'This is a recurring message!' }\n  });\n\n  try {\n    const recurringResult = await new AsyncFlow(scheduleRecurringNode).runAsync({});\n    console.log('Recurring task scheduled:', recurringResult);\n  } catch (error) {\n    console.error('Failed to schedule recurring task:', error.message);\n  }\n\n  // --- Example 2: Schedule a one-time task (in 5 seconds) ---\n  console.log('\\n--- Scheduling a one-time task (in 5 seconds) ---');\n  const oneTimeTaskId = 'my-one-time-task';\n  const scheduleOneTimeNode = new SchedulerNode();\n  scheduleOneTimeNode.setParams({\n    action: 'start',\n    id: oneTimeTaskId,\n    schedule: 5000, // 5000 milliseconds = 5 seconds\n    flow: myScheduledFlow,\n    flowParams: { message: 'This is a one-time message!' }\n  });\n\n  try {\n    const oneTimeResult = await new AsyncFlow(scheduleOneTimeNode).runAsync({});\n    console.log('One-time task scheduled:', oneTimeResult);\n  } catch (error) {\n    console.error('Failed to schedule one-time task:', error.message);\n  }\n\n  // --- Wait for a bit to see tasks execute ---\n  console.log('\\n--- Waiting for 25 seconds to observe scheduled tasks ---');\n  await new Promise(resolve => setTimeout(resolve, 25000));\n\n  // --- Example 3: Stop the recurring task ---\n  console.log('\\n--- Stopping the recurring task ---');\n  const stopRecurringNode = new SchedulerNode();\n  stopRecurringNode.setParams({\n    action: 'stop',\n    id: recurringTaskId\n  });\n\n  try {\n    const stopResult = await new AsyncFlow(stopRecurringNode).runAsync({});\n    console.log('Recurring task stop result:', stopResult);\n  } catch (error) {\n    console.error('Failed to stop recurring task:', error.message);\n  }\n\n  console.log('\\n--- SchedulerNode Example Finished ---');\n  console.log('Note: If the process exits, in-memory scheduled tasks will be lost.');\n})();",
    "raw": "## SchedulerNode\n\nThe `SchedulerNode` schedules qflow flows for future or recurring execution using cron syntax or a delay.\n\n### Parameters\n\n*   `action`: The action to perform: 'start' a new schedule or 'stop' an existing one.\n*   `schedule`: Required for 'start'. A cron string (e.g., '0 3 * * *') or a number in milliseconds for a one-time delay.\n*   `flow`: Required for 'start'. The name of the qflow AsyncFlow instance to trigger.\n*   `flowParams`: Optional. Parameters (shared object) to pass to the triggered flow's runAsync.\n*   `id`: Optional. A unique ID for the scheduled task (required for 'stop' action). If not provided for 'start', a random one will be generated.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { SchedulerNode, SystemNotificationNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Define a flow to be scheduled ---\nclass ScheduledTaskFlow extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    const message = shared.message || 'Hello from a scheduled task!';\n    console.log(`\n[ScheduledTaskFlow] Executing: ${message} (Timestamp: ${new Date().toLocaleTimeString()})`);\n\n    // Example: Send a system notification\n    const notifyNode = new SystemNotificationNode();\n    notifyNode.setParams({\n      message: `Scheduled task executed: ${message}`,\n      title: 'QFlow Scheduler Alert'\n    });\n    await new AsyncFlow(notifyNode).runAsync({});\n\n    return { status: 'executed', message: message, timestamp: new Date().toISOString() };\n  }\n}\n\nconst myScheduledFlow = new AsyncFlow(new ScheduledTaskFlow());\n\n// --- Main Flow: Schedule and manage tasks ---\n(async () => {\n  console.log('--- Running SchedulerNode Example ---');\n  console.log(\"[Setup] Please ensure you have the 'node-cron' library installed (`npm install node-cron` or `bun add node-cron`).\");\n\n  // --- Example 1: Schedule a recurring task (every 10 seconds) ---\n  console.log('\\n--- Scheduling a recurring task (every 10 seconds) ---');\n  const recurringTaskId = 'my-recurring-task';\n  const scheduleRecurringNode = new SchedulerNode();\n  scheduleRecurringNode.setParams({\n    action: 'start',\n    id: recurringTaskId,\n    schedule: '*/10 * * * * *', // Every 10 seconds\n    flow: myScheduledFlow,\n    flowParams: { message: 'This is a recurring message!' }\n  });\n\n  try {\n    const recurringResult = await new AsyncFlow(scheduleRecurringNode).runAsync({});\n    console.log('Recurring task scheduled:', recurringResult);\n  } catch (error) {\n    console.error('Failed to schedule recurring task:', error.message);\n  }\n\n  // --- Example 2: Schedule a one-time task (in 5 seconds) ---\n  console.log('\\n--- Scheduling a one-time task (in 5 seconds) ---');\n  const oneTimeTaskId = 'my-one-time-task';\n  const scheduleOneTimeNode = new SchedulerNode();\n  scheduleOneTimeNode.setParams({\n    action: 'start',\n    id: oneTimeTaskId,\n    schedule: 5000, // 5000 milliseconds = 5 seconds\n    flow: myScheduledFlow,\n    flowParams: { message: 'This is a one-time message!' }\n  });\n\n  try {\n    const oneTimeResult = await new AsyncFlow(scheduleOneTimeNode).runAsync({});\n    console.log('One-time task scheduled:', oneTimeResult);\n  } catch (error) {\n    console.error('Failed to schedule one-time task:', error.message);\n  }\n\n  // --- Wait for a bit to see tasks execute ---\n  console.log('\\n--- Waiting for 25 seconds to observe scheduled tasks ---');\n  await new Promise(resolve => setTimeout(resolve, 25000));\n\n  // --- Example 3: Stop the recurring task ---\n  console.log('\\n--- Stopping the recurring task ---');\n  const stopRecurringNode = new SchedulerNode();\n  stopRecurringNode.setParams({\n    action: 'stop',\n    id: recurringTaskId\n  });\n\n  try {\n    const stopResult = await new AsyncFlow(stopRecurringNode).runAsync({});\n    console.log('Recurring task stop result:', stopResult);\n  } catch (error) {\n    console.error('Failed to stop recurring task:', error.message);\n  }\n\n  console.log('\\n--- SchedulerNode Example Finished ---');\n  console.log('Note: If the process exits, in-memory scheduled tasks will be lost.');\n})();\n```"
  },
  "semantic_memory": {
    "title": "SemanticMemoryNode",
    "description": "",
    "parameters": "*   `action`: The action to perform: 'store' or 'retrieve'.\n*   `content`: Required for 'store' action. The text content of the memory to store.\n*   `query`: Required for 'retrieve' action. The text query for semantic search.\n*   `id`: Optional for 'store' action. A unique identifier for the memory. If not provided, one will be generated.\n*   `metadata`: Optional for 'store' action. Key-value pairs to store alongside the memory.\n*   `memoryPath`: Optional. The directory path where memories and the index are stored. Defaults to './semantic_memories'.\n*   `embeddingModel`: Optional. The Ollama embedding model name to use (e.g., 'nomic-embed-text'). Defaults to 'nomic-embed-text'.\n*   `embeddingBaseUrl`: Optional. The base URL of the Ollama API for embeddings. Defaults to 'http://localhost:11434'.\n*   `topK`: Optional for 'retrieve' action. The number of top similar results to retrieve. Defaults to 5.",
    "returns": "Returns execution result. Writes shared.semanticMemoryResult.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { SemanticMemoryNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running SemanticMemoryNode Example ---');\n\n  // --- Example 1: Store a semantic memory ---\n  console.log('\\n--- Storing a Semantic Memory ---');\n  const storeSemanticMemoryNode = new SemanticMemoryNode();\n  storeSemanticMemoryNode.setParams({\n    action: 'store',\n    content: 'The capital of France is Paris. It is known for the Eiffel Tower and its romantic atmosphere.',\n    id: 'france_capital_sem',\n    metadata: { source: 'wikipedia', topic: 'geography' }\n  });\n\n  const storeSemanticFlow = new AsyncFlow(storeSemanticMemoryNode);\n  try {\n    const result = await storeSemanticFlow.runAsync({});\n    console.log('Store Semantic Memory Result:', result);\n  } catch (error) {\n    console.error('Store Semantic Memory Flow Failed:', error);\n  }\n\n  // --- Example 2: Retrieve a semantic memory ---\n  console.log('\\n--- Retrieving a Semantic Memory ---');\n  const retrieveSemanticMemoryNode = new SemanticMemoryNode();\n  retrieveSemanticMemoryNode.setParams({\n    action: 'retrieve',\n    query: 'city of love',\n    topK: 1\n  });\n\n  const retrieveSemanticFlow = new AsyncFlow(retrieveSemanticMemoryNode);\n  try {\n    const result = await retrieveSemanticFlow.runAsync({});\n    console.log('Retrieve Semantic Memory Result:', result);\n  } catch (error) {\n    console.error('Retrieve Semantic Memory Flow Failed:', error);\n  }\n\n  console.log('\\n--- SemanticMemoryNode Example Finished ---');\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { SemanticMemoryNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running SemanticMemoryNode Example ---');\n\n  // --- Example 1: Store a semantic memory ---\n  console.log('\\n--- Storing a Semantic Memory ---');\n  const storeSemanticMemoryNode = new SemanticMemoryNode();\n  storeSemanticMemoryNode.setParams({\n    action: 'store',\n    content: 'The capital of France is Paris. It is known for the Eiffel Tower and its romantic atmosphere.',\n    id: 'france_capital_sem',\n    metadata: { source: 'wikipedia', topic: 'geography' }\n  });\n\n  const storeSemanticFlow = new AsyncFlow(storeSemanticMemoryNode);\n  try {\n    const result = await storeSemanticFlow.runAsync({});\n    console.log('Store Semantic Memory Result:', result);\n  } catch (error) {\n    console.error('Store Semantic Memory Flow Failed:', error);\n  }\n\n  // --- Example 2: Retrieve a semantic memory ---\n  console.log('\\n--- Retrieving a Semantic Memory ---');\n  const retrieveSemanticMemoryNode = new SemanticMemoryNode();\n  retrieveSemanticMemoryNode.setParams({\n    action: 'retrieve',\n    query: 'city of love',\n    topK: 1\n  });\n\n  const retrieveSemanticFlow = new AsyncFlow(retrieveSemanticMemoryNode);\n  try {\n    const result = await retrieveSemanticFlow.runAsync({});\n    console.log('Retrieve Semantic Memory Result:', result);\n  } catch (error) {\n    console.error('Retrieve Semantic Memory Flow Failed:', error);\n  }\n\n  console.log('\\n--- SemanticMemoryNode Example Finished ---');\n})();",
    "raw": "## SemanticMemoryNode\n\nThe `SemanticMemoryNode` stores and retrieves text memories via semantic search (requires Ollama).\n\n### Parameters\n\n*   `action`: The action to perform: 'store' or 'retrieve'.\n*   `content`: Required for 'store' action. The text content of the memory to store.\n*   `query`: Required for 'retrieve' action. The text query for semantic search.\n*   `id`: Optional for 'store' action. A unique identifier for the memory. If not provided, one will be generated.\n*   `metadata`: Optional for 'store' action. Key-value pairs to store alongside the memory.\n*   `memoryPath`: Optional. The directory path where memories and the index are stored. Defaults to './semantic_memories'.\n*   `embeddingModel`: Optional. The Ollama embedding model name to use (e.g., 'nomic-embed-text'). Defaults to 'nomic-embed-text'.\n*   `embeddingBaseUrl`: Optional. The base URL of the Ollama API for embeddings. Defaults to 'http://localhost:11434'.\n*   `topK`: Optional for 'retrieve' action. The number of top similar results to retrieve. Defaults to 5.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { SemanticMemoryNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running SemanticMemoryNode Example ---');\n\n  // --- Example 1: Store a semantic memory ---\n  console.log('\\n--- Storing a Semantic Memory ---');\n  const storeSemanticMemoryNode = new SemanticMemoryNode();\n  storeSemanticMemoryNode.setParams({\n    action: 'store',\n    content: 'The capital of France is Paris. It is known for the Eiffel Tower and its romantic atmosphere.',\n    id: 'france_capital_sem',\n    metadata: { source: 'wikipedia', topic: 'geography' }\n  });\n\n  const storeSemanticFlow = new AsyncFlow(storeSemanticMemoryNode);\n  try {\n    const result = await storeSemanticFlow.runAsync({});\n    console.log('Store Semantic Memory Result:', result);\n  } catch (error) {\n    console.error('Store Semantic Memory Flow Failed:', error);\n  }\n\n  // --- Example 2: Retrieve a semantic memory ---\n  console.log('\\n--- Retrieving a Semantic Memory ---');\n  const retrieveSemanticMemoryNode = new SemanticMemoryNode();\n  retrieveSemanticMemoryNode.setParams({\n    action: 'retrieve',\n    query: 'city of love',\n    topK: 1\n  });\n\n  const retrieveSemanticFlow = new AsyncFlow(retrieveSemanticMemoryNode);\n  try {\n    const result = await retrieveSemanticFlow.runAsync({});\n    console.log('Retrieve Semantic Memory Result:', result);\n  } catch (error) {\n    console.error('Retrieve Semantic Memory Flow Failed:', error);\n  }\n\n  console.log('\\n--- SemanticMemoryNode Example Finished ---');\n})();\n```"
  },
  "shell_command": {
    "title": "ShellCommandNode",
    "description": "/**",
    "parameters": "*   `command`: The full shell command to execute (e.g., 'ls -l', 'npm install cheerio').",
    "returns": "{Promise<object>} A promise that resolves to an object containing the stdout and stderr of the command.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ShellCommandNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running Shell Command Test Workflow ---');\n\n  // 1. Simple echo command\n  const simpleEcho = new ShellCommandNode();\n  simpleEcho.setParams({ command: \"echo 'Hello from qflow!'\" });\n  simpleEcho.postAsync = async (shared, prepRes, execRes) => {\n    console.log(execRes.stdout.trim());\n    return 'default';\n  };\n\n  const shellFlow = new AsyncFlow(simpleEcho);\n\n  try {\n    await shellFlow.runAsync({});\n    console.log('\\n--- Shell Command Test Workflow Finished ---');\n  } catch (error) {\n    console.error('\\n--- Shell Command Test Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ShellCommandNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running Shell Command Test Workflow ---');\n\n  // 1. Simple echo command\n  const simpleEcho = new ShellCommandNode();\n  simpleEcho.setParams({ command: \"echo 'Hello from qflow!'\" });\n  simpleEcho.postAsync = async (shared, prepRes, execRes) => {\n    console.log(execRes.stdout.trim());\n    return 'default';\n  };\n\n  const shellFlow = new AsyncFlow(simpleEcho);\n\n  try {\n    await shellFlow.runAsync({});\n    console.log('\\n--- Shell Command Test Workflow Finished ---');\n  } catch (error) {\n    console.error('\\n--- Shell Command Test Workflow Failed ---', error);\n  }\n})();",
    "raw": "## ShellCommandNode\n\nThe `ShellCommandNode` executes shell commands.\n\n### Parameters\n\n*   `command`: The full shell command to execute (e.g., 'ls -l', 'npm install cheerio').\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ShellCommandNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running Shell Command Test Workflow ---');\n\n  // 1. Simple echo command\n  const simpleEcho = new ShellCommandNode();\n  simpleEcho.setParams({ command: \"echo 'Hello from qflow!'\" });\n  simpleEcho.postAsync = async (shared, prepRes, execRes) => {\n    console.log(execRes.stdout.trim());\n    return 'default';\n  };\n\n  const shellFlow = new AsyncFlow(simpleEcho);\n\n  try {\n    await shellFlow.runAsync({});\n    console.log('\\n--- Shell Command Test Workflow Finished ---');\n  } catch (error) {\n    console.error('\\n--- Shell Command Test Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "speech_synthesis": {
    "title": "SpeechSynthesisNode",
    "description": "",
    "parameters": "*   `text`: The text to convert to speech.\n*   `provider`: Optional. The speech synthesis provider to use. Defaults to OS-specific. 'google' requires GOOGLE_TTS_API_KEY.\n*   `voice`: Optional. The specific voice to use (e.g., 'Alex' for macOS, 'en-us' for espeak, 'en-US-Wavenet-D' for Google).\n*   `outputFilePath`: Optional. If provided, saves the audio to this file path instead of playing it directly.",
    "returns": "Returns playback or output file result depending on configuration.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { SpeechSynthesisNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running SpeechSynthesisNode Example ---');\n\n  // --- IMPORTANT: Prerequisites ---\n  console.log(\"[Setup] For macOS: 'say' command is built-in.\");\n  console.log(\"[Setup] For Linux: Install 'espeak' (`sudo apt install espeak` or `sudo pacman -S espeak`) and 'alsa-utils' (`sudo apt install alsa-utils` or `sudo pacman -S alsa-utils`).\");\n\n  // --- Example: Default OS Provider (play directly) ---\n  console.log('\\n--- Synthesizing speech using default OS provider ---');\n  const defaultTtsNode = new SpeechSynthesisNode();\n  defaultTtsNode.setParams({\n    text: 'Hello, this is a test from QFlow using your default system voice.'\n  });\n\n  try {\n    const result = await new AsyncFlow(defaultTtsNode).runAsync({});\n    console.log('Default TTS successful:', result);\n  } catch (error) {\n    console.error('Default TTS failed:', error.message);\n  }\n\n  console.log('\\n--- SpeechSynthesisNode Example Finished ---');\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { SpeechSynthesisNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running SpeechSynthesisNode Example ---');\n\n  // --- IMPORTANT: Prerequisites ---\n  console.log(\"[Setup] For macOS: 'say' command is built-in.\");\n  console.log(\"[Setup] For Linux: Install 'espeak' (`sudo apt install espeak` or `sudo pacman -S espeak`) and 'alsa-utils' (`sudo apt install alsa-utils` or `sudo pacman -S alsa-utils`).\");\n\n  // --- Example: Default OS Provider (play directly) ---\n  console.log('\\n--- Synthesizing speech using default OS provider ---');\n  const defaultTtsNode = new SpeechSynthesisNode();\n  defaultTtsNode.setParams({\n    text: 'Hello, this is a test from QFlow using your default system voice.'\n  });\n\n  try {\n    const result = await new AsyncFlow(defaultTtsNode).runAsync({});\n    console.log('Default TTS successful:', result);\n  } catch (error) {\n    console.error('Default TTS failed:', error.message);\n  }\n\n  console.log('\\n--- SpeechSynthesisNode Example Finished ---');\n})();",
    "raw": "## SpeechSynthesisNode\n\nThe `SpeechSynthesisNode` converts text to spoken audio using OS capabilities or cloud APIs.\n\n### Parameters\n\n*   `text`: The text to convert to speech.\n*   `provider`: Optional. The speech synthesis provider to use. Defaults to OS-specific. 'google' requires GOOGLE_TTS_API_KEY.\n*   `voice`: Optional. The specific voice to use (e.g., 'Alex' for macOS, 'en-us' for espeak, 'en-US-Wavenet-D' for Google).\n*   `outputFilePath`: Optional. If provided, saves the audio to this file path instead of playing it directly.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { SpeechSynthesisNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running SpeechSynthesisNode Example ---');\n\n  // --- IMPORTANT: Prerequisites ---\n  console.log(\"[Setup] For macOS: 'say' command is built-in.\");\n  console.log(\"[Setup] For Linux: Install 'espeak' (`sudo apt install espeak` or `sudo pacman -S espeak`) and 'alsa-utils' (`sudo apt install alsa-utils` or `sudo pacman -S alsa-utils`).\");\n\n  // --- Example: Default OS Provider (play directly) ---\n  console.log('\\n--- Synthesizing speech using default OS provider ---');\n  const defaultTtsNode = new SpeechSynthesisNode();\n  defaultTtsNode.setParams({\n    text: 'Hello, this is a test from QFlow using your default system voice.'\n  });\n\n  try {\n    const result = await new AsyncFlow(defaultTtsNode).runAsync({});\n    console.log('Default TTS successful:', result);\n  } catch (error) {\n    console.error('Default TTS failed:', error.message);\n  }\n\n  console.log('\\n--- SpeechSynthesisNode Example Finished ---');\n})();\n```"
  },
  "spreadsheet": {
    "title": "SpreadsheetNode",
    "description": "",
    "parameters": "*   `action`: The action to perform.\n*   `filePath`: The absolute path to the spreadsheet file.\n*   `sheetName`: Required for .xlsx/.xls files and sheet-specific actions. The name of the sheet.\n*   `data`: Required for 'write', 'write_range', 'append_rows'. The data to write (array of arrays or array of objects).\n*   `headerRow`: Optional. True if the first row is a header. Defaults to true.\n*   `range`: Required for 'read_range', 'write_range', 'format_cells'. A1 notation (e.g., 'Sheet1!A1:C10').\n*   `startRow`: Required for 'delete_rows', 'insert_rows'. The 1-indexed starting row.\n*   `numRows`: Required for 'delete_rows', 'insert_rows'. The number of rows to delete/insert.\n*   `newSheetName`: Required for 'add_sheet', 'rename_sheet'. The name of the new sheet.\n*   `formats`: Required for 'format_cells'. Formatting options (conceptual, basic XLSX.js has limited styling).",
    "returns": "Returns read data or write confirmation depending on action.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { SpreadsheetNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {\n  console.log('--- Running SpreadsheetNode Example ---');\n\n  const tempDir = os.tmpdir();\n  const csvFilePath = path.join(tempDir, 'sample_data.csv');\n\n  // --- IMPORTANT: Prerequisites ---\n  console.log(\"[Setup] Please ensure you have the 'xlsx' library installed (`npm install xlsx` or `bun add xlsx`).\");\n\n  // --- Create initial dummy CSV file ---\n  const dummyCsvContent = \"Name,Age,City\\nAlice,30,New York\\nBob,24,London\\nCharlie,35,Paris\";\n  try {\n    await fs.writeFile(csvFilePath, dummyCsvContent);\n    console.log(`[Setup] Created dummy CSV file: ${csvFilePath}`);\n  } catch (error) {\n    console.error('[Setup] Failed to create dummy CSV file:', error);\n    return;\n  }\n\n  // --- Example: Read data from CSV ---\n  console.log('\\n--- Reading data from CSV ---');\n  const readCsvNode = new SpreadsheetNode();\n  readCsvNode.setParams({\n    action: 'read',\n    filePath: csvFilePath,\n    headerRow: true\n  });\n\n  try {\n    const result = await new AsyncFlow(readCsvNode).runAsync({});\n    console.log('Read CSV Result:', result);\n  } catch (error) {\n    console.error('Read CSV Failed:', error.message);\n  }\n\n  console.log('\\n--- SpreadsheetNode Example Finished ---');\n\n  // --- Cleanup ---\n  try {\n    console.log('\\n[Cleanup] Removing generated files...');\n    await fs.unlink(csvFilePath).catch(() => {});\n    console.log('[Cleanup] Cleanup complete.');\n  } catch (e) {\n    console.warn('[Cleanup] Failed to remove some temporary files:', e.message);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { SpreadsheetNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {\n  console.log('--- Running SpreadsheetNode Example ---');\n\n  const tempDir = os.tmpdir();\n  const csvFilePath = path.join(tempDir, 'sample_data.csv');\n\n  // --- IMPORTANT: Prerequisites ---\n  console.log(\"[Setup] Please ensure you have the 'xlsx' library installed (`npm install xlsx` or `bun add xlsx`).\");\n\n  // --- Create initial dummy CSV file ---\n  const dummyCsvContent = \"Name,Age,City\\nAlice,30,New York\\nBob,24,London\\nCharlie,35,Paris\";\n  try {\n    await fs.writeFile(csvFilePath, dummyCsvContent);\n    console.log(`[Setup] Created dummy CSV file: ${csvFilePath}`);\n  } catch (error) {\n    console.error('[Setup] Failed to create dummy CSV file:', error);\n    return;\n  }\n\n  // --- Example: Read data from CSV ---\n  console.log('\\n--- Reading data from CSV ---');\n  const readCsvNode = new SpreadsheetNode();\n  readCsvNode.setParams({\n    action: 'read',\n    filePath: csvFilePath,\n    headerRow: true\n  });\n\n  try {\n    const result = await new AsyncFlow(readCsvNode).runAsync({});\n    console.log('Read CSV Result:', result);\n  } catch (error) {\n    console.error('Read CSV Failed:', error.message);\n  }\n\n  console.log('\\n--- SpreadsheetNode Example Finished ---');\n\n  // --- Cleanup ---\n  try {\n    console.log('\\n[Cleanup] Removing generated files...');\n    await fs.unlink(csvFilePath).catch(() => {});\n    console.log('[Cleanup] Cleanup complete.');\n  } catch (e) {\n    console.warn('[Cleanup] Failed to remove some temporary files:', e.message);\n  }\n})();",
    "raw": "## SpreadsheetNode\n\nThe `SpreadsheetNode` reads from and writes to spreadsheet files (.xlsx, .xls, .csv) with advanced manipulation.\n\n### Parameters\n\n*   `action`: The action to perform.\n*   `filePath`: The absolute path to the spreadsheet file.\n*   `sheetName`: Required for .xlsx/.xls files and sheet-specific actions. The name of the sheet.\n*   `data`: Required for 'write', 'write_range', 'append_rows'. The data to write (array of arrays or array of objects).\n*   `headerRow`: Optional. True if the first row is a header. Defaults to true.\n*   `range`: Required for 'read_range', 'write_range', 'format_cells'. A1 notation (e.g., 'Sheet1!A1:C10').\n*   `startRow`: Required for 'delete_rows', 'insert_rows'. The 1-indexed starting row.\n*   `numRows`: Required for 'delete_rows', 'insert_rows'. The number of rows to delete/insert.\n*   `newSheetName`: Required for 'add_sheet', 'rename_sheet'. The name of the new sheet.\n*   `formats`: Required for 'format_cells'. Formatting options (conceptual, basic XLSX.js has limited styling).\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { SpreadsheetNode } from '@fractal-solutions/qflow/nodes';\nimport path from 'path';\nimport os from 'os';\nimport { promises as fs } from 'fs';\n\n(async () => {\n  console.log('--- Running SpreadsheetNode Example ---');\n\n  const tempDir = os.tmpdir();\n  const csvFilePath = path.join(tempDir, 'sample_data.csv');\n\n  // --- IMPORTANT: Prerequisites ---\n  console.log(\"[Setup] Please ensure you have the 'xlsx' library installed (`npm install xlsx` or `bun add xlsx`).\");\n\n  // --- Create initial dummy CSV file ---\n  const dummyCsvContent = \"Name,Age,City\\nAlice,30,New York\\nBob,24,London\\nCharlie,35,Paris\";\n  try {\n    await fs.writeFile(csvFilePath, dummyCsvContent);\n    console.log(`[Setup] Created dummy CSV file: ${csvFilePath}`);\n  } catch (error) {\n    console.error('[Setup] Failed to create dummy CSV file:', error);\n    return;\n  }\n\n  // --- Example: Read data from CSV ---\n  console.log('\\n--- Reading data from CSV ---');\n  const readCsvNode = new SpreadsheetNode();\n  readCsvNode.setParams({\n    action: 'read',\n    filePath: csvFilePath,\n    headerRow: true\n  });\n\n  try {\n    const result = await new AsyncFlow(readCsvNode).runAsync({});\n    console.log('Read CSV Result:', result);\n  } catch (error) {\n    console.error('Read CSV Failed:', error.message);\n  }\n\n  console.log('\\n--- SpreadsheetNode Example Finished ---');\n\n  // --- Cleanup ---\n  try {\n    console.log('\\n[Cleanup] Removing generated files...');\n    await fs.unlink(csvFilePath).catch(() => {});\n    console.log('[Cleanup] Cleanup complete.');\n  } catch (e) {\n    console.warn('[Cleanup] Failed to remove some temporary files:', e.message);\n  }\n})();\n```"
  },
  "stripe": {
    "title": "StripeNode",
    "description": "",
    "parameters": "*   `action`: The Stripe action to perform.\n*   `apiKey`: Your Stripe API key.\n*   `amount`: The amount to charge.\n*   `currency`: The currency of the charge.\n*   `source`: The source of the charge (e.g., a token or card ID).\n*   `description`: A description of the charge.",
    "returns": "Returns Stripe API response for the requested action.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { CreateChargeNode, GetBalanceNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Configuration ---\n// IMPORTANT: Replace with your actual Stripe secret key and a valid source\nconst STRIPE_API_KEY = process.env.STRIPE_API_KEY || 'YOUR_STRIPE_API_KEY_HERE';\nconst STRIPE_SOURCE = 'tok_visa'; // Use a test token\n\n// --- Test Workflow for Stripe Nodes ---\n\n(async () => {\n  if (STRIPE_API_KEY === 'YOUR_STRIPE_API_KEY_HERE') {\n    console.warn(\"WARNING: Stripe API key is not set. Please set it to run the flow.\");\n    return;\n  }\n\n  console.log('--- Running Stripe Test Workflow ---');\n\n  // 1. Create instances of the nodes\n  const createCharge = new CreateChargeNode();\n  createCharge.setParams({\n    apiKey: STRIPE_API_KEY,\n    amount: 1000, // $10.00\n    currency: 'usd',\n    source: STRIPE_SOURCE,\n    description: '[qflow] Test Charge'\n  });\n\n  const getBalance = new GetBalanceNode();\n  getBalance.setParams({ apiKey: STRIPE_API_KEY });\n\n  // 2. Define the workflow\n  createCharge.next(getBalance);\n\n  // 3. Create and run the flow\n  const stripeFlow = new AsyncFlow(createCharge);\n\n  try {\n    const result = await stripeFlow.runAsync({});\n    console.log('\\n--- Stripe Test Workflow Finished ---');\n    console.log('Final Result:', result);\n  } catch (error) {\n    console.error('\\n--- Stripe Test Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { CreateChargeNode, GetBalanceNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Configuration ---\n// IMPORTANT: Replace with your actual Stripe secret key and a valid source\nconst STRIPE_API_KEY = process.env.STRIPE_API_KEY || 'YOUR_STRIPE_API_KEY_HERE';\nconst STRIPE_SOURCE = 'tok_visa'; // Use a test token\n\n// --- Test Workflow for Stripe Nodes ---\n\n(async () => {\n  if (STRIPE_API_KEY === 'YOUR_STRIPE_API_KEY_HERE') {\n    console.warn(\"WARNING: Stripe API key is not set. Please set it to run the flow.\");\n    return;\n  }\n\n  console.log('--- Running Stripe Test Workflow ---');\n\n  // 1. Create instances of the nodes\n  const createCharge = new CreateChargeNode();\n  createCharge.setParams({\n    apiKey: STRIPE_API_KEY,\n    amount: 1000, // $10.00\n    currency: 'usd',\n    source: STRIPE_SOURCE,\n    description: '[qflow] Test Charge'\n  });\n\n  const getBalance = new GetBalanceNode();\n  getBalance.setParams({ apiKey: STRIPE_API_KEY });\n\n  // 2. Define the workflow\n  createCharge.next(getBalance);\n\n  // 3. Create and run the flow\n  const stripeFlow = new AsyncFlow(createCharge);\n\n  try {\n    const result = await stripeFlow.runAsync({});\n    console.log('\\n--- Stripe Test Workflow Finished ---');\n    console.log('Final Result:', result);\n  } catch (error) {\n    console.error('\\n--- Stripe Test Workflow Failed ---', error);\n  }\n})();",
    "raw": "## StripeNode\n\nThe `StripeNode` performs Stripe operations like creating charges and retrieving account balances.\n\n### Parameters\n\n*   `action`: The Stripe action to perform.\n*   `apiKey`: Your Stripe API key.\n*   `amount`: The amount to charge.\n*   `currency`: The currency of the charge.\n*   `source`: The source of the charge (e.g., a token or card ID).\n*   `description`: A description of the charge.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { CreateChargeNode, GetBalanceNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Configuration ---\n// IMPORTANT: Replace with your actual Stripe secret key and a valid source\nconst STRIPE_API_KEY = process.env.STRIPE_API_KEY || 'YOUR_STRIPE_API_KEY_HERE';\nconst STRIPE_SOURCE = 'tok_visa'; // Use a test token\n\n// --- Test Workflow for Stripe Nodes ---\n\n(async () => {\n  if (STRIPE_API_KEY === 'YOUR_STRIPE_API_KEY_HERE') {\n    console.warn(\"WARNING: Stripe API key is not set. Please set it to run the flow.\");\n    return;\n  }\n\n  console.log('--- Running Stripe Test Workflow ---');\n\n  // 1. Create instances of the nodes\n  const createCharge = new CreateChargeNode();\n  createCharge.setParams({\n    apiKey: STRIPE_API_KEY,\n    amount: 1000, // $10.00\n    currency: 'usd',\n    source: STRIPE_SOURCE,\n    description: '[qflow] Test Charge'\n  });\n\n  const getBalance = new GetBalanceNode();\n  getBalance.setParams({ apiKey: STRIPE_API_KEY });\n\n  // 2. Define the workflow\n  createCharge.next(getBalance);\n\n  // 3. Create and run the flow\n  const stripeFlow = new AsyncFlow(createCharge);\n\n  try {\n    const result = await stripeFlow.runAsync({});\n    console.log('\\n--- Stripe Test Workflow Finished ---');\n    console.log('Final Result:', result);\n  } catch (error) {\n    console.error('\\n--- Stripe Test Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "sub_flow": {
    "title": "SubFlowNode",
    "description": "",
    "parameters": "*   `flow`: The name of the flow to execute, as registered in the flow registry.\n*   `shared`: The shared object to pass to the sub-flow.",
    "returns": "Returns execution result. Writes shared.subFlowResult.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { SubFlowNode } from '@fractal-solutions/qflow/nodes';\n\n// Define a simple sub-flow\nclass MySubNode extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    const input = shared.input || 0;\n    const result = input * 2;\n    console.log(`  [SubFlow] Received input: ${input}, Produced result: ${result}`);\n    return result;\n  }\n}\nconst mySubFlow = new AsyncFlow(new MySubNode());\n\n(async () => {\n  console.log('--- Running SubFlowNode Example ---');\n\n  // Register the sub-flow (this would typically be done in a central registry)\n  const flowRegistry = {\n    'MySubFlow': mySubFlow\n  };\n\n  const subFlowNode = new SubFlowNode();\n  subFlowNode.setParams({\n    flow: flowRegistry['MySubFlow'], // Pass the sub-flow instance\n    shared: { input: 10 } // Pass initial data to the sub-flow\n  });\n\n  const mainFlow = new AsyncFlow(subFlowNode);\n\n  try {\n    const result = await mainFlow.runAsync({});\n    console.log('\\n--- SubFlowNode Example Finished ---');\n    console.log('Result from sub-flow execution:', result);\n  } catch (error) {\n    console.error('\\n--- SubFlowNode Example Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { SubFlowNode } from '@fractal-solutions/qflow/nodes';\n\n// Define a simple sub-flow\nclass MySubNode extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    const input = shared.input || 0;\n    const result = input * 2;\n    console.log(`  [SubFlow] Received input: ${input}, Produced result: ${result}`);\n    return result;\n  }\n}\nconst mySubFlow = new AsyncFlow(new MySubNode());\n\n(async () => {\n  console.log('--- Running SubFlowNode Example ---');\n\n  // Register the sub-flow (this would typically be done in a central registry)\n  const flowRegistry = {\n    'MySubFlow': mySubFlow\n  };\n\n  const subFlowNode = new SubFlowNode();\n  subFlowNode.setParams({\n    flow: flowRegistry['MySubFlow'], // Pass the sub-flow instance\n    shared: { input: 10 } // Pass initial data to the sub-flow\n  });\n\n  const mainFlow = new AsyncFlow(subFlowNode);\n\n  try {\n    const result = await mainFlow.runAsync({});\n    console.log('\\n--- SubFlowNode Example Finished ---');\n    console.log('Result from sub-flow execution:', result);\n  } catch (error) {\n    console.error('\\n--- SubFlowNode Example Failed ---', error);\n  }\n})();",
    "raw": "## SubFlowNode\n\nThe `SubFlowNode` executes a sub-flow.\n\n### Parameters\n\n*   `flow`: The name of the flow to execute, as registered in the flow registry.\n*   `shared`: The shared object to pass to the sub-flow.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { SubFlowNode } from '@fractal-solutions/qflow/nodes';\n\n// Define a simple sub-flow\nclass MySubNode extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    const input = shared.input || 0;\n    const result = input * 2;\n    console.log(`  [SubFlow] Received input: ${input}, Produced result: ${result}`);\n    return result;\n  }\n}\nconst mySubFlow = new AsyncFlow(new MySubNode());\n\n(async () => {\n  console.log('--- Running SubFlowNode Example ---');\n\n  // Register the sub-flow (this would typically be done in a central registry)\n  const flowRegistry = {\n    'MySubFlow': mySubFlow\n  };\n\n  const subFlowNode = new SubFlowNode();\n  subFlowNode.setParams({\n    flow: flowRegistry['MySubFlow'], // Pass the sub-flow instance\n    shared: { input: 10 } // Pass initial data to the sub-flow\n  });\n\n  const mainFlow = new AsyncFlow(subFlowNode);\n\n  try {\n    const result = await mainFlow.runAsync({});\n    console.log('\\n--- SubFlowNode Example Finished ---');\n    console.log('Result from sub-flow execution:', result);\n  } catch (error) {\n    console.error('\\n--- SubFlowNode Example Failed ---', error);\n  }\n})();\n```"
  },
  "system_notification": {
    "title": "SystemNotificationNode",
    "description": "",
    "parameters": "*   `message`: The main message content of the notification.\n*   `title`: Optional. The title of the notification. Defaults to 'QFlow Notification'.\n*   `icon`: Optional. Path to an icon file or a system icon name (Linux specific). Ignored on macOS/Windows.",
    "returns": "Returns { message, title, stdout, stderr } after the notification is sent.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { SystemNotificationNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running SystemNotificationNode Example ---');\n\n  const notifyNode = new SystemNotificationNode();\n  notifyNode.setParams({\n    message: 'Your QFlow task is complete!',\n    title: 'Task Status',\n    icon: 'dialog-information' // Linux specific icon name, ignored on macOS/Windows\n  });\n\n  const flow = new AsyncFlow(notifyNode);\n  try {\n    await flow.runAsync({});\n    console.log('Notification workflow finished.');\n  } catch (error) {\n    console.error('Notification Workflow Failed:', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { SystemNotificationNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running SystemNotificationNode Example ---');\n\n  const notifyNode = new SystemNotificationNode();\n  notifyNode.setParams({\n    message: 'Your QFlow task is complete!',\n    title: 'Task Status',\n    icon: 'dialog-information' // Linux specific icon name, ignored on macOS/Windows\n  });\n\n  const flow = new AsyncFlow(notifyNode);\n  try {\n    await flow.runAsync({});\n    console.log('Notification workflow finished.');\n  } catch (error) {\n    console.error('Notification Workflow Failed:', error);\n  }\n})();",
    "raw": "## SystemNotificationNode\n\nThe `SystemNotificationNode` displays a system-level notification across OSs.\n\n### Parameters\n\n*   `message`: The main message content of the notification.\n*   `title`: Optional. The title of the notification. Defaults to 'QFlow Notification'.\n*   `icon`: Optional. Path to an icon file or a system icon name (Linux specific). Ignored on macOS/Windows.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { SystemNotificationNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running SystemNotificationNode Example ---');\n\n  const notifyNode = new SystemNotificationNode();\n  notifyNode.setParams({\n    message: 'Your QFlow task is complete!',\n    title: 'Task Status',\n    icon: 'dialog-information' // Linux specific icon name, ignored on macOS/Windows\n  });\n\n  const flow = new AsyncFlow(notifyNode);\n  try {\n    await flow.runAsync({});\n    console.log('Notification workflow finished.');\n  } catch (error) {\n    console.error('Notification Workflow Failed:', error);\n  }\n})();\n```"
  },
  "transform": {
    "title": "TransformNode",
    "description": "",
    "parameters": "*   `input`: The data to be transformed.\n*   `transformFunction`: A JavaScript function string that takes 'data' as an argument and returns the transformed result.",
    "returns": "Returns execution result. Writes shared.transformedData.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { TransformNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running TransformNode Example ---');\n\n  // --- Example: Simple Array Transformation (Map) ---\n  console.log('\\n--- Array Map ---');\n  const arrayInput = [1, 2, 3, 4, 5];\n  const transformMapNode = new TransformNode();\n  transformMapNode.setParams({\n    input: arrayInput,\n    transformFunction: '(data) => data.map(x => x * 2)'\n  });\n\n  const transformMapFlow = new AsyncFlow(transformMapNode);\n  try {\n    const result = await transformMapFlow.runAsync({});\n    console.log('Transformed Array (Map):', result);\n  } catch (error) {\n    console.error('Array Map Flow Failed:', error);\n  }\n\n  console.log('\\n--- TransformNode Example Finished ---');\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { TransformNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running TransformNode Example ---');\n\n  // --- Example: Simple Array Transformation (Map) ---\n  console.log('\\n--- Array Map ---');\n  const arrayInput = [1, 2, 3, 4, 5];\n  const transformMapNode = new TransformNode();\n  transformMapNode.setParams({\n    input: arrayInput,\n    transformFunction: '(data) => data.map(x => x * 2)'\n  });\n\n  const transformMapFlow = new AsyncFlow(transformMapNode);\n  try {\n    const result = await transformMapFlow.runAsync({});\n    console.log('Transformed Array (Map):', result);\n  } catch (error) {\n    console.error('Array Map Flow Failed:', error);\n  }\n\n  console.log('\\n--- TransformNode Example Finished ---');\n})();",
    "raw": "## TransformNode\n\nThe `TransformNode` transforms input data using a provided JavaScript function.\n\n### Parameters\n\n*   `input`: The data to be transformed.\n*   `transformFunction`: A JavaScript function string that takes 'data' as an argument and returns the transformed result.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { TransformNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running TransformNode Example ---');\n\n  // --- Example: Simple Array Transformation (Map) ---\n  console.log('\\n--- Array Map ---');\n  const arrayInput = [1, 2, 3, 4, 5];\n  const transformMapNode = new TransformNode();\n  transformMapNode.setParams({\n    input: arrayInput,\n    transformFunction: '(data) => data.map(x => x * 2)'\n  });\n\n  const transformMapFlow = new AsyncFlow(transformMapNode);\n  try {\n    const result = await transformMapFlow.runAsync({});\n    console.log('Transformed Array (Map):', result);\n  } catch (error) {\n    console.error('Array Map Flow Failed:', error);\n  }\n\n  console.log('\\n--- TransformNode Example Finished ---');\n})();\n```"
  },
  "web_scraper": {
    "title": "WebScraperNode",
    "description": "",
    "parameters": "*   `url`: The URL to scrape.",
    "returns": "Returns the fetched HTML content as a string.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ScrapeURLNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running WebScraperNode Example ---');\n\n  const scrapeNode = new ScrapeURLNode();\n  scrapeNode.setParams({\n    url: 'https://example.com'\n  });\n\n  scrapeNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('\\n--- Scraped Content ---');\n    console.log(execRes.substring(0, 500) + '...'); // Log first 500 characters\n    console.log('------------------------\\n');\n    return 'default';\n  };\n\n  const scrapeFlow = new AsyncFlow(scrapeNode);\n\n  try {\n    await scrapeFlow.runAsync({});\n    console.log('--- WebScraperNode Example Finished ---');\n  } catch (error) {\n    console.error('--- WebScraperNode Example Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ScrapeURLNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running WebScraperNode Example ---');\n\n  const scrapeNode = new ScrapeURLNode();\n  scrapeNode.setParams({\n    url: 'https://example.com'\n  });\n\n  scrapeNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('\\n--- Scraped Content ---');\n    console.log(execRes.substring(0, 500) + '...'); // Log first 500 characters\n    console.log('------------------------\\n');\n    return 'default';\n  };\n\n  const scrapeFlow = new AsyncFlow(scrapeNode);\n\n  try {\n    await scrapeFlow.runAsync({});\n    console.log('--- WebScraperNode Example Finished ---');\n  } catch (error) {\n    console.error('--- WebScraperNode Example Failed ---', error);\n  }\n})();",
    "raw": "## WebScraperNode\n\nThe `WebScraperNode` fetches the HTML content of a given URL.\n\n### Parameters\n\n*   `url`: The URL to scrape.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { ScrapeURLNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running WebScraperNode Example ---');\n\n  const scrapeNode = new ScrapeURLNode();\n  scrapeNode.setParams({\n    url: 'https://example.com'\n  });\n\n  scrapeNode.postAsync = async (shared, prepRes, execRes) => {\n    console.log('\\n--- Scraped Content ---');\n    console.log(execRes.substring(0, 500) + '...'); // Log first 500 characters\n    console.log('------------------------\\n');\n    return 'default';\n  };\n\n  const scrapeFlow = new AsyncFlow(scrapeNode);\n\n  try {\n    await scrapeFlow.runAsync({});\n    console.log('--- WebScraperNode Example Finished ---');\n  } catch (error) {\n    console.error('--- WebScraperNode Example Failed ---', error);\n  }\n})();\n```"
  },
  "webhook": {
    "title": "WebhookNode",
    "description": "",
    "parameters": "*   `port`: Optional. The port number to listen on. Defaults to 3000.\n*   `path`: Optional. The URL path for the webhook endpoint. Defaults to '/webhook'.\n*   `flow`: The qflow `AsyncFlow` instance to trigger when a webhook is received.\n*   `waitForFlow` (boolean, optional, default: false): This parameter controls the response behavior:\n    *   If `false` (the default), the server responds immediately with a predefined status and body, and triggers the flow in a \"fire-and-forget\" manner.\n    *   If `true`, the server will await the completion of the entire flow. It will then automatically take the return value of the flow, convert it to a JSON string, and send it as the HTTP response with a 200 OK status. If the flow throws an error, it will send a generic `500 Flow execution failed` error.\n*   `sharedSecret`: Optional. A shared secret for HMAC verification of incoming webhooks.\n*   `responseStatus`: Optional. The HTTP status code to send back. **Only used when `waitForFlow` is `false`**. Defaults to 200.\n*   `responseBody`: Optional. The JSON body to send back. **Only used when `waitForFlow` is `false`**. Defaults to `{ status: 'received' }`.",
    "returns": "Returns immediately with the configured response or the flow result when waitForFlow is true.",
    "notes": "",
    "requirements": "",
    "examplesText": "#### Example 1: Fire-and-Forget\n\nThis is the default behavior. The webhook responds immediately and triggers a flow in the background.\n\n```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { WebhookNode, HttpRequestNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Define a flow to be triggered by the webhook ---\nclass WebhookTriggeredFlow extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    console.log('\\n[WebhookTriggeredFlow] Webhook received!');\n    console.log('[WebhookTriggeredFlow] Received data:', shared.webhookData);\n    // Process the webhook data here\n    return { processed: true, data: shared.webhookData };\n  }\n}\n\nconst myWebhookFlow = new AsyncFlow(new WebhookTriggeredFlow());\n\n// --- Main Flow: Set up the WebhookNode and simulate a call ---\n(async () => {\n  console.log('--- Running WebhookNode Example ---');\n\n  // 1. Set up the WebhookNode\n  const webhookNode = new WebhookNode();\n  webhookNode.setParams({\n    port: 3000,\n    path: '/my-custom-webhook',\n    flow: myWebhookFlow // Pass the flow instance to be triggered\n  });\n\n  // Start the webhook listener in the background\n  const webhookFlow = new AsyncFlow(webhookNode);\n  webhookFlow.runAsync({}); // Run without awaiting to keep it listening\n  console.log('Webhook listener started on http://localhost:3000/my-custom-webhook');\n  console.log('Waiting for 3 seconds before simulating a webhook call...');\n\n  await new Promise(resolve => setTimeout(resolve, 3000)); // Wait for server to start\n\n  // 2. Simulate an incoming webhook call using HttpRequestNode\n  console.log('\\n--- Simulating incoming webhook call ---');\n  const simulateWebhookCall = new HttpRequestNode();\n  simulateWebhookCall.setParams({\n    url: 'http://localhost:3000/my-custom-webhook',\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: { event: 'user.created', data: { id: 123, name: 'John Doe' } }\n  });\n\n  try {\n    const response = await new AsyncFlow(simulateWebhookCall).runAsync({});\n    console.log('Simulated Webhook Call Response:', response);\n  } catch (error) {\n    console.error('Simulated Webhook Call Failed:', error.message);\n  }\n\n  console.log('\\n--- WebhookNode Example Finished ---');\n  // In a real application, you would keep the process alive for the webhook to continue listening.\n  // For this example, the process will exit after the simulated call.\n})();\n```",
    "example": "import { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { WebhookNode, HttpRequestNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Define a flow to be triggered by the webhook ---\nclass WebhookTriggeredFlow extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    console.log('\\n[WebhookTriggeredFlow] Webhook received!');\n    console.log('[WebhookTriggeredFlow] Received data:', shared.webhookData);\n    // Process the webhook data here\n    return { processed: true, data: shared.webhookData };\n  }\n}\n\nconst myWebhookFlow = new AsyncFlow(new WebhookTriggeredFlow());\n\n// --- Main Flow: Set up the WebhookNode and simulate a call ---\n(async () => {\n  console.log('--- Running WebhookNode Example ---');\n\n  // 1. Set up the WebhookNode\n  const webhookNode = new WebhookNode();\n  webhookNode.setParams({\n    port: 3000,\n    path: '/my-custom-webhook',\n    flow: myWebhookFlow // Pass the flow instance to be triggered\n  });\n\n  // Start the webhook listener in the background\n  const webhookFlow = new AsyncFlow(webhookNode);\n  webhookFlow.runAsync({}); // Run without awaiting to keep it listening\n  console.log('Webhook listener started on http://localhost:3000/my-custom-webhook');\n  console.log('Waiting for 3 seconds before simulating a webhook call...');\n\n  await new Promise(resolve => setTimeout(resolve, 3000)); // Wait for server to start\n\n  // 2. Simulate an incoming webhook call using HttpRequestNode\n  console.log('\\n--- Simulating incoming webhook call ---');\n  const simulateWebhookCall = new HttpRequestNode();\n  simulateWebhookCall.setParams({\n    url: 'http://localhost:3000/my-custom-webhook',\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: { event: 'user.created', data: { id: 123, name: 'John Doe' } }\n  });\n\n  try {\n    const response = await new AsyncFlow(simulateWebhookCall).runAsync({});\n    console.log('Simulated Webhook Call Response:', response);\n  } catch (error) {\n    console.error('Simulated Webhook Call Failed:', error.message);\n  }\n\n  console.log('\\n--- WebhookNode Example Finished ---');\n  // In a real application, you would keep the process alive for the webhook to continue listening.\n  // For this example, the process will exit after the simulated call.\n})();",
    "raw": "## WebhookNode\n\nThe `WebhookNode` exposes an HTTP endpoint to receive webhooks, triggering a specified qflow flow.\n\n### Parameters\n\n*   `port`: Optional. The port number to listen on. Defaults to 3000.\n*   `path`: Optional. The URL path for the webhook endpoint. Defaults to '/webhook'.\n*   `flow`: The qflow `AsyncFlow` instance to trigger when a webhook is received.\n*   `waitForFlow` (boolean, optional, default: false): This parameter controls the response behavior:\n    *   If `false` (the default), the server responds immediately with a predefined status and body, and triggers the flow in a \"fire-and-forget\" manner.\n    *   If `true`, the server will await the completion of the entire flow. It will then automatically take the return value of the flow, convert it to a JSON string, and send it as the HTTP response with a 200 OK status. If the flow throws an error, it will send a generic `500 Flow execution failed` error.\n*   `sharedSecret`: Optional. A shared secret for HMAC verification of incoming webhooks.\n*   `responseStatus`: Optional. The HTTP status code to send back. **Only used when `waitForFlow` is `false`**. Defaults to 200.\n*   `responseBody`: Optional. The JSON body to send back. **Only used when `waitForFlow` is `false`**. Defaults to `{ status: 'received' }`.\n\n### Example Usage\n\n#### Example 1: Fire-and-Forget\n\nThis is the default behavior. The webhook responds immediately and triggers a flow in the background.\n\n```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { WebhookNode, HttpRequestNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Define a flow to be triggered by the webhook ---\nclass WebhookTriggeredFlow extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    console.log('\\n[WebhookTriggeredFlow] Webhook received!');\n    console.log('[WebhookTriggeredFlow] Received data:', shared.webhookData);\n    // Process the webhook data here\n    return { processed: true, data: shared.webhookData };\n  }\n}\n\nconst myWebhookFlow = new AsyncFlow(new WebhookTriggeredFlow());\n\n// --- Main Flow: Set up the WebhookNode and simulate a call ---\n(async () => {\n  console.log('--- Running WebhookNode Example ---');\n\n  // 1. Set up the WebhookNode\n  const webhookNode = new WebhookNode();\n  webhookNode.setParams({\n    port: 3000,\n    path: '/my-custom-webhook',\n    flow: myWebhookFlow // Pass the flow instance to be triggered\n  });\n\n  // Start the webhook listener in the background\n  const webhookFlow = new AsyncFlow(webhookNode);\n  webhookFlow.runAsync({}); // Run without awaiting to keep it listening\n  console.log('Webhook listener started on http://localhost:3000/my-custom-webhook');\n  console.log('Waiting for 3 seconds before simulating a webhook call...');\n\n  await new Promise(resolve => setTimeout(resolve, 3000)); // Wait for server to start\n\n  // 2. Simulate an incoming webhook call using HttpRequestNode\n  console.log('\\n--- Simulating incoming webhook call ---');\n  const simulateWebhookCall = new HttpRequestNode();\n  simulateWebhookCall.setParams({\n    url: 'http://localhost:3000/my-custom-webhook',\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: { event: 'user.created', data: { id: 123, name: 'John Doe' } }\n  });\n\n  try {\n    const response = await new AsyncFlow(simulateWebhookCall).runAsync({});\n    console.log('Simulated Webhook Call Response:', response);\n  } catch (error) {\n    console.error('Simulated Webhook Call Failed:', error.message);\n  }\n\n  console.log('\\n--- WebhookNode Example Finished ---');\n  // In a real application, you would keep the process alive for the webhook to continue listening.\n  // For this example, the process will exit after the simulated call.\n})();\n```\n\n### Example 2: Wait for Flow and Return Result\n\nThis example demonstrates how to use `waitForFlow: true` to run a flow and return its result as the HTTP response.\n\n```javascript\nimport { AsyncFlow, AsyncNode } from '@fractal-solutions/qflow';\nimport { WebhookNode, HttpRequestNode } from '@fractal-solutions/qflow/nodes';\n\n// --- Define a flow that does some work and returns a result ---\nclass DataProcessingFlow extends AsyncNode {\n  async execAsync(prepRes, shared) {\n    console.log('\\n[DataProcessingFlow] Webhook received!');\n    const inputData = shared.webhookData;\n    console.log('[DataProcessingFlow] Processing data:', inputData);\n    \n    // Simulate some processing\n    const result = {\n      processed: true,\n      original_event: inputData.event,\n      timestamp: new Date().toISOString()\n    };\n\n    // The return value of this node will be the HTTP response\n    return result;\n  }\n}\n\nconst myDataFlow = new AsyncFlow(new DataProcessingFlow());\n\n// --- Main Flow: Set up the WebhookNode and simulate a call ---\n(async () => {\n  console.log('--- Running WebhookNode Example (waitForFlow: true) ---');\n\n  // 1. Set up the WebhookNode to wait for the flow\n  const webhookNode = new WebhookNode();\n  webhookNode.setParams({\n    port: 3001, // Use a different port for this example\n    path: '/webhook-wait',\n    flow: myDataFlow,\n    waitForFlow: true // Key parameter\n  });\n\n  // Start the webhook listener in the background\n  const webhookFlow = new AsyncFlow(webhookNode);\n  webhookFlow.runAsync({});\n  console.log('Webhook listener started on http://localhost:3001/webhook-wait');\n  console.log('Waiting for 1 second before simulating a webhook call...');\n\n  await new Promise(resolve => setTimeout(resolve, 1000));\n\n  // 2. Simulate an incoming webhook call\n  console.log('\\n--- Simulating incoming webhook call ---');\n  const simulateWebhookCall = new HttpRequestNode();\n  simulateWebhookCall.setParams({\n    url: 'http://localhost:3001/webhook-wait',\n    method: 'POST',\n    headers: { 'Content-Type': 'application/json' },\n    body: { event: 'data.process', data: { value: 42 } }\n  });\n\n  try {\n    // The 'response' here will be the result of myDataFlow\n    const response = await new AsyncFlow(simulateWebhookCall).runAsync({});\n    console.log('Simulated Webhook Call Response (from flow):', response);\n  } catch (error) {\n    console.error('Simulated Webhook Call Failed:', error.message);\n  }\n\n  console.log('\\n--- WebhookNode Example Finished ---');\n  // In a real app, you would keep the process alive.\n  process.exit();\n})();\n```"
  },
  "websockets": {
    "title": "WebSocketsNode",
    "description": "",
    "parameters": "*   `url`: The WebSocket URL to connect to.\n*   `action`: The action to perform: 'connect', 'send', 'receive', 'close'.\n*   `message`: The message to send (for 'send' action).",
    "returns": "Returns the connection/message result for the selected action.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { WebSocketsNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running WebSockets Test Workflow ---');\n\n  // 1. Connect to the WebSocket server\n  const connectNode = new WebSocketsNode();\n  connectNode.setParams({\n    action: 'connect',\n    url: 'wss://echo.websocket.org/', // A public echo server\n  });\n\n  // 2. Send a message\n  const sendNode = new WebSocketsNode();\n  sendNode.setParams({\n    action: 'send',\n    message: { text: 'Hello from qflow!' },\n  });\n\n  // 3. Receive the echoed message\n  const receiveNode = new WebSocketsNode();\n  receiveNode.setParams({\n    action: 'receive',\n  });\n\n  // 4. Close the connection\n  const closeNode = new WebSocketsNode();\n  closeNode.setParams({\n    action: 'close',\n  });\n\n  // Chain the nodes\n  connectNode.next(sendNode);\n  sendNode.next(receiveNode);\n  receiveNode.next(closeNode);\n\n  // Create and run the flow\n  const webSocketFlow = new AsyncFlow(connectNode);\n\n  try {\n    const finalResult = await webSocketFlow.runAsync({});\n    console.log('WebSocket workflow finished successfully.');\n    console.log('Final received message:', finalResult);\n  } catch (error) {\n    console.error('WebSocket workflow failed:', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { WebSocketsNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running WebSockets Test Workflow ---');\n\n  // 1. Connect to the WebSocket server\n  const connectNode = new WebSocketsNode();\n  connectNode.setParams({\n    action: 'connect',\n    url: 'wss://echo.websocket.org/', // A public echo server\n  });\n\n  // 2. Send a message\n  const sendNode = new WebSocketsNode();\n  sendNode.setParams({\n    action: 'send',\n    message: { text: 'Hello from qflow!' },\n  });\n\n  // 3. Receive the echoed message\n  const receiveNode = new WebSocketsNode();\n  receiveNode.setParams({\n    action: 'receive',\n  });\n\n  // 4. Close the connection\n  const closeNode = new WebSocketsNode();\n  closeNode.setParams({\n    action: 'close',\n  });\n\n  // Chain the nodes\n  connectNode.next(sendNode);\n  sendNode.next(receiveNode);\n  receiveNode.next(closeNode);\n\n  // Create and run the flow\n  const webSocketFlow = new AsyncFlow(connectNode);\n\n  try {\n    const finalResult = await webSocketFlow.runAsync({});\n    console.log('WebSocket workflow finished successfully.');\n    console.log('Final received message:', finalResult);\n  } catch (error) {\n    console.error('WebSocket workflow failed:', error);\n  }\n})();",
    "raw": "## WebSocketsNode\n\nThe `WebSocketsNode` provides real-time, two-way communication with web services.\n\n### Parameters\n\n*   `url`: The WebSocket URL to connect to.\n*   `action`: The action to perform: 'connect', 'send', 'receive', 'close'.\n*   `message`: The message to send (for 'send' action).\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { WebSocketsNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running WebSockets Test Workflow ---');\n\n  // 1. Connect to the WebSocket server\n  const connectNode = new WebSocketsNode();\n  connectNode.setParams({\n    action: 'connect',\n    url: 'wss://echo.websocket.org/', // A public echo server\n  });\n\n  // 2. Send a message\n  const sendNode = new WebSocketsNode();\n  sendNode.setParams({\n    action: 'send',\n    message: { text: 'Hello from qflow!' },\n  });\n\n  // 3. Receive the echoed message\n  const receiveNode = new WebSocketsNode();\n  receiveNode.setParams({\n    action: 'receive',\n  });\n\n  // 4. Close the connection\n  const closeNode = new WebSocketsNode();\n  closeNode.setParams({\n    action: 'close',\n  });\n\n  // Chain the nodes\n  connectNode.next(sendNode);\n  sendNode.next(receiveNode);\n  receiveNode.next(closeNode);\n\n  // Create and run the flow\n  const webSocketFlow = new AsyncFlow(connectNode);\n\n  try {\n    const finalResult = await webSocketFlow.runAsync({});\n    console.log('WebSocket workflow finished successfully.');\n    console.log('Final received message:', finalResult);\n  } catch (error) {\n    console.error('WebSocket workflow failed:', error);\n  }\n})();\n```"
  },
  "webview": {
    "title": "WebviewNode",
    "description": "/**",
    "parameters": "*   `html` (string, required): The HTML content to display in the webview.\n*   `title` (string, optional): The title of the webview window. Defaults to `'Qflow Webview'`.\n*   `width` (number, optional): The width of the webview window. Defaults to `800`.\n*   `height` (number, optional): The height of the webview window. Defaults to `600`.",
    "returns": "Returns \"default\" when the webview is closed.",
    "notes": "",
    "requirements": "",
    "examplesText": "",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { WebviewNode } from '@fractal-solutions/qflow/nodes';\n\nconst showMyUI = new WebviewNode();\nshowMyUI.setParams({\n  title: 'My Custom UI',\n  html: `\n    <html>\n      <body>\n        <h1>Hello from qflow!</h1>\n        <p>This is a custom UI rendered in a webview.</p>\n      </body>\n    </html>\n  `,\n  width: 600,\n  height: 400,\n});\n\nconst flow = new AsyncFlow(showMyUI);\nawait flow.runAsync({});",
    "raw": "# WebviewNode\n\nThe `WebviewNode` provides a simple way to display a webview window with custom HTML content. It's useful for showing rich, interactive UIs as part of a workflow.\n\n## Parameters\n\n*   `html` (string, required): The HTML content to display in the webview.\n*   `title` (string, optional): The title of the webview window. Defaults to `'Qflow Webview'`.\n*   `width` (number, optional): The width of the webview window. Defaults to `800`.\n*   `height` (number, optional): The height of the webview window. Defaults to `600`.\n\n## Example\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { WebviewNode } from '@fractal-solutions/qflow/nodes';\n\nconst showMyUI = new WebviewNode();\nshowMyUI.setParams({\n  title: 'My Custom UI',\n  html: `\n    <html>\n      <body>\n        <h1>Hello from qflow!</h1>\n        <p>This is a custom UI rendered in a webview.</p>\n      </body>\n    </html>\n  `,\n  width: 600,\n  height: 400,\n});\n\nconst flow = new AsyncFlow(showMyUI);\nawait flow.runAsync({});\n```"
  },
  "write_file": {
    "title": "WriteFileNode",
    "description": "/**",
    "parameters": "*   `filePath`: The absolute path to the file to write to.\n*   `content`: The content to write to the file.",
    "returns": "{Promise<void>} A promise that resolves when the file has been written.",
    "notes": "",
    "requirements": "",
    "examplesText": "```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { WriteFileNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running WriteFileNode Example ---');\n\n  const writeFile = new WriteFileNode();\n  writeFile.setParams({\n    filePath: './output.txt',\n    content: 'Hello, qflow! This is a test file.'\n  });\n\n  const writeFlow = new AsyncFlow(writeFile);\n\n  try {\n    const result = await writeFlow.runAsync({});\n    console.log('--- WriteFileNode Result ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```",
    "example": "import { AsyncFlow } from '@fractal-solutions/qflow';\nimport { WriteFileNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running WriteFileNode Example ---');\n\n  const writeFile = new WriteFileNode();\n  writeFile.setParams({\n    filePath: './output.txt',\n    content: 'Hello, qflow! This is a test file.'\n  });\n\n  const writeFlow = new AsyncFlow(writeFile);\n\n  try {\n    const result = await writeFlow.runAsync({});\n    console.log('--- WriteFileNode Result ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();",
    "raw": "## WriteFileNode\n\nThe `WriteFileNode` writes content to a specified file.\n\n### Parameters\n\n*   `filePath`: The absolute path to the file to write to.\n*   `content`: The content to write to the file.\n\n### Example Usage\n\n```javascript\nimport { AsyncFlow } from '@fractal-solutions/qflow';\nimport { WriteFileNode } from '@fractal-solutions/qflow/nodes';\n\n(async () => {\n  console.log('--- Running WriteFileNode Example ---');\n\n  const writeFile = new WriteFileNode();\n  writeFile.setParams({\n    filePath: './output.txt',\n    content: 'Hello, qflow! This is a test file.'\n  });\n\n  const writeFlow = new AsyncFlow(writeFile);\n\n  try {\n    const result = await writeFlow.runAsync({});\n    console.log('--- WriteFileNode Result ---');\n    console.log(result);\n    console.log('--- Workflow Finished ---');\n  } catch (error) {\n    console.error('--- Workflow Failed ---', error);\n  }\n})();\n```"
  },
  "user_input": {
    "title": "UserInputNode",
    "description": "Prompt the user in the terminal and capture the response. Stores the answer in shared.userInput.",
    "parameters": "* `prompt`: The message to display to the user.",
    "returns": "Returns the raw user input string and writes shared.userInput.",
    "notes": "",
    "requirements": "",
    "examplesText": "",
    "example": "const input = new UserInputNode();\ninput.setParams({ prompt: 'Enter a city: ' });\nconst flow = new AsyncFlow(input);\nconst result = await flow.runAsync({});",
    "raw": ""
  },
  "interactive_input": {
    "title": "InteractiveInputNode",
    "description": "Cross-platform GUI input prompt using system dialogs (zenity/kdialog, osascript, or PowerShell).",
    "parameters": "* `prompt`: Prompt text.\n* `title`: Optional dialog title.\n* `defaultValue`: Optional default value.",
    "returns": "Returns the entered string or null if the dialog is cancelled.",
    "notes": "",
    "requirements": "Requires GUI utilities (zenity/kdialog on Linux, osascript on macOS, PowerShell on Windows).",
    "examplesText": "",
    "example": "const dialog = new InteractiveInputNode();\ndialog.setParams({ prompt: 'Project name', title: 'Qflow', defaultValue: 'my-app' });\nconst result = await new AsyncFlow(dialog).runAsync({});",
    "raw": ""
  },
  "summarize": {
    "title": "SummarizeNode",
    "description": "Summarize text with an LLM. Requires an LLM node instance to be passed in params.",
    "parameters": "* `text`: Text to summarize.\n* `llmNode`: LLM node instance used for summarization.",
    "returns": "Returns the summary string and writes shared.summary.",
    "notes": "",
    "requirements": "Requires a configured LLM node instance.",
    "examplesText": "",
    "example": "const summarize = new SummarizeNode();\nsummarize.setParams({ text: longText, llmNode });\nconst summary = await new AsyncFlow(summarize).runAsync({});",
    "raw": ""
  },
  "s3": {
    "title": "S3CloudStorageNode",
    "description": "Interact with S3-compatible object storage using Bun's S3 client.",
    "parameters": "* `accessKeyId`, `secretAccessKey`, `bucket`, `endpoint`, `region`: S3 config.\n* `action`: upload|download|delete|presign|list.\n* `key`: Object key.\n* `content`: Content for upload.\n* `prefix`, `maxKeys`, `startAfter`: Listing options.\n* `acl`, `expiresIn`, `method`: Presign options.",
    "returns": "Returns action-specific result and writes shared.s3_result.",
    "notes": "",
    "requirements": "Requires Bun runtime and access to an S3-compatible endpoint.",
    "examplesText": "",
    "example": "const s3 = new S3CloudStorageNode();\ns3.setParams({\n  accessKeyId, secretAccessKey, bucket, endpoint,\n  action: 'upload', key: 'reports/report.json', content: JSON.stringify(data)\n});\nawait new AsyncFlow(s3).runAsync({});",
    "raw": ""
  },
  "agent_llm": {
    "title": "Agent LLM Nodes",
    "description": "Agent-specific LLM adapters that accept full conversation history as JSON string and return model output.",
    "parameters": "* `prompt`: JSON stringified messages array.\n* `apiKey`: Provider API key.\n* `keyword`: Optional log keyword.",
    "returns": "Returns the model response string and writes shared.llmResponse.",
    "notes": "",
    "requirements": "Requires provider API key and a valid model.",
    "examplesText": "",
    "example": "const agentLLM = new AgentDeepSeekLLMNode();\nagentLLM.setParams({ prompt: JSON.stringify(messages), apiKey });\nconst result = await agentLLM.runAsync({});",
    "raw": ""
  }
};

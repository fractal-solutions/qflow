## IteratorNode

The `IteratorNode` iterates items, executes sub-flow for each.

### Parameters

*   `items`: The list of items to iterate over.
*   `flow`: The name of the flow to execute for each item, as registered in the flow registry.
